# Changelog

Every release on `dev` has a section here, newest first. `version-check.yml`
fails a push that bumps `package.json` without adding one, so this file is the
complete record of what shipped and when — read it top-down to learn how the
system got here, and `docs/FEATURES.md` to learn what it does today.

Generated by `scripts/changelog.sh backfill`; new releases are written by hand
(`scripts/changelog.sh draft` prints a starting point). Sections at v1.16.1 and
older, and the appendix at the foot of this file, are hand-written prose from
the era before the checks existed and are richer than the generated bullets.

Every version `package.json` has ever held has a tag and a section here —
`scripts/changelog.sh check` asserts it. Four early releases (0.1.0, 1.9.0,
1.52.0, 1.53.0) shipped without a tag at the time and were tagged retroactively
at the last commit that carried each version.

## [Unreleased]

## [1.123.1] - 2026-09-03

### Fixes
- fix(scan): **a label scan that times out is retried once instead of becoming
  an error dialog.** Seen in production: a purchaser held their phone over a RAM
  stick, waited twenty seconds, got "OCR failed", re-shot the same label, and
  the second attempt came back in two. The retry belongs on this side of the
  phone. Only a timeout is retried — an HTTP error from the model would fail
  the same way twice.

  Both attempts and the existing not-valid-JSON re-ask now share **one 45-second
  budget**. Each of those used to mint a fresh 20-second signal, so a single
  scan could spend forty seconds on the model on top of the route's own
  fifteen-second image upload, with nothing bounding the pair. The receipt
  renamer shares this client, so its worst case is now bounded too.
- fix(boot): **a database that is slow to accept connections during a deploy no
  longer takes production down until someone redeploys by hand.** The container
  runs `migrate.mjs && init-admin.mjs && pnpm start`, so a failed migration
  means the server never starts; Railway then burns its `ON_FAILURE` retries and
  the service **stays down after Postgres comes back**. The migration runner now
  treats an unreachable database as the transient it usually is: six attempts
  with backoff over roughly twenty-three seconds, each logged, before exiting
  non-zero. Its connect timeout drops from the postgres.js default of thirty
  seconds to five, so the retries fit inside a sensible window. A migration that
  fails on its *SQL* still exits immediately — only the connect is retried.
- fix(fx): the exchange-rate fetch carries a 20-second timeout, matching every
  other outbound client. It was the only one without a signal, bounded only by
  undici's ~300-second default — and it is reachable from request handlers, not
  just the six-hourly loop: the manager refresh, and, on a cold rate table, the
  **unauthenticated** vendor portal ([RS-020](docs/tickets/RS-020-boot-fx-and-ocr-each-hang-or-die-on-a-transient-that.md)).

## [1.123.0] - 2026-09-03

### Fixes
- fix(shipping): **a demo shipping label no longer charges the purchase order.**
  Production has been running on the stub label provider since the feature
  shipped — `SHIPSAVING_APP_KEY`/`SECRET` are unset, which is deliberate, since
  credentials may lag a deploy. What was not deliberate is where the demo money
  went: the stub returns plausible prices (`USPS Priority $12.45`), the rate
  object carried no marker saying which provider produced it, and buying folded
  that invented figure into `orders.other_fees` — where `po-cost.ts` amortizes
  it into per-line cost and commission. The only warning was a `Demo` chip
  suppressed for `draft` and `quoted`, so it appeared *after* the money was on
  the order.

  A demo purchase now moves nothing: `other_fees` is untouched, and the row
  carries `fees_applied = FALSE` with `label_cost = NULL` (the two are read
  together — by the void reversal, and by the cost tape's shipping-vs-other
  split, which would otherwise re-badge a real fee as shipping). The demo price
  still lands in `rate_amount` for display. Voiding such a label subtracts
  nothing, and the manager notification says a demo label was made rather than
  quoting a dollar figure nobody paid.

  The label wizard now says so before you commit rather than after: the rates
  step returns which provider quoted it, and demo rates carry a `Demo` chip, a
  callout, "No charge" in place of the amount, and a *Make demo label* button.

  Found by reading six days of production logs. No purchase order was affected
  — the window contained one shipment, zero rate fetches and zero label buys
  ([RS-013](docs/tickets/RS-013-demo-shipping-labels-must-not-charge-a-po-and-bank-s.md)).
- fix(payments): **the bank reconciliation loop now reports what it did.** It
  syncs Mercury and PayPal every six hours, and `doSync` deliberately catches
  each provider's failure into its own slot so one bank being down cannot block
  the other — which meant the loop's own `catch` never fired for the failure
  that mattered, and the result it returned was dropped on the floor. Nothing
  logged on success either, so six days of production logs contained no trace
  of the loop at all: twenty-four clean passes and twenty-four silent failures
  looked identical, and the only way to tell was to open Payments and press
  Sync now. Each pass now emits a `warn` naming any source that failed and its
  error, and an `info` carrying per-source counts otherwise — through
  `lib/log.ts`, so the lines are structured and carry the build.

### Other
- The paid ShipSaving label path is covered by tests for the first time. Every
  shipping money assertion previously ran through the stub, which was
  indistinguishable from the real thing only because the stub also moved money;
  with that fixed, the fee fold and the void reversal would have had no test at
  all. `tests/helpers/shipsaving.ts` fakes the v2 account so the real path runs
  through the real route.

## [1.122.0] - 2026-09-03

### Changes

- **The cold load stops waiting on things it never needed to wait for.**  Three
  measurements shaped this.  The access token lived 15 minutes, and the
  production log showed `/api/auth/refresh` firing 19 times against 23 app
  mounts — four out of five loads opened with the whole bootstrap 401ing, a
  refresh, and a retry, three serial round trips before a single pixel, because
  a user who steps away for a coffee comes back past the deadline.  It is now
  60 minutes.  What actually bounds a stolen cookie is the rotating refresh
  family and its reuse-revocation, not that number; and deactivating a user
  still takes effect on their very next request, because `authMiddleware`
  re-checks `active = TRUE` every time rather than trusting the JWT.

  Second, nothing started until the bundle had.  The shell is chosen at runtime
  by viewport width, so Vite cannot emit a `modulepreload` for it and the
  browser only learned it needed `DesktopApp-*.js` after parsing 255 KB of
  JavaScript — and `/api/me`, `/api/lookups` and `/api/workspace`, none of which
  needs React, waited behind the same parse.  A small boot script now goes in
  ahead of the entry and starts all of it.  A prefetch that comes back 401 costs
  nothing: it resolves to null and the normal path refreshes and retries.  The
  two halves ship together deliberately — they already overlapped each other, so
  either alone would have saved little.

  Third, `/api/warehouses` had ten call sites across nine components, each with
  its own `useEffect` and no shared state; one session asked for it six times in
  1.6 seconds.  It now uses the same single-flight module cache as lookups and
  workspace settings, invalidated explicitly by the Settings panel that writes
  it.

- **And the load is now measurable rather than felt.**  `POST
  /api/client-timings` records navigation timing, LCP, and how many requests and
  refreshes a page load actually cost — the sibling of the existing client-error
  sink, and the thing whose absence meant the original report could only be
  "sometimes it feels slow".  Numbers only; nothing that could carry a URL.

## [1.121.1] - 2026-09-03

### Fixes

- **A deploy no longer breaks tabs that were already open.**  Chunk filenames
  are content-hashed, so shipping a release replaces the whole asset manifest
  and the names an open tab still holds stop existing.  Asking for one of them
  did not return a 404: `not_found_handling = "single-page-application"` treated
  every miss under `/assets/` as a hit on `index.html`, so the browser was
  handed a document where it had asked for an ES module.  The lazy page never
  loaded, the skeleton never resolved, and the tab sat there looking slow — the
  shape most of the "the page is loading slowly" reports actually took.  Worse,
  the `/assets/*` rule in `_headers` stamped that HTML `immutable, max-age=1y`,
  so the failure outlived the deploy that caused it by a year.  It also meant
  the Worker never ran for those paths, so a missing asset was answered over
  plain http with no redirect to https.

  A missing hashed asset now returns a real 404, the Worker owns the SPA
  fallback for actual document paths, and a tab that hits a replaced chunk
  reloads itself once onto the current build instead of stalling.  Recovery
  hangs off Vite's own `vite:preloadError`, so it covers every lazy route in
  the app and any added later without a per-call-site wrapper.

## [1.121.0] - 2026-09-03

### Changes

- **The Payments ledger has an Owner column.**  A payment's owner used to be a
  `Assigned to …` chip tucked into the Status cell beside whatever that cell
  already said — an annotation on the status rather than a fact about the row,
  impossible to scan down the page, and widening the Status cell on exactly the
  rows that carried it.  That placement was a compromise from when the table had
  no room for it; merging Status and Purchase order in v1.120.0 made the room.

  Owner now sits between Amount and Status as the person's avatar and first
  name, full name on hover, with a muted dash where nobody owns the payment.
  Owner and purchase order are mutually exclusive by database constraint —
  linking a payment clears its owner — so the column is deliberately all dashes
  on the Linked tab and behind the Refunds tile.  Initials come from the API
  rather than being derived from the name, because a member can be renamed
  without their stored initials being recomputed.

## [1.120.0] - 2026-09-03

### Changes

- The Payments ledger's right-hand side used to be a wall.  Every row in the
  Unlinked queue printed an amber "Unlinked" chip — a whole column restating
  the tab that was already selected — beside a Purchase order cell that carried
  the suggestion *and* two or three buttons at equal weight.  On the rows that
  actually had a match those buttons wrapped, so those rows stood a line taller
  and the table stepped; and because they were left-aligned in a cell whose
  width followed its content, no two `Link…` buttons shared an x.

  Status and Purchase order were one question asked twice — what is this money
  attached to? — so they are now one column that answers it: a linked PO with
  its cost, a suggested PO with how far apart the two dates are, `Transfer`,
  `Ignored`, or `Unlinked`.  The seventh column freed up by the merge is an
  actions rail: one primary button per row, flush right, at the same x on every
  row.  `Ignore`, `Not it` and `Not the same` step back and appear when the row
  is hovered or takes keyboard focus — they keep their place in the tab order,
  and a device without hover shows them permanently.  Rows are now a uniform
  height whatever state they are in.

## [1.119.1] - 2026-09-02

### Fixes

- A company-paid purchase order that never named the payment funding it could
  be left behind silently.  The carrier reporting the box in transit runs the
  same advance a person does, and the same rule blocks it — but where a person
  gets a 409 explaining why, the carrier path threw the answer away: the
  shipment moved, the order stayed in Draft, and nothing anywhere said so.  It
  now records the stall against the order, the shipment and the tracking
  number.  The order still needs a human to add the transaction id; the
  difference is that the stall is findable instead of being mistaken for the
  rule no longer applying.
- Assigning an owner to a payment whose two legs are only half linked returned
  a server error.  A paired payment is read from its PayPal leg, so a pair
  linked through its Mercury leg looked unlinked, the assignment was allowed,
  and it then collided with the constraint that keeps a payment from being
  owned and answered at once.  Unlinking hit the same blind spot from the other
  side and reported the payment as "not linked", which left no way out of the
  state at all.  Both now read the whole payment rather than one leg of it, as
  do ignoring, marking a transfer, and linking.

## [1.119.0] - 2026-09-01

### Features
- feat(payments): internal transaction records, and an owner for payments with
  no PO. Two things a bank row had nowhere to put. **Internal transactions** are
  manager-written records that group the rows of one internal money movement — a
  Mercury→PayPal transfer, a card-funding chain — and carry the note explaining
  it; filing a row takes it off the reconciliation queue, and the record's
  totals count a transfer's two opposite legs as the two real movements they are
  while still counting a payment pair once. **Assigning a member** does the
  opposite on purpose: it leaves the payment in the queue, because a payment
  that needs explaining still needs explaining — it just now has someone to
  explain it, and an Assignee filter to find it by. The two are mutually
  exclusive with a PO link: linking clears the owner (a triage tag), and refuses
  a filed row (part of a record someone wrote a note on).
## [1.118.0] - 2026-08-31

### Features
- feat(orders): **a PayPal transaction ID now links the PO and the payment the
  moment a human types it, from whichever side they type it on.** v1.115.0 and
  v1.116.0 both made the ID mandatory on the promise that it "is what auto-link
  matches on" — but the only thing relating `orders.paypal_txn_id` to
  `bank_transactions.paypal_txn_id` was `autoLink()` inside the bank sync, which
  runs **every six hours**. So a purchaser could type the ID and leave a manager
  reading an unlinked queue whose answer was already in the database; and a
  manager who linked the payment on the Payments page wrote nothing back, so the
  PO still read as unpaid and auto-link still had nothing to match on.

  Both write points now close the loop. Saving a transaction ID on a PO —
  through the edit form, on create, or on the draft PO minted from a delivered
  package's screenshot scan — claims the matching transaction immediately, and
  records it as a human link (`link_auto = FALSE`) rather than an automatic
  guess. Linking on the Payments page fills the PO's transaction ID and writes
  the fill into the order's activity log against the manager who did it.

  Two collisions decide in favour of whoever spoke first. A PO that already
  names a *different* transaction keeps what the purchaser typed — the payment
  still links, the field is not overwritten. And a typed ID claims only *free*
  transactions: one already linked to another PO, one a manager ignored, and one
  a manager deliberately unlinked (the `no_auto_link` tombstone) are all left
  where they are.

  The write itself is one shared function, so the typed path and the sync path
  cannot drift. It carries one tightening the six-hour cadence had made
  unreachable and instant links do not: the free leg of a pair whose other leg
  belongs to another PO is no longer claimable, because that splits one payment
  across two orders — a state `POST /:id/pair` already refuses outright
  ([RS-010](docs/tickets/RS-010-paypal-transaction-id-links-the-po-and-the-payment-i.md))

## [1.117.0] - 2026-08-31

### Features
- feat(payments): **a linked payment now shows what the PO cost, beside the PO
  it paid.** The Linked tab ended each row with a bare `PO-1400` pill while the
  money that moved sat two columns to its left, so the only question worth
  asking of that list — did this payment cover that PO? — could not be answered
  without opening the PO. One screenshot had `PO-1412` on two separate $2,800
  rows with no way to tell whether it was now settled, short, or paid twice.
  The figure shown is the PO's cost as its own page states it: goods plus
  `other_fees`, not the goods total alone. Fees are a separate column the bank
  very much did charge — it is why the matcher already scores a payment against
  both — so a goods-only number would have read as a shortfall on most
  fee-carrying POs and manufactured the confusion it was added to remove.
  ([RS-009](docs/tickets/RS-009-linked-payments-do-not-show-the-po-s-cost.md))

## [1.116.0] - 2026-08-31

### Features
- feat(shipping): **adding a tracked package now requires the PayPal transaction
  ID.** The field had been optional on the Add-package form since it landed in
  v1.87.0, which quietly cost more than a blank column: `routes/packages.ts`
  copies a package's `paypal_txn_id` onto the PO minted from the delivered box,
  and `banktx/sync.ts` auto-links a Mercury/PayPal row to a PO on exactly that
  value. A package added without one therefore became a PO that could only be
  reconciled by a manager guessing from amount and date. Submitting without the
  ID now opens the same blocking dialog every other blocked submit uses, and —
  because the Payments page is manager-only and a purchaser has no way to look
  the ID up — the dialog names the one route they do have: ask the manager who
  paid for the order. The gate lives in the shared `useAddPackageForm` hook, so
  the phone and the desktop page cannot drift on it, and it sits ahead of the
  double-submit latch rather than behind it, where an early return would have
  swallowed every later attempt in silence. This is the shipping-side twin of
  v1.115.0's PO rule: a package added with an ID mints a PO that already
  satisfies the advance guard, so nobody is asked twice
  ([RS-008](docs/tickets/RS-008-adding-a-tracked-package-requires-a-transaction-id.md))

## [1.115.0] - 2026-08-31

### Features
- feat(orders): **a company-paid PO now names the payment that funded it.**
  `orders.payment` and `orders.paypal_txn_id` had sat side by side since `0001`
  and `0099` with nothing relating them, so a purchaser could file a company-paid
  PO, move it to In Transit, and leave no record of which payment covered it —
  reconciliation then happened backwards, a manager on the Payments page guessing
  from amount and date. Leaving Draft now requires the transaction ID. The guard
  lives in `advanceOrderTx`, not the route, so it holds for a manager stage-jump
  and for carrier movement too; a rule the two commonest paths could route around
  would not be one. Self-pay POs are untouched.

  The field is also what auto-link matches on, so this does more than create a
  paper trail: a company-paid PO now arrives able to reconcile itself the moment
  Mercury/PayPal syncs.

  Scoped to orders created after the rule reaches an environment — migration
  `0115` stamps the cutoff as `NOW()`, so each environment grandfathers exactly
  what it already had. The server decides and reports it as `txnRequired` on the
  order, because a shell that judged for itself would block the very pre-cutoff
  drafts the rule exempts. Mobile gained the field as an input; it had been
  read-only there, which left a mobile purchaser no way to satisfy the rule at
  all.

## [1.114.2] - 2026-08-31

### Fixes
- fix(orders): **a long activity history no longer opens a dead band above
  "Order status" on the desktop PO page.** The side column's "scroll inside your
  own card" rule was addressed by position — `:nth-child(2)` — but the aside has
  a conditional middle child: the manager-only payments ledger, which appears
  once a bank transaction is linked to the PO. On those orders the cap landed on
  the ledger and the activity log ran to its full natural height, roughly a
  screen and a half for 47 events. The log is now addressed by class
  (`.oe-side-activity`), which is the only thing that holds: the component
  renders nothing until its own fetch lands, so the child count moves 1 → 3
  during load and no positional selector is right throughout. The grid rows
  changed with it (`1fr auto` → `auto 1fr`, with the action card pinned to the
  top of its row) so that if the side column is ever the taller of the two, the
  surplus is trailing space *under* the status bar instead of a gap above it.

## [1.114.1] - 2026-08-31

### Fixes
- fix(logging): **portal tokens stopped reaching the log stream.**
  `/api/public/shipping/<token>` was missing from the redaction list while its
  own route header says the token in the URL *is* the credential, so every
  seller-fill request wrote one to stdout — including through the new
  one-line-per-request logger. And a client error report stored `href` raw
  beside a `path` that was carefully redacted, which is the likelier leak of the
  two: a purchaser checking what a counterparty sees sits on `/v/<token>`, so
  the token is in the address bar of every report the page sends. The new
  redactor works on the raw string rather than parsing it, because `new URL()`
  throws on the relative hrefs a browser can post and recomposing the pieces
  would drop the hash the SPA keeps its whole route in.
- fix(orders): **`supplierId` is checked at the boundary, and a client's name is
  scoped to the person reading it.** It was the one FK on `orders` with no
  validation, so a malformed id 500ed instead of 400ing — and any well-formed id
  was accepted, including another purchaser's, whose name the unscoped join then
  handed back. Reads are owner-scoped precisely because seller addresses arrive
  from shipments, which are already scoped to the ordering purchaser.
- fix(clients): **owner changes have one door again.** `PATCH /:id` wrote
  `owner_id` through the generic column setter, skipping the `owner_changed`
  timeline row, the target-purchaser check and the collision 409 that
  `POST /:id/reassign` exists to provide — a book could change hands as a silent
  column update.
- fix(clients): **the suggestion rail counts purchase orders, not boxes.**
  `shipments` is one row per carton, so a PO shipped in three counted three
  times and added its cost three times; the rail advertised "3 POs · $36,000"
  for one $12,000 order and, since spend decides which suggestions survive,
  ranked multi-parcel sellers above genuinely bigger ones.
- fix(inventory): **a blanked RPM or health actually clears now.** Both became
  editable dropdowns whose blank option sends `null`, but the columns stayed on
  `COALESCE`, which reads `null` as "no change" — so the save reported success
  and the value snapped back. The audit trail made it worse rather than catching
  it, logging a `7200 → null` edit the database had refused.
- fix(payments): **the unlinked and suggested tiles take the same direction lens
  as the list.** The queue defaults to money out while the tiles counted both
  directions, so the number disagreed with the rows beneath it — and a drained
  money-out queue reported "nothing left to reconcile" while unlinked incoming
  payments sat unseen.
- fix(ci): the frontend is typechecked and tested. `apps/frontend/**` was in no
  workflow's path filter, so a frontend-only change ran neither — while
  `deploy-frontend.yml` shipped it to the production Worker on merge. 350
  frontend tests existed and gated nothing.

## [1.114.0] - 2026-08-31

### Features
- feat(backend): **one log module, every line stamped with the release version**
  and correlated to the request that caused it. An `AsyncLocalStorage` context
  opened by the outermost middleware carries a `requestId` (also returned as
  `X-Request-Id`) into every `log.*` call downstream — including calls inside a
  `.catch()` a route registered and never awaited — without threading anything
  through. One structured line per completed request replaces Hono's default
  logger, with the URL redacted the same way the error sink redacts it, because
  vendor portal tokens travel in the path.
- feat(backend): the silent failure paths got a log line. `/api/health` now
  resolves its version and commit through the same helpers the logs use, so the
  health payload and the logs can never disagree about which build is running.
- feat(ci): `backend-tests.yml` runs typecheck and the backend suite against a
  real Postgres on every push.

### Fixes
- fix(mcp): a tool call that ran and failed is logged. Since the reply became a
  tool *result* rather than a JSON-RPC error it carries no error code and rides
  an HTTP 200, so without this a broken write tool left no trace anywhere.
- fix(tests): the template migration is serialised behind an advisory lock. On
  a fresh cluster — exactly what CI brings up — parallel workers raced
  check-then-act on `0042 CREATE ROLE metrics` and could hand a test a
  half-migrated template.

## [1.113.0] - 2026-08-31

### Features
- feat(inventory): the Inventory **screens read in the same order as the workbook
  they download**. The export moved to the vendor bid sheet's sequence in
  v1.107.0 — brand, then capacity, speed — and the screens stayed on recency, so
  the page and the file disagreed. Both list routes now ship category rank first
  (RAM, SSD, HDD, Other), then brand, capacity, speed: a workbook gets the
  category grouping free from its tabs, a screen is one flat table and has to
  earn it. Sorted after the routes' caps, deliberately — *which* rows appear is
  unchanged, only their arrangement. The phone list drops its 30-row window,
  which under the new order would have been RAM-only and would never have shown
  a line you just submitted. (RS-004)

## [1.112.0] - 2026-08-31

### Features
- feat(banktx): **two transactions can be grouped into one payment by hand.**
  `autoPair` builds its amount bucket over the whole unpaired set with no date
  bound, so a single stale same-amount leg suppresses a pairing forever — and
  the manual endpoint for exactly that case (`POST /:id/pair`, shipped in
  v1.91.0) had never been reachable from the UI. Unpaired rows now carry a
  `pairCandidate` when the match is unique from both sides, and a
  "Group with…" picker (`GET /:id/pair-candidates`) covers the case where no
  single candidate stood out.

### Fixes
- fix(banktx): the group suggestion now **outranks the PO suggestion**. Two
  ungrouped legs of one payment each showed their own "Likely PO-xxxx", and
  `/link` has no double-claim guard, so working the queue could put twice the
  money against one order. Until the manager has decided whether the legs are
  one payment, linking either is premature.

## [1.111.0] - 2026-08-31

### Features
- feat(payments): the reconciliation queue **defaults to money out**. Every tile
  but Refunds sits at that direction, so "no direction lens" now means money
  out rather than all — an empty unlinked queue reads as drained, which is only
  a lie under the money-in lens. Tiles gained subtitles and a real `active`
  class instead of inline border styling.
- feat(payments): the **PO picker is no longer clipped** by the table. Its
  trigger sits in a `.table-scroll` cell whose `overflow-y: hidden` sheared the
  dropdown off at the last row; it now positions itself against the viewport
  and flips above the row when there is no room below.

### Fixes
- fix(payments): the filter selects zero their vertical padding — at 32px the
  `.select` box clipped descenders, so "Money out" read as "Monev out".

## [1.110.0] - 2026-08-31

### Features
- feat(inventory): an item's **specs are editable from the item itself**. The
  Details tab rendered brand, capacity, generation and the rest through a helper
  that wore the `.input` class with `pointerEvents: none` — seven boxes that
  looked like text fields and swallowed every click, so a mislabelled brand
  could only be fixed from the order that brought it in. `PATCH
  /api/inventory/:id` now accepts the spec columns, mirroring what `PATCH
  /api/orders/:id` already does rather than inventing a second mechanism.
  Category stays fixed here, which is what lets the stale-column clear and the
  synthetic part-number rebuild be skipped.

## [1.109.0] - 2026-08-31

### Features
- feat(orders): on mobile, the four add-an-item targets are **docked above the
  action bar** on the PO edit screen instead of sitting at the end of the line
  list. Saving a line leaves the screen and comes back, which remounts it at
  the top of the scroll, so the row a purchaser needed next receded by one line
  every time they used it — the feature got harder to use the more it was used.
  Each category keeps its own direct target: a single "Add another" button
  would put the old category mode back in the user's head. The bar became a
  two-row stack rather than growing a second floating strip, because a strip
  would have had to hard-code a bar height that changes with whether the order
  can be deleted, archived or saved (RS-003)

## [1.108.0] - 2026-08-31

### Features
- feat(clients): the people we buy from are a record, not free text. Purchase orders had no counterparty at all — who we bought from survived only as `shipments.from_name`, `packages.seller_name` and a blob in `orders.notes`, which is why `banktx/match.ts` fuzzy-matches strings to reconcile a payment. New `suppliers` table (UI: **Clients**; 供货商, since 客户 is already the sell-side customers) with an owner, structured preferences, a contact log and `orders.supplier_id`. Standing (prospect/active/archived) is stored because a human decides it; **tier, health and the follow-up date are derived per read** from order history, the same discipline `orders.category` and `total_cost` follow — a stored status is one that goes stale
- feat(clients): a client is judged against **their own rhythm**, not a company-wide threshold. Silent past twice the median gap between their POs reads as "gone quiet", past four times as "lost touch". A weekly seller quiet for three weeks is in trouble; a twice-a-year seller quiet for three weeks is fine, and one fixed threshold would nag about the second while saying nothing about the first
- feat(clients): desktop **Clients** page at `/clients` for both roles, opening on "Needs a call" rather than everything — a list of four is a task, a list of sixty-seven is a chore someone closes. No system vocabulary reaches the screen: tier A/B/C renders as Top seller / Regular / Occasional, health as On track / Gone quiet / Lost touch, a prospect as a New lead. The mapping lives in `lib/clients.ts` so a future field can't leak `dormant` or `adopt` into the UI. The rhythm strip draws each PO as a mark across a year with the silence since the last one as a bar — the rule stays sophisticated while the screen stays simple
- feat(clients): logging a call is two taps and no typing — kind pre-selected, outcomes as chips, and the next call already scheduled from the client's tier cadence, so **doing nothing is the correct action**. If logging cost more than that, purchasers would make calls and not log them, and the follow-up list would start lying
- feat(clients): migration `0114` seeds the book from shipping history so it is useful on day one, grouping on the compressed name the generated `match_key` derives from — not the punctuation-sensitive key `/api/shipping/contacts` uses, or two spellings form separate groups and then collide on insert. Display name is the spelling used most often, contact details come from the newest row, and follow-ups spread over two weeks so day one isn't sixty calls
- feat(clients): buying a shipping label deliberately creates **nothing** — sellers surface in a suggestion rail instead. This business buys from plenty of people once, and auto-creating would bury the twenty relationships that matter
- feat(clients): naming a client is bookkeeping, like a note — `supplier_id` is in `META_FIELDS` for the audit trail but **not** in `materialEdit`, so attributing a submitted PO does not bounce it back to Draft (v1.97.0's revert rule). `orderDraft.ts`, the second `INSERT INTO orders`, carries it too, or the package→PO path stays unattributed

## [1.107.0] - 2026-08-31

### Features
- feat(inventory): export rows in the vendor bid sheet's order ([#222](https://github.com/JinhuW/recycle_servres_erp/pull/222))

## [1.106.0] - 2026-08-31

### Features
- feat(scan): make the purchaser name a RAM brand the AI couldn't read ([#221](https://github.com/JinhuW/recycle_servres_erp/pull/221))

## [1.105.0] - 2026-08-31

### Features
- feat(observability): record client-side failures instead of losing them ([#219](https://github.com/JinhuW/recycle_servres_erp/pull/219))

## [1.104.1] - 2026-08-31

### Fixes
- fix(parts,warehouses): release-review findings before the prod cut ([#216](https://github.com/JinhuW/recycle_servres_erp/pull/216))

## [1.104.0] - 2026-08-28

### Features
- feat(parts): one part number, however it was typed ([#215](https://github.com/JinhuW/recycle_servres_erp/pull/215))

## [1.103.0] - 2026-08-28

### Fixes
- fix(warehouses): saved shipping address was hidden by a stale cache ([#214](https://github.com/JinhuW/recycle_servres_erp/pull/214))

## [1.102.1] - 2026-08-28

### Fixes
- fix(shipping,parts): release-review findings before the prod cut ([#211](https://github.com/JinhuW/recycle_servres_erp/pull/211))

## [1.102.0] - 2026-08-27

### Features
- feat(shipping): track packages through Shippo, driven by webhooks ([#209](https://github.com/JinhuW/recycle_servres_erp/pull/209))

## [1.101.0] - 2026-08-27

### Features
- feat(parts): suggest existing part numbers while typing ([#208](https://github.com/JinhuW/recycle_servres_erp/pull/208))

## [1.100.1] - 2026-08-27

### Fixes
- fix(banktx,scan): release-review findings on the PO matcher ([#206](https://github.com/JinhuW/recycle_servres_erp/pull/206))

## [1.100.0] - 2026-08-27

### Features
- feat(shipping): AI reads the PayPal seller name, and packages record their source ([#205](https://github.com/JinhuW/recycle_servres_erp/pull/205))

## [1.99.0] - 2026-08-27

### Features
- feat(banktx): suggest matching POs for unlinked bank transactions ([#204](https://github.com/JinhuW/recycle_servres_erp/pull/204))

## [1.98.0] - 2026-08-27

### Features
- feat(shipping): name who submitted each tracked package ([#202](https://github.com/JinhuW/recycle_servres_erp/pull/202))

## [1.97.2] - 2026-08-27

### Features
- feat(scan): say so when AI recognition is unavailable, and name the escalation ([#201](https://github.com/JinhuW/recycle_servres_erp/pull/201))

## [1.97.1] - 2026-08-27

### Fixes
- fix(po,banktx): release-review findings on the revert and transfer rules ([#199](https://github.com/JinhuW/recycle_servres_erp/pull/199))

## [1.97.0] - 2026-08-26

### Features
- feat(po): purchaser edits before Done send the PO back to Draft ([#198](https://github.com/JinhuW/recycle_servres_erp/pull/198))

## [1.96.2] - 2026-08-26

### Fixes
- fix(banktx): auto-classify Mercury<->PayPal internal transfers ([#197](https://github.com/JinhuW/recycle_servres_erp/pull/197))

## [1.96.1] - 2026-08-26

### Fixes
- fix(shipping): harden the label-scan flow — release-review findings ([#193](https://github.com/JinhuW/recycle_servres_erp/pull/193))

## [1.96.0] - 2026-08-26

### Features
- feat(scan): AI label scanner for SSD lines, gated by categories.ai_capture ([#195](https://github.com/JinhuW/recycle_servres_erp/pull/195))

## [1.95.1] - 2026-08-26

### Features
- feat(scripts): name the tmux tab after the session in new-session.sh ([#194](https://github.com/JinhuW/recycle_servres_erp/pull/194))

### Fixes
- fix(shipping): one-line Create PO button, PayPal txn id on shipping rows ([#191](https://github.com/JinhuW/recycle_servres_erp/pull/191))

## [1.95.0] - 2026-08-26

### Features
- feat(shipping): mobile label scan — lookup, note, create-PO; Shipping tab for managers ([#192](https://github.com/JinhuW/recycle_servres_erp/pull/192))

## [1.94.0] - 2026-08-26

### Features
- feat(payments): auto-classify transfers — Mercury kind + counterparty rules ([#189](https://github.com/JinhuW/recycle_servres_erp/pull/189))

## [1.93.2] - 2026-08-26

### Fixes
- fix(pwa): re-check for a new service worker on foreground and hourly ([#188](https://github.com/JinhuW/recycle_servres_erp/pull/188))

## [1.93.1] - 2026-08-26

### Fixes
- fix(shipping): let iPhone users pick the PayPal screenshot from the photo library ([#187](https://github.com/JinhuW/recycle_servres_erp/pull/187))

## [1.93.0] - 2026-08-25

### Features
- feat(payments): classify internal Mercury<->PayPal transfers out of the unlinked queue ([#185](https://github.com/JinhuW/recycle_servres_erp/pull/185))

## [1.92.2] - 2026-08-25

### Fixes
- fix(payments): keep the filter strip on one line ([#183](https://github.com/JinhuW/recycle_servres_erp/pull/183))

## [1.92.1] - 2026-08-25

### Fixes
- fix: the two request errors prod was actually logging ([#182](https://github.com/JinhuW/recycle_servres_erp/pull/182))

## [1.92.0] - 2026-08-25

### Features
- feat(payments): manager-only Payments page — bank-transaction reconciliation UI ([#180](https://github.com/JinhuW/recycle_servres_erp/pull/180))

## [1.91.0] - 2026-08-25

### Features
- feat(banktx): manager-only bank-transactions API + background sync loop ([#179](https://github.com/JinhuW/recycle_servres_erp/pull/179))

## [1.90.0] - 2026-08-25

### Features
- feat(banktx): Mercury/PayPal transaction ingest with auto-pair and auto-link ([#178](https://github.com/JinhuW/recycle_servres_erp/pull/178))

## [1.89.0] - 2026-08-24

### Features
- feat(orders): hide Done POs by default in the PO list, with a toggle to show them ([#175](https://github.com/JinhuW/recycle_servres_erp/pull/175))

### Other
- chore: bump to 1.89.0 (v1.88.0 taken by a parallel session's #174)

## [1.88.0] - 2026-08-24

### Features
- feat(orders): mobile managers see the whole org's POs, Done hidden by default ([#174](https://github.com/JinhuW/recycle_servres_erp/pull/174))

## [1.87.0] - 2026-08-24

### Features
- feat(shipping,orders): PayPal payment screenshot on add-package, AI-extracted txn id carried onto the PO ([#172](https://github.com/JinhuW/recycle_servres_erp/pull/172))

### Other
- docs: debug note — PWA service worker caused post-deploy stale pages

## [1.86.3] - 2026-08-24

### Fixes
- fix(desktop): restore RAM AI auto-fill on the PO edit page ([#171](https://github.com/JinhuW/recycle_servres_erp/pull/171))

### Other
- chore: bump to 1.86.3 (v1.86.2 taken by a parallel session's #171)
- refactor(shipping,orders): one draft-PO insert path, shared row predicates, one shipment column list ([#170](https://github.com/JinhuW/recycle_servres_erp/pull/170))

## [1.86.2] - 2026-08-24

### Fixes
- fix(pwa): stale UI after deploy — missed needRefresh, snoozed toast, desktop SW leftover ([#169](https://github.com/JinhuW/recycle_servres_erp/pull/169))

### Other
- chore: v1.86.2 — version collision with #166

## [1.86.1] - 2026-08-24

### Other
- perf(shipping): inbound counts come from one SQL round-trip; keyset index + pre-0095 quote backfill ([#166](https://github.com/JinhuW/recycle_servres_erp/pull/166))

## [1.86.0] - 2026-08-24

### Features
- feat(mobile): neutral role gate for managers + Profile role switch ([#167](https://github.com/JinhuW/recycle_servres_erp/pull/167))

## [1.85.0] - 2026-08-24

### Features
- feat: owner default warehouse + manager creates package PO at any status ([#164](https://github.com/JinhuW/recycle_servres_erp/pull/164))

## [1.84.0] - 2026-08-24

### Features
- feat(orders): managers can reassign a PO's purchaser until it is Done ([#162](https://github.com/JinhuW/recycle_servres_erp/pull/162))

## [1.83.4] - 2026-08-24

### Fixes
- fix(coordinator): honest zero-state for review volume

### Other
- chore: dead-code sweep + doc refresh from 2026-08-24 deep review

## [1.83.3] - 2026-08-24

### Fixes
- fix(mobile): SN scanner reads DataMatrix on iPhone — zxing-wasm replaces jsQR

## [1.83.2] - 2026-08-24

### Features
- feat(mobile): SN scanner is single-shot — capture, confirm, auto-close

## [1.83.1] - 2026-08-24

### Features
- feat(coordinator): Facebook tracker page with live fleet, review stats and filter prompt

### Fixes
- fix(mobile): SN scanner decodes a native-res center crop; smaller viewfinder

## [1.83.0] - 2026-08-24

### Features
- feat(mobile): QR scan button on the serial-number field

## [1.82.2] - 2026-08-23

### Fixes
- fix: max-effort review of the dev→prod release diff — shipping/orders/packages fixes ([#160](https://github.com/JinhuW/recycle_servres_erp/pull/160))

## [1.82.1] - 2026-08-23

### Fixes
- fix(orders): chip numbers get one upper-case spelling; RAM form stops offering an empty photo slot ([#159](https://github.com/JinhuW/recycle_servres_erp/pull/159))

## [1.82.0] - 2026-08-23

### Features
- feat(orders): managers can submit a PO on behalf of a purchaser ([#158](https://github.com/JinhuW/recycle_servres_erp/pull/158))

### Fixes
- fix(scripts): prune's merged check compares against dev history, not its tip ([#157](https://github.com/JinhuW/recycle_servres_erp/pull/157))

## [1.81.0] - 2026-08-23

### Features
- feat(orders): managers can reopen a Done PO back to Reviewing ([#156](https://github.com/JinhuW/recycle_servres_erp/pull/156))

## [1.80.2] - 2026-08-22

### Fixes
- fix(shipping): Copy seller link no longer 500s before a destination is picked ([#155](https://github.com/JinhuW/recycle_servres_erp/pull/155))

## [1.80.1] - 2026-08-22

### Fixes
- fix(edge): redirect plain http to https so Secure auth cookies survive login ([#154](https://github.com/JinhuW/recycle_servres_erp/pull/154))

## [1.80.0] - 2026-08-22

### Features
- feat(shipping): mobile shipping — inbound glance, paste-to-track, delivered→capture ([#153](https://github.com/JinhuW/recycle_servres_erp/pull/153))

### Other
- docs(shipping): ShipSaving v1↔v2 endpoint comparison — v2 wins everywhere we call ([#152](https://github.com/JinhuW/recycle_servres_erp/pull/152))

## [1.79.0] - 2026-08-22

### Features
- feat(shipping): ShipSaving v2 client — OAuth, quick_rate, idempotent buy ([#151](https://github.com/JinhuW/recycle_servres_erp/pull/151))

## [1.78.0] - 2026-08-22

### Features
- feat(shipping): swap the UI onto the real backend, retire the mock ([#150](https://github.com/JinhuW/recycle_servres_erp/pull/150))

## [1.77.0] - 2026-08-22

### Features
- feat(shipping): packages API, cross-PO shipments list, server-side PO advance ([#149](https://github.com/JinhuW/recycle_servres_erp/pull/149))

## [1.76.0] - 2026-08-22

### Features
- feat(submit): SSD lines drop health/serials, brand required above 800GB ([#148](https://github.com/JinhuW/recycle_servres_erp/pull/148))

## [1.75.1] - 2026-08-22

### Fixes
- fix(shipping): review fixes — races, clipboard truth, CSV, ETA timezone ([#147](https://github.com/JinhuW/recycle_servres_erp/pull/147))

## [1.75.0] - 2026-08-22

### Features
- feat(shipping): add external labels with carrier detection and delivered→PO flow ([#146](https://github.com/JinhuW/recycle_servres_erp/pull/146))

## [1.74.0] - 2026-08-22

### Features
- feat(shipping): seller-name typeahead and info-dense contact cards ([#145](https://github.com/JinhuW/recycle_servres_erp/pull/145))

## [1.73.0] - 2026-08-22

### Features
- feat(shipping): seller-contacts rail beside the label wizard ([#144](https://github.com/JinhuW/recycle_servres_erp/pull/144))

## [1.72.0] - 2026-08-22

### Features
- feat(shipping): previous-sellers address book in the label wizard ([#143](https://github.com/JinhuW/recycle_servres_erp/pull/143))

## [1.71.0] - 2026-08-21

### Features
- feat(shipping): ShipStation-style shipments table and full-page label wizard ([#142](https://github.com/JinhuW/recycle_servres_erp/pull/142))

## [1.70.3] - 2026-08-18

### Fixes
- fix(shipping): warn on the shipping page that the feature is not ready ([#140](https://github.com/JinhuW/recycle_servres_erp/pull/140))

## [1.70.2] - 2026-08-18

### Fixes
- fix(submit): unstick the commit bar and cap the items table at 10 rows ([#139](https://github.com/JinhuW/recycle_servres_erp/pull/139))

## [1.70.1] - 2026-08-17

### Fixes
- fix(shipping): give label spend its own row in the PO cost breakdown ([#138](https://github.com/JinhuW/recycle_servres_erp/pull/138))

## [1.70.0] - 2026-08-17

### Features
- feat(shipping): shipping-first flow on a dedicated Shipping page (UI only) ([#137](https://github.com/JinhuW/recycle_servres_erp/pull/137))

## [1.69.1] - 2026-08-17

### Fixes
- fix(shipping): move the seller-link option inside the new-label form ([#136](https://github.com/JinhuW/recycle_servres_erp/pull/136))

## [1.69.0] - 2026-08-17

### Features
- feat(shipping): seller self-service link and shipping on the submit page ([#135](https://github.com/JinhuW/recycle_servres_erp/pull/135))

## [1.68.0] - 2026-08-17

### Features
- feat(shipping): prepaid ShipSaving labels on purchase orders ([#134](https://github.com/JinhuW/recycle_servres_erp/pull/134))

## [1.67.0] - 2026-08-15

### Features
- feat(market): sort the desktop table by clicking column headers ([#132](https://github.com/JinhuW/recycle_servres_erp/pull/132))

## [1.66.2] - 2026-08-15

### Fixes
- fix(oauth): ceiling non-manager grants to market:read only ([#131](https://github.com/JinhuW/recycle_servres_erp/pull/131))

## [1.66.1] - 2026-08-15

### Fixes
- fix(settings): let purchasers actually reach the Connectors page ([#130](https://github.com/JinhuW/recycle_servres_erp/pull/130))

## [1.66.0] - 2026-08-15

### Features
- feat(connectors): open the MCP connect page to purchasers ([#129](https://github.com/JinhuW/recycle_servres_erp/pull/129))

## [1.65.0] - 2026-08-14

### Features
- feat(tracker): fleet status filter and infinite scroll ([#127](https://github.com/JinhuW/recycle_servres_erp/pull/127))

## [1.64.0] - 2026-08-14

### Features
- feat(tracker): add Reddit tracker admin page and API proxy ([#125](https://github.com/JinhuW/recycle_servres_erp/pull/125))

## [1.63.0] - 2026-08-12

### Other
- perf(frontend): cut the cold-load critical path by ~64% ([#124](https://github.com/JinhuW/recycle_servres_erp/pull/124))

## [1.62.6] - 2026-08-09

### Fixes
- fix(sell-orders): commit inventory by quantity, not by the whole lot ([#122](https://github.com/JinhuW/recycle_servres_erp/pull/122))

## [1.62.5] - 2026-08-09

### Fixes
- fix(inventory): give legacy Other lines the type they never had ([#121](https://github.com/JinhuW/recycle_servres_erp/pull/121))

## [1.62.4] - 2026-08-09

### Fixes
- fix(sell-orders): sort the price template by brand, capacity, speed ([#119](https://github.com/JinhuW/recycle_servres_erp/pull/119))

### Other
- docs: note the connector cleanup predicate mismatch

## [1.62.3] - 2026-08-09

### Fixes
- fix(connectors): our own confirm dialog, and a cleanup button that cleans up ([#116](https://github.com/JinhuW/recycle_servres_erp/pull/116))

### Other
- chore: bump version to 1.62.3

## [1.62.2] - 2026-08-09

### Fixes
- fix(sell-orders): strip bid-only columns from the packing checklist ([#117](https://github.com/JinhuW/recycle_servres_erp/pull/117))

## [1.62.1] - 2026-08-09

### Features
- feat(sell-orders): sort the bid sheet, and ship it pre-sorted ([#103](https://github.com/JinhuW/recycle_servres_erp/pull/103))

### Fixes
- fix(submit): ask an Other line for its part number, not its prose ([#115](https://github.com/JinhuW/recycle_servres_erp/pull/115))
- fix(sell-orders): let the bid sheet header filter ([#102](https://github.com/JinhuW/recycle_servres_erp/pull/102))

## [1.62.0] - 2026-08-08

### Fixes
- fix(orders): photos that upload, and a cost that survives the sale ([#113](https://github.com/JinhuW/recycle_servres_erp/pull/113))

## [1.61.1] - 2026-08-08

### Fixes
- fix(ui): make the still-needed toast loud, and raise it on the phone too ([#112](https://github.com/JinhuW/recycle_servres_erp/pull/112))

## [1.61.0] - 2026-08-08

### Features
- feat(submit): tighten the PO commit bar, and say what a line still needs where it's filled ([#111](https://github.com/JinhuW/recycle_servres_erp/pull/111))

## [1.60.1] - 2026-08-08

### Fixes
- fix(sell-orders): back-port the bid sheet's sort and filter, unprotected ([#110](https://github.com/JinhuW/recycle_servres_erp/pull/110))

## [1.60.0] - 2026-08-08

### Features
- feat(ui): say errors out loud, in a dialog that lists what to fix ([#109](https://github.com/JinhuW/recycle_servres_erp/pull/109))

## [1.59.1] - 2026-08-08

### Fixes
- fix(orders): hold the unpriced-price rule in the schema, and stop emptying POs ([#108](https://github.com/JinhuW/recycle_servres_erp/pull/108))

## [1.59.0] - 2026-08-08

### Fixes
- fix(mobile): let one screen own a purchase order ([#107](https://github.com/JinhuW/recycle_servres_erp/pull/107))

## [1.58.3] - 2026-08-08

### Fixes
- fix(orders): charge the whole fee on the list row, and say so in the tests ([#106](https://github.com/JinhuW/recycle_servres_erp/pull/106))

## [1.58.2] - 2026-08-08

### Fixes
- fix(orders): keep the goods total, part keys, and back navigation honest ([#105](https://github.com/JinhuW/recycle_servres_erp/pull/105))

## [1.58.0] - 2026-08-07

### Fixes
- fix(orders): close the review's findings, and derive the goods total ([#104](https://github.com/JinhuW/recycle_servres_erp/pull/104))

## [1.57.4] - 2026-08-06

### Fixes
- fix(mobile): never leave an order with nothing in it ([#101](https://github.com/JinhuW/recycle_servres_erp/pull/101))

## [1.57.3] - 2026-08-06

### Fixes
- fix(mobile): add the fee up to the total, and stop losing it ([#100](https://github.com/JinhuW/recycle_servres_erp/pull/100))

## [1.57.2] - 2026-08-06

### Fixes
- fix(mobile): state the order total, don't let it be typed ([#99](https://github.com/JinhuW/recycle_servres_erp/pull/99))

## [1.57.1] - 2026-08-06

### Fixes
- fix(mobile): a blank qty field, and no second way to add a line ([#98](https://github.com/JinhuW/recycle_servres_erp/pull/98))

## [1.57.0] - 2026-08-06

### Features
- feat(mobile): ask which PO first, not which category

## [1.56.1] - 2026-08-06

### Fixes
- fix(po): drop the goods-total override, now that the tape states it

## [1.56.0] - 2026-08-06

### Features
- feat(po): render the PO's money as the receipt the design called for

## [1.55.0] - 2026-08-06

### Features
- feat(po): group a mixed order's lines by category, with the cost breakdown

## [1.54.1] - 2026-08-06

### Fixes
- fix(po): stop the photo slot reading as something the line needs

## [1.54.0] - 2026-08-06

### Features
- feat(market): price a line while the buy can still change

## [1.53.0] - 2026-08-06

### Features
- feat(orders): let any purchase-order line carry photos

### Fixes
- fix(orders): an unpriced line contributes no revenue

### Other
- chore: drop a screenshot committed by mistake

## [1.52.0] - 2026-08-06

### Features
- feat(orders): allow one purchase order to hold several categories

### Fixes
- fix(po): start the unit-cost field blank instead of at 0 ([#91](https://github.com/JinhuW/recycle_servres_erp/pull/91))

## [1.51.2] - 2026-08-06

### Features
- feat(sell-orders): sort the bid sheet, and ship it pre-sorted ([#103](https://github.com/JinhuW/recycle_servres_erp/pull/103))

## [1.51.1] - 2026-08-06

### Fixes
- fix(sell-orders): let the bid sheet header filter ([#102](https://github.com/JinhuW/recycle_servres_erp/pull/102))

## [1.51.0] - 2026-08-05

### Features
- feat(inventory): drop cost, price and submitter from the grouped export ([#89](https://github.com/JinhuW/recycle_servres_erp/pull/89))

## [1.50.1] - 2026-08-05

### Fixes
- fix(mcp): report tool failures as tool results, not JSON-RPC errors ([#87](https://github.com/JinhuW/recycle_servres_erp/pull/87))

## [1.50.0] - 2026-08-04

### Features
- feat(connectors): collapse the service-client form and sweep unused connectors ([#84](https://github.com/JinhuW/recycle_servres_erp/pull/84))

### Other
- chore: bump to 1.50.0 ([#85](https://github.com/JinhuW/recycle_servres_erp/pull/85))

## [1.49.0] - 2026-08-04

### Features
- feat(inventory): filter Other stock by Untyped ([#83](https://github.com/JinhuW/recycle_servres_erp/pull/83))

## [1.48.1] - 2026-08-04

### Fixes
- fix(version): show the build date instead of a commit sha ([#78](https://github.com/JinhuW/recycle_servres_erp/pull/78))

## [1.48.0] - 2026-08-04

### Features
- feat(oauth): let the user choose which permissions a connector gets ([#81](https://github.com/JinhuW/recycle_servres_erp/pull/81))

## [1.47.2] - 2026-08-04

### Fixes
- fix(inventory): let the Other category refine by item type ([#77](https://github.com/JinhuW/recycle_servres_erp/pull/77))

## [1.47.1] - 2026-08-04

### Fixes
- fix(activity): make "Open record" actually open the record ([#76](https://github.com/JinhuW/recycle_servres_erp/pull/76))

## [1.47.0] - 2026-08-03

### Features
- feat(purchase-orders): label what an Other-type item actually is ([#70](https://github.com/JinhuW/recycle_servres_erp/pull/70))

### Other
- docs(debug-notes): Railway rewrites X-Forwarded-Host; one-entry allowlists hide it ([#75](https://github.com/JinhuW/recycle_servres_erp/pull/75))

## [1.46.1] - 2026-08-03

### Features
- feat(session): start a session on an existing branch with --checkout ([#69](https://github.com/JinhuW/recycle_servres_erp/pull/69))

### Fixes
- fix(oauth): carry the public hostname in a header Railway won't rewrite ([#73](https://github.com/JinhuW/recycle_servres_erp/pull/73))

## [1.46.0] - 2026-08-03

### Features
- feat(mcp): make the MCP server connectable from Claude and ChatGPT ([#68](https://github.com/JinhuW/recycle_servres_erp/pull/68))

## [1.45.0] - 2026-08-03

### Features
- feat(purchase-orders): move a goods overflow into Other fees ([#67](https://github.com/JinhuW/recycle_servres_erp/pull/67))

## [1.44.0] - 2026-08-03

### Features
- feat(purchase-orders): purchasers keep notes and attachments open until Done

## [1.43.1] - 2026-08-03

### Other
- refactor(purchase-orders): make the items footer a cost ledger ([#66](https://github.com/JinhuW/recycle_servres_erp/pull/66))

## [1.43.0] - 2026-08-03

### Features
- feat(purchase-orders): record other fees separately from the goods total ([#65](https://github.com/JinhuW/recycle_servres_erp/pull/65))

## [1.42.1] - 2026-08-01

### Fixes
- fix(purchase-orders): keep the line drawer viewable on a Done PO ([#63](https://github.com/JinhuW/recycle_servres_erp/pull/63))

## [1.42.0] - 2026-08-01

### Features
- feat(inventory): serial-number search everywhere + DDR5 serial rules on order lines ([#62](https://github.com/JinhuW/recycle_servres_erp/pull/62))

## [1.41.0] - 2026-07-27

### Features
- feat(sell-orders): drafts are proposals; inventory is claimed on promotion ([#59](https://github.com/JinhuW/recycle_servres_erp/pull/59))
- feat(scripts): recycle session worktrees instead of accumulating them ([#57](https://github.com/JinhuW/recycle_servres_erp/pull/57))

### Other
- chore(claude): apply bypass permissions to every session, not just launcher ones ([#56](https://github.com/JinhuW/recycle_servres_erp/pull/56))

## [1.40.1] - 2026-07-26

### Features
- feat(purchase-orders): drop the Item column, give the PO download a download icon

### Other
- chore(scripts): make permission bypass opt-in when launching session worktrees

## [1.40.0] - 2026-07-26

### Features
- feat(purchase-orders): per-type spec columns in the PO spreadsheet, drop the invoice PDF

### Fixes
- fix(scripts): stop --prune reading a failed git command as "safe to delete" ([#54](https://github.com/JinhuW/recycle_servres_erp/pull/54))
- fix(scripts): make --prune safe and actually able to reclaim worktrees ([#53](https://github.com/JinhuW/recycle_servres_erp/pull/53))

### Other
- chore(scripts): launch session worktrees with permission prompts off ([#55](https://github.com/JinhuW/recycle_servres_erp/pull/55))

## [1.39.1] - 2026-07-26

### Fixes
- fix(workspace,auth): two prod-log defects, plus session-worktree tooling ([#52](https://github.com/JinhuW/recycle_servres_erp/pull/52))

## [1.39.0] - 2026-07-26

### Features
- feat(purchase-orders): move the stage filter into the table toolbar ([#51](https://github.com/JinhuW/recycle_servres_erp/pull/51))

## [1.38.0] - 2026-07-26

### Features
- feat(activity): auto-load on scroll, pin the column head ([#50](https://github.com/JinhuW/recycle_servres_erp/pull/50))

## [1.37.0] - 2026-07-26

### Features
- feat(activity): global activity log page ([#44](https://github.com/JinhuW/recycle_servres_erp/pull/44))

## [1.36.0] - 2026-07-25

### Features
- feat(purchase-orders): require RAM Chip # only for Micron and Other

## [1.35.0] - 2026-07-25

### Features
- feat(purchase-orders): Confirm line writes through to the database

## [1.34.0] - 2026-07-24

### Features
- feat(inventory): offer 1TB instead of 1024GB in the SSD capacity dropdown
- feat(purchase-orders): accept spreadsheet attachments on the PO submit page

### Other
- docs(purchase-orders): spec spreadsheet attachments on the PO submit page

## [1.33.0] - 2026-07-24

### Features
- feat(purchase-orders): record everything on a draft, nothing filtered ([#40](https://github.com/JinhuW/recycle_servres_erp/pull/40))

## [1.32.0] - 2026-07-24

### Fixes
- fix(purchase-orders): audit drafts so the PO Activity panel isn't empty ([#38](https://github.com/JinhuW/recycle_servres_erp/pull/38))

## [1.31.3] - 2026-07-23

### Fixes
- fix(inventory): validate selection-export ids before the uuid cast ([#36](https://github.com/JinhuW/recycle_servres_erp/pull/36))

## [1.31.2] - 2026-07-23

### Fixes
- fix(health): empty Dockerfile version-arg defaults so the fallback fires ([#35](https://github.com/JinhuW/recycle_servres_erp/pull/35))

## [1.31.1] - 2026-07-23

### Fixes
- fix(health): report real version/commit on Railway builds ([#34](https://github.com/JinhuW/recycle_servres_erp/pull/34))

## [1.31.0] - 2026-07-23

### Features
- feat(inventory): selection-aware export + designed workbook styling ([#33](https://github.com/JinhuW/recycle_servres_erp/pull/33))

## [1.30.0] - 2026-07-23

### Features
- feat(inventory): per-category tabs in the inventory export ([#32](https://github.com/JinhuW/recycle_servres_erp/pull/32))

## [1.29.1] - 2026-07-23

### Fixes
- fix(sell-orders): packing tabs named 'Pack - <warehouse>' ([#30](https://github.com/JinhuW/recycle_servres_erp/pull/30))

### Other
- chore: bump version to 1.29.1 (squash-drift reconcile for #30) ([#31](https://github.com/JinhuW/recycle_servres_erp/pull/31))

## [1.29.0] - 2026-07-22

### Features
- feat(purchase-orders): require all RAM spec fields in the PO submit form ([#29](https://github.com/JinhuW/recycle_servres_erp/pull/29))

## [1.28.0] - 2026-07-22

### Features
- feat(sell-orders): per-warehouse packing-checklist tabs in the price template ([#28](https://github.com/JinhuW/recycle_servres_erp/pull/28))

## [1.27.0] - 2026-07-22

### Features
- feat(sell-orders): price template splits into one worksheet per category ([#27](https://github.com/JinhuW/recycle_servres_erp/pull/27))

## [1.26.0] - 2026-07-22

### Features
- feat(sell-orders)!: single price-template export with Chip # and editable Note column ([#26](https://github.com/JinhuW/recycle_servres_erp/pull/26))

## [1.25.0] - 2026-07-22

### Features
- feat(sell-orders): price template gets per-attribute spec columns + Image URL links ([#25](https://github.com/JinhuW/recycle_servres_erp/pull/25))

## [1.24.2] - 2026-07-22

### Fixes
- fix(sell-orders): restore per-attribute spec columns in the spreadsheet ([#24](https://github.com/JinhuW/recycle_servres_erp/pull/24))

## [1.24.1] - 2026-07-22

### Fixes
- fix(sell-orders): spreadsheet keeps specs in one composed Spec field ([#23](https://github.com/JinhuW/recycle_servres_erp/pull/23))

## [1.24.0] - 2026-07-22

### Features
- feat(sell-orders): per-category spreadsheet export with sectioned Summary ([#22](https://github.com/JinhuW/recycle_servres_erp/pull/22))

## [1.23.0] - 2026-07-22

### Features
- feat(sell-orders): order-summary card with adjustment breakdown

## [1.22.2] - 2026-07-22

### Fixes
- fix(sell-orders): adjust final total from the edit modal too

## [1.22.1] - 2026-07-22

### Fixes
- fix(sell-orders): bid-sheet photos via delivery-URL fallback

## [1.22.0] - 2026-07-22

### Features
- feat(sell-orders): negotiated final-price adjustment

## [1.21.0] - 2026-07-22

### Features
- feat(sell-orders): vendor price round-trip via bid-sheet XLSX

## [1.20.1] - 2026-07-21

### Fixes
- fix(sell-orders): customer, currency, payment receiver on one line in edit modal ([#16](https://github.com/JinhuW/recycle_servres_erp/pull/16))

### Other
- chore(deploy): release from main — retire the prod branch

## [1.20.0] - 2026-07-20

### Features
- feat(inventory): category-specific columns in the grouped export ([#7](https://github.com/JinhuW/recycle_servres_erp/pull/7))

## [1.19.0] - 2026-07-20

### Features
- feat(inventory): select/unselect all lots in the current filter ([#11](https://github.com/JinhuW/recycle_servres_erp/pull/11))

## [1.18.0] - 2026-07-19

### Features
- feat(sell-orders): show created and updated dates in list and detail ([#8](https://github.com/JinhuW/recycle_servres_erp/pull/8))

## [1.17.5] - 2026-07-19

### Fixes
- fix(deploy): redeploy dev backend after nightly db-sync restore

### Other
- Release v1.17.1: payment receiver rules, SSD sort, 523-outage fix + deploy guards ([#6](https://github.com/JinhuW/recycle_servres_erp/pull/6))

## [1.17.4] - 2026-07-16

### Fixes
- fix(deploy): gate db-sync restore on a fully successful pg_dump

### Other
- ci(frontend): deploy the dev Worker on dev pushes

## [1.17.3] - 2026-07-16

### Fixes
- fix(deploy): reset dev schema wholesale in db-sync instead of pg_dump --clean

## [1.17.2] - 2026-07-14

### Fixes
- fix(orders): let managers remove Done evidence photos after the transition
- fix(attachments): shrink oversized images to fit upload cap before AI rename

## [1.17.1] - 2026-07-13

### Fixes
- fix(db-sync): restore deploy/railway-sync build files on main
- fix(backup): harden VM hourly dump — file-based dump + integrity gate
- fix(backup): integrity-check dump from file, not a gunzip pipe

### Other
- Release: SSD capacity select, RAM chip numbers, receipt OCR naming, market datapoints ([#4](https://github.com/JinhuW/recycle_servres_erp/pull/4))
- docs(backup): drop legacy prod-VM references — Railway is the only deployment
- chore(backup): remove deprecated VM backup script; repoint README
- chore(backup): restore daily 03:30 UTC cron after verifying green run
- infra(backup): add Railway cron service to back up Postgres to R2

## [1.17.0] - 2026-07-13

### Features
- feat(deploy): guard against Worker custom-domain loss (2026-07-12 523 outage)

## [1.16.1] - 2026-07-12

### Fixes
- fix(deploy): serve inventory.recycleservers.com from the prod Worker (self-hosted stack retired)

## [1.16.0] - 2026-07-12

### Features
- feat(sell-orders): receiver column in list + managers-only receiver rule

## [1.15.0] - 2026-07-12

### Features
- feat(sell-orders): payment receiver, creator-only reopen, single-chevron picker

### Other
- docs: debug note — shell-exported DATABASE_URL overrides repo .env

## [1.14.0] - 2026-07-12

### Features
- feat(orders): sort SSD capacity options by size ascending
- feat(orders): SSD PO capacity becomes a strict select with expanded size list
- feat(orders): chip number field on RAM PO lines + spreadsheet exports
- feat(attachments): auto-name payment receipts via OCR
- feat: record a market data point when a sell order completes (#3)
- feat(infra): terraform for api.prod/dev.recycleservers.com backend DNS
- feat(security): gate Railway origin behind a Worker shared secret
- feat(deploy): custom domains inventory-prod/dev.recycleservers.com for workers
- feat(deploy): split frontend worker into prod + dev (per-backend)
- feat(deploy): prod->dev Postgres sync job + dev/prod railway topology note
- feat(deploy): cloudflare worker scaffold for experimental deploy

### Fixes
- fix: address review findings on receipt rename, SSD catalog, chip number
- fix(orders): carry serialNumber through mobile/desktop order line mapping
- fix(deploy): fail sync loudly on pg_dump error (pipefail) + dest-host safety rails
- fix(deploy): pin Dockerfile builder + cron for db-sync via railway.toml
- fix(deploy): default cloudflare_account_id so backups TF needs no gitignored tfvars

### Other
- docs: debug note resolution — build flushed through; verify migrations via DB, not logs
- docs: debug note — Railway prod builds hanging in BUILDING (platform-side)
- docs(security): document Worker<->Railway shared-secret origin lockdown
- docs(deploy): two frontend workers + custom domains
- docs(deploy): dev/prod + native cron db-sync complete
- docs(deploy): record verified dev/prod + prod->dev sync state
- docs(deploy): note + ready-to-apply files for Postgres->R2 backups
- docs(deploy): fix runbook login curl, document /metrics + R2 risks, reconcile OAuth scope
- docs(deploy): experimental railway + cloudflare runbook
- chore(deploy): wire worker BACKEND_URL to railway + note OAuth signing-key boot requirement
- docs(plan): experimental Railway + Cloudflare deployment plan
- docs(deploy): experimental Railway + Cloudflare deployment design

## [1.13.0] - 2026-06-17

### Features
- feat(sell-orders): allow Draft to move directly to Awaiting payment

## [1.12.1] - 2026-06-17

### Fixes
- fix(orders): shorten status-advance button label to "Confirm"

## [1.12.0] - 2026-06-17

### Features
- feat(market): paginate market values with infinite scroll

## [1.11.1] - 2026-06-17

### Fixes
- fix(orders): allow reopening status dialog to add more notes/attachments

## [1.11.0] - 2026-06-17

### Features
- feat(submit): lazy PO creation on submit; attachment upload wired to final order id
- feat(order): submission attachments inside Order details; in-box upload hint
- feat(submit): note that receipt/chat-history/etc can be uploaded
- feat(submit): view & edit submission attachments on the order page
- feat(submit): optional attachment on PO submit (buffered upload)
- feat(submit): backend Submission meta key, owner-editable on Draft

### Fixes
- fix(submit): surface upload/delete errors on order-page attachments

### Refactors
- refactor(attachments): shared AttachmentDropzone reused across sell & purchase orders
- refactor(submit): stable per-file object URLs for attachment previews

### Other
- docs(plan): optional attachment at PO submit
- docs(spec): optional attachment at PO submit (reuse order notes)

## [1.10.0] - 2026-06-17

### Features
- feat(submit): explicit 'Use <typed>' row in catalog combobox

### Fixes
- fix(submit): single combobox for SSD/HDD capacity & brand
- fix(submit): make SSD/HDD capacity/brand custom entry reliable

### Other
- polish(ui): unify native select arrow with the Combobox chevron
- polish(submit): refine catalog combobox interactions

## [1.9.0] - 2026-06-16

### Features
- feat(submit): custom capacity & brand for SSD/HDD via dropdown + input

## [1.8.0] - 2026-06-16

### Features
- feat(sell-orders): group spreadsheet Summary tab by part number

## [1.7.1] - 2026-06-15

### Features
- feat(orders): sell price, profit & commission in PO spreadsheet export

## [1.7.0] - 2026-06-15

### Features
- feat(connectors): checkbox multi-select for service-client scopes

### Other
- docs(spec): checkbox multi-select for connector scopes

## [1.6.0] - 2026-06-14

### Features
- feat(orders): downloadable PO spreadsheet (XLSX) with payment info
- feat(backup): container-mode dumps with R2 offsite copy and rotation

## [1.5.0] - 2026-06-14

### Features
- feat(backup): container-mode dumps with R2 offsite copy and rotation

## [1.4.1] - 2026-06-12

### Fixes
- fix(orders): let any user advance a PO from Draft to In Transit

## [1.4.0] - 2026-06-11

### Features
- feat(orders): optional note & attachments when moving a PO to Done

### Other
- docs(orders): spec for optional note/attachments when moving a PO to Done

## [1.3.10] - 2026-06-10

### Features
- feat(sell-orders): open image attachments in a lightbox, not a new tab

## [1.3.9] - 2026-06-10

### Fixes
- fix(security): full-codebase security review — close OAuth offboarding gap, server-side edit gates, prod boot guards

### Other
- build: revert override re-add — keep package.json in sync with committed lockfile
- build: restore pnpm exceljs>uuid override to match lockfile

## [1.3.8] - 2026-06-10

### Features
- feat(sell-orders): name the product & order in the line-conflict error

## [1.3.7] - 2026-06-10

### Features
- feat(sell-orders): add inventory to an existing order

## [1.3.6] - 2026-06-10

### Features
- feat(sell-orders): show per-status notes & attachments in view mode

## [1.3.5] - 2026-06-10

### Fixes
- fix(transfers): neutralize spreadsheet formula injection in CSV export

### Other
- docs(sell-orders): spec for adding inventory to an existing order

## [1.3.4] - 2026-06-10

### Features
- feat(sell-orders): make status note/attachments optional

## [1.3.3] - 2026-06-09

### Features
- feat(frontend): full-width desktop content, drop inventory status dropdown

## [1.3.2] - 2026-06-09

### Features
- feat(sell-orders): replace hide-inactive with a Show closed & done toggle

## [1.3.1] - 2026-06-09

### Features
- feat(sell-orders): add hide-inactive toggle to the desktop list

## [1.3.0] - 2026-06-09

### Features
- feat(inventory): drop grouped/flat toggle, add hide-in-pending-sell-orders filter

## [1.2.8] - 2026-06-08

### Features
- feat(sell-orders): drop cost/profit/internal columns from per-order spreadsheet

## [1.2.7] - 2026-06-08

### Fixes
- fix(frontend): decode RFC 5987 filename* so downloads keep Chinese names

## [1.2.6] - 2026-06-08

### Fixes
- fix(sell-orders): keep Chinese customer names in spreadsheet download filename

## [1.2.5] - 2026-06-08

### Other
- chore(test-harness): mark reviewed migration-runner line nosec

## [1.2.4] - 2026-06-08

### Features
- feat(sell-orders): RMB prices + customer name in spreadsheet export

## [1.2.3] - 2026-06-08

### Fixes
- fix(sell-orders): show customer names in the audit timeline, not raw UUIDs

## [1.2.2] - 2026-06-08

### Features
- feat(sell-orders): per-order spreadsheet export with per-warehouse tabs

### Fixes
- fix(test-harness): sanitise ephemeral DB identifier + mark reviewed DDL
- fix(customers): return created customer so the order-form picker doesn't blank the page
- fix(test-harness): give each vitest run its own ephemeral database

### Other
- docs(changelog): note unreleased test-harness + customers fixes

## [1.2.1] - 2026-06-08

### Features
- feat(sell-orders): warehouse-grouped detail view + one-price-per-product edit

### Fixes
- fix(connectors): make the service-client name field usable

## [1.2.0] - 2026-06-08

### Features
- feat(connectors): mint sell-order MCP service clients
- feat(mcp): create_sell_order_draft tool
- feat(mcp): sell-order tool surface (search_sellable_inventory + scope-gated dispatch)
- feat(mcp): server-side inventory label/spec helper
- feat(mcp): seed MCP sell-order customer + default-customer setting
- feat(oauth): add sellorder:read/sellorder:write scopes

### Refactors
- refactor(sell-orders): extract shared createSellOrderDraft service

### Other
- docs(mcp): implementation plan for sell-order-draft MCP service
- docs(mcp): lock sell-order-draft spec decisions (setting-based customer, advisory price)
- docs(mcp): sell-order-draft MCP service design

## [1.1.4] - 2026-06-08

### Features
- feat(sell-orders): packing-list download button in detail view
- feat(sell-orders): packing-list PDF endpoint
- feat(sell-orders): packing-list PDF builder

### Other
- i18n(sell-orders): packing-list button strings (EN+ZH)
- test(sell-orders): packing-list endpoint integration tests
- docs(sell-orders): packing-list implementation plan
- docs(sell-orders): packing-list PDF design spec

## [1.1.3] - 2026-06-08

### Other
- Revert "feat(sell-orders): edit sell price once per product across warehouses"

## [1.1.2] - 2026-06-08

### Features
- feat(sell-orders): edit sell price once per product across warehouses

## [1.1.1] - 2026-06-08

### Features
- feat(sell-orders): group saved-order line items by warehouse

## [1.1.0] - 2026-06-08

### Features
- feat(oauth): grant market:write through the interactive MCP flow on manager consent

### Fixes
- fix(oauth): resolve MCP issuer from the trusted origin allowlist, not a localhost env

## [1.0.2] - 2026-06-07

### Fixes
- fix(release): emit the oldest commit in the changelog range (tformat)
- fix(web): submit accepted synthesized part numbers; render vendor bid currency
- fix(security): redact vendor portal tokens before writing the error log
- fix(vendor-bids): promote at the FX rate frozen on the bid
- fix(sell-orders): prevent intra-order oversell; resolve FX outside the transaction
- fix(orders): drop client-settable line status; fix keyset cursor for non-default sorts

### Other
- docs: add 2026-06-07 deep code review report

## [1.0.1] - 2026-06-07

### Features
- feat(dashboard): contributor leaderboard uses projected Done-PO profit

## [1.0.0] - 2026-06-07

## [0.1.10] - 2026-06-07

### Features
- feat(dashboard): purchasers see projected profit from their own Done POs
- feat(inventory): hide sold lots by default with a Show sold toggle
- feat(sell-orders): hide Closed from the status stepper (UI only)
- feat(sell-orders): allow any open stage to move directly to Done; latest-first history
- feat(sell-orders): per-order currency (USD/CNY) with FX snapshot

### Fixes
- fix(sell-orders): seed sell_order_statuses via migration

### Other
- test(orders): align draft-visibility tests with managers-see-drafts

## [0.1.9] - 2026-06-07

### Features
- feat(inventory): hide sold lots by default with a Show sold toggle

## [0.1.8] - 2026-06-07

### Features
- feat(sell-orders): hide Closed from the status stepper (UI only)

## [0.1.7] - 2026-06-07

### Features
- feat(sell-orders): allow any open stage to move directly to Done; latest-first history

## [0.1.6] - 2026-06-07

### Fixes
- fix(sell-orders): seed sell_order_statuses via migration

## [0.1.5] - 2026-06-07

### Features
- feat(sell-orders): per-order currency (USD/CNY) with FX snapshot

## [0.1.4] - 2026-06-05

### Features
- feat(release): one-shot release + deploy via pnpm release:deploy
- feat(mcp): add market:write-scoped set_market_price tool

### Fixes
- fix(release): consume [Unreleased] on cut; de-duplicate changelog

## [0.1.3] - 2026-06-05

### Features
- feat(orders): the mobile PO list now color-codes its metadata — each warehouse gets a stable hashed-hue pill (icon + short code), status keeps its semantic tone as a dotted pill (Draft gray / In Transit blue / Reviewing amber / Done green), and the owner shows as a hashed-hue initials coin; the date recedes to neutral so the three facets read apart at a glance
- feat(analysis): the Inventory Analysis page is now a collapsible console — the title, scope filters and KPI strip stay pinned while everything below folds into numbered sections (composition & flow, warehouses & brands, and one per type) whose headers keep a live summary visible when collapsed (e.g. `201u · $29,951 → $41,338 · +38%`); per-type sections keep their category-coloured header as the toggle. Open/closed state persists per-user via the preferences store (`analysis.collapsed`), with an Expand/Collapse-all control (EN + ZH)

### Other
- build: upgrade runtime to Node 24 (Dockerfiles node:24-slim, engines >=24, .nvmrc)
- chore: drop accidentally-tracked playwright-mcp scratch, gitignore it
- chore: pin Node 24 via .nvmrc

## [0.1.2] - 2026-06-05

### Features
- feat(analysis): collapsible sections on the Inventory Analysis page
- feat(orders): color-code warehouse, status & owner in mobile PO list

### Other
- build: upgrade runtime to Node 24
- chore: drop accidentally-tracked playwright-mcp scratch, gitignore it
- chore: pin Node 24 via .nvmrc

## [0.1.1] - 2026-06-05

### Features
- feat(vendor): general-link section in the vendor-links manager
- feat(vendor): promote-time customer picker for general-link bids
- feat(vendor): show General badge on customer-less bids
- feat(vendor): portal greets generically for customer-less links
- feat(vendor): i18n keys for general vendor links (EN+ZH)
- feat(vendor): promote picks/validates a customer for general-link bids
- feat(vendor): bid list/detail tolerate null customer
- feat(vendor): public portal handles customer-less link
- feat(vendor): expose general link in vendor-links listing
- feat(vendor): endpoint to mint/rotate the general vendor link
- feat(vendor): migration — optional customer on vendor links/bids
- feat(pwa): polished mobile install onboarding
- feat(vendor): faceted client-side catalog filtering in the portal
- feat(orders): optional per-lot serial numbers on order lines
- feat(orders): require part number with auto-generate confirm dialog
- feat(fe): iOS apple-touch-startup-image splash set
- feat(orders): synthetic part number for Mixed-brand SSDs
- feat(fe): scope PWA install/SW/share-target to the mobile shell only
- feat(fe): web share target with desktop dropzone hydration (injectManifest SW)
- feat(fe): background-sync queue for /api/attachments uploads
- feat(fe): show update-available toast when new SW is waiting
- feat(fe): in-app install prompt + iOS Add-to-Home-Screen hint
- feat(infra): caddy cache headers for sw.js, manifest, and workbox bundles
- feat(fe): link new PWA icon set + manifest from index.html
- feat(fe): register service worker on app boot (excludes vendor portal)
- feat(scan): high-res capture + client-side MozJPEG compression for label OCR
- feat(fe): enable vite-plugin-pwa with manifest + workbox runtime caching
- feat(fe): add PWA icon set (192/512 + maskable 512 + apple-touch 180)
- feat(mobile): default the role gate to Purchaser; tab bar follows effective role
- feat(inventory): show PO line scan image in grouped lots table
- feat(release): versioned docker images, changelog, release script
- feat(analysis): manager inventory analysis page (composition, warehouses, per-type sub-analysis)
- feat(inventory): RAM device type column + grouped xlsx export
- feat(desktop): let managers submit POs from the sidebar
- feat(settings): promote FX rates to its own section + desktop styling
- feat(account): self-service password change (desktop + mobile)
- feat(mobile): surface RAM rank/speed chips + highlight Qty in PO views
- feat(ai): keep full part-number SKU, drop only after first space
- feat(transfers): desktop Discard button + confirm modal
- feat(transfers): discard a Pending transfer order (DELETE endpoint)
- feat(transfers): record prior_status on transferred events
- feat(order-edit): activity log max-height tracks the item-table row count
- feat(orders): per-PO commercial invoice PDF + inventory export columns
- feat(export): invoice-style PDF export for purchase orders
- feat(submit): desktop choice to add lines to an existing draft PO
- feat(submit): pure eligibleDraftTargets helper for merge-target filtering
- feat(export): real .xlsx export for inventory & sell orders
- feat(submit): Add Line auto-confirms the previous line to the DB
- feat(orders): managers see all PO statuses, including drafts
- feat(fe): dev-only hint with demo account on the login page
- feat(mobile): promote AI label capture to top of submit form
- feat(fe): inline AI label drop zone on desktop RAM submit
- feat(submit): confirm-line auto-saves order metadata too
- feat(scan): alert at scan-time when part number matches an existing line
- feat(ai): reduce extracted part numbers to the core SKU
- feat(scan): log "partial fill" warn records when AI skips expected fields
- feat(fe): RAM speed becomes a free-text field
- feat(po): warn on duplicate part numbers within a PO
- feat(fe): SO history shows source currency for vendor-bid promotions
- feat(fe): FX rates settings panel (manager-only)
- feat(fe): vendor-bids list+detail FX columns, filter, badge, promote label
- feat(scan): retake CTA + low-conf red borders; upgrade OCR to Gemini 2.5 Flash
- feat(fe): vendor portal currency picker + live USD-equiv subtotal
- feat(fe): fmtMoney helper + i18n keys for currency/FX
- feat(vendor-bids): expose currency + USD-equiv totals on list/detail
- feat(vendor-bids): re-snapshot fx on promote; stamp SO line audit cols + event detail
- feat(vendor): accept currency on bid + GET /fx endpoint
- feat(fx): manager routes GET/POST/refresh /api/workspace/fx-rates
- feat(fx): start refresh loop on backend boot
- feat(fx): module — supported currencies, Frankfurter fetch, manual override, refresh loop
- feat(db): mig 0056 multi-currency fx_rates ledger + bid/SO audit cols
- feat(fe): mobile scan resumes existing drafts instead of spawning new
- feat(fe): mobile submit form parity — catalog dropdowns, totals & P&L
- feat(fe): wave 3e i18n parity — close the remaining JSX text leaks
- feat(fe): wave 3d i18n parity — market, draft SO, product table, dashboards
- feat(fe): wave 3c i18n parity — PO edit & inventory edit modals
- feat(fe): wave 3b i18n parity — submit form, line drawer & line fields
- feat(fe): wave 3a i18n parity — settings panels
- feat(fe): wave 2 i18n parity — desktop list toolbars & sell-order actions
- feat(fe): wave 1 i18n parity — shared dialogs, vendor portal, mobile archive
- feat(fe): rename SO close action to Discard, gate to edit mode
- feat(fe): drop Cancel button from SO edit modal footer
- feat(fe): refresh SO history after StatusChangeDialog mutations
- feat(fe): mount SellOrderHistory panel on SO detail page
- feat(fe): add SellOrderHistory timeline component
- feat(fe): add SellOrderEvent + SellOrderEventKind types
- feat(be): add GET /api/sell-orders/:id/events endpoint
- feat(be): emit status_meta_changed events for SO status notes/attachments
- feat(be): emit status_changed event on SO status transition
- feat(be): emit meta_changed + line events on PATCH /api/sell-orders/:id
- feat(be): emit created event when promoting a vendor bid
- feat(be): emit created event on POST /api/sell-orders
- feat(db): rename sell-order id prefix SL- → SO-
- feat(be): expand SellOrderEventKind + add SO field lists
- feat(fe): SO close dialog, reopen, Closed chip + edit-pencil guard
- feat(fe): broaden SO status union and lookups type for Closed
- feat(be): sell-order close + reopen via transition map
- feat(be): expose closeReasons in /api/lookups
- feat(be): add 'closed'/'reopened' to SellOrderEventKind
- feat(db): seed Closed status + 5 close-reason rows
- feat(db): mig 0053 — add 'Closed' status + sell_order_close_reasons
- feat(be): scope manager PO reads when rolePreview=as_purchaser
- feat(be): persist unhandled 500s to mounted JSONL file
- feat(fe): remember last login email on Login page
- feat(fe): Show stale only toggle on Market page (persists)
- feat(fe): manager-only inline manual price entry on Market page
- feat(fe): show last_price + 5d stale signal on Market; maxBuy from lastPrice
- feat(fe): staleness helper for Market page (5-day window)
- feat(seed): populate last_price + ref_price_events from synthetic history
- feat(be): expose lastPrice/recentPrices on /api/market and migrate maxBuy basis
- feat(be): manager-only POST /api/market/:id/manual-price
- feat(be): appendPriceEvent helper + integration tests
- feat(db): add last_price columns and ref_price_events audit table
- feat(market): auto-track PO parts and source internal-sales row from PO
- feat(fe): Show archived toggle + chip + faded rows on sell-order list
- feat(fe): Archive/Unarchive buttons in SO edit modal footer
- feat(fe): ArchiveSellOrderDialog with type-to-confirm guard
- feat(fe): archiveSellOrder/unarchiveSellOrder helpers + archivedAt types
- feat(be): POST /api/sell-orders/:id/archive + /unarchive
- feat(be): sell-order detail response exposes archivedAt
- feat(be): sell-order list supports ?includeArchived + archivedAt field
- feat(ui): polish invite modal & auto-collapse inventory refine panel
- feat(be): writeSellOrderEvent audit helper
- feat(inventory): foldable per-category attribute filter bar
- feat(db): add append-only sell_order_events audit log
- feat(db): add sell_orders.archived_at for soft-hide archive
- feat(oauth): last_used_at in Connectors + Deny button on consent screen
- feat(observability): prom-client counters for oauth grants, refresh revocations, mcp calls, market writes
- feat(settings): Connectors tab — list, create service clients, revoke
- feat(frontend): OAuth consent screen at /authorize
- feat(market): POST /api/market/values write endpoint (bearer market:write)
- feat(mcp): JSON-RPC server with list_market_values + get_market_value behind bearerGuard
- feat(oauth): /revoke + bearerGuard + CSRF exemption for /oauth/* + /.well-known/*
- feat(oauth): /token endpoint — authorization_code + refresh_token + client_credentials grants
- feat(oauth): /authorize + consent + pending-request handoff to SPA
- feat(oauth): RFC 7591 Dynamic Client Registration (gated by OAUTH_DCR_OPEN)
- feat(oauth): RFC 8414 + RFC 9728 discovery endpoints
- feat(oauth): Ed25519 JWS access tokens + rotating refresh families
- feat(vendor): show product preview photo in catalog
- feat(oauth): PKCE S256 helpers
- feat(oauth): client CRUD + secret hashing
- feat(backend): 0046 oauth schema — clients, codes, refresh tokens
- feat(orders): archive PO + complete SO→PO id rename
- feat(ai-scan): expand RAM rank vocabulary + warn on off-catalog values
- feat: real OCR confidence + post-login role picker
- feat(metrics): count OCR calls per provider+outcome
- feat(metrics): mount /metrics route + http-duration middleware
- feat(metrics): add prom-client registry + http middleware module
- feat(metrics): expose :9090 scrape endpoint via Caddy path-based proxy
- feat(metrics): add postgres-exporter sidecar (read-only pg_monitor role)
- feat(metrics): add read-only metrics Postgres role (migration 0042)
- feat(vendor-bids): manager Vendor Bids review screen
- feat(vendor-bids): manager vendor-link panel on customers
- feat(vendor-bids): vendor portal (browse/review/my-offers)
- feat(vendor-bids): frontend vendor helpers
- feat(vendor-bids): promote accepted lines to draft sell order
- feat(vendor-bids): manager inbox + detail + decide
- feat(vendor-bids): vendor-link CRUD on customers route
- feat(vendor-bids): public my-offers list
- feat(vendor-bids): public bid submission
- feat(vendor-bids): public me + catalog routes
- feat(inventory): grouped product view with per-PO drilldown + flat toggle
- feat(inventory): InventoryProductTable — grouped rows + per-PO drilldown
- feat(prefs): add inventory.view (grouped|flat) preference key
- feat(vendor-bids): schema + VB id counter
- feat(inventory): GET /products — part-number-grouped inventory
- feat(orders): only auto-fold status/meta when the items table overflows
- feat(orders): make the warehouse/payment/cost meta block foldable
- feat(orders): pin PO-edit chrome, scroll only the items table
- feat(commission): drop tiers/commission_settings/users.commission_rate
- feat(commission): PO-detail rate field; remove Commission rules + member rate UI
- feat(commission): profile lifetime commission uses per-order rate
- feat(commission): orders API surfaces + manager-PATCHes per-order rate
- feat(commission): dashboard uses per-order rate, drops tier model
- feat(commission): add nullable orders.commission_rate + seed it
- feat(infra): prod environment root + R2 state backend + runbook
- feat(infra): bootstrap config for the TF-state R2 bucket
- feat(infra): module outputs mapping to backend R2_* env vars
- feat(infra): scoped R2 API token for the backend
- feat(infra): R2 custom domain (auto-manages proxied CNAME)
- feat(infra): R2 bucket, CORS, optional lifecycle
- feat(infra): cloudflare module provider pin + variables
- feat(backend): @hono/node-server entry point
- feat(backend): buildEnv() maps process.env to Env
- feat(frontend): show 'reopened' events in the activity drawer
- feat(frontend): transfer-order cards, filter, re-open, per-order CSV + print manifest
- feat(backend): R2 attachments via S3 API instead of Worker binding
- feat(backend): POST /transfer-orders/:id/reopen with safety guard
- feat(backend): POST /transfer-orders/:id/receive replaces POST /receive
- feat(backend): GET /transfer-orders replaces GET /transfers
- feat(backend): /transfer creates a TO-<n> transfer order
- feat(backend): transfer_orders table + order_lines.transfer_order_id (0028)
- feat(fe): inventory views show RAM generation
- feat(fe): RAM short labels use generation
- feat(fe): map generation through draft builders
- feat(fe): label-photo thumbnail + lightbox in order lists
- feat(fe): desktop RAM form — generation + device type
- feat(fe): mobile RAM form — generation + device type
- feat(fe): generation type, catalog + i18n
- feat(ai): Gemma 3 27B default, temp 0, one JSON retry
- feat(ai): RAM prompt splits generation vs device type
- feat(inventory): carry RAM generation through list/transfers/receive
- feat(orders): persist and return RAM generation
- feat(types): add OrderLine.generation
- feat(db): rename order_lines.type to generation, add device type
- feat(mobile): carry in-progress draft through RAM rescan
- feat(frontend): show 'received' events in the activity drawer
- feat(frontend): Transfers page — list, bulk receive, CSV export
- feat(frontend): route /transfers → transfers view
- feat(backend): POST /api/inventory/receive — bulk In Transit → Done
- feat(backend): GET /api/inventory/transfers — in-transit lines with from→to
- feat(frontend): shareable deep links for sell orders
- feat(frontend): resolve sell-order deep links to the sellorders view
- feat(ocr): fail-fast 502 on OCR error; test harness env override
- feat(ocr): provider selection by credentials, drop STUB_OCR
- feat(ocr): add OpenRouter vision provider
- feat(ocr): extract workers-ai provider
- feat(ocr): extract stub provider
- feat(ocr): add shared prompts + robust JSON parser
- feat(ocr): add shared ai types module
- feat(desktop): AI scan photo tile + lightbox in LineDrawer
- feat(mobile): AI scan photo thumbnail + lightbox in line edit
- feat(ui): add shared ImageLightbox component
- feat(types): add scanImageUrl to OrderLine and DraftLine
- feat(orders): return scanImageUrl on single-order lines
- feat(sell-orders): reconcile WIP sell-order edit UI + tests with main
- feat(warehouses): remove receiving-cutoff and floor-area attributes
- feat(frontend): remove payment terms from sell-order screens + lookups lib
- feat(settings): warehouse manager picked from DB user dropdown
- feat(orders-ui): guarded delete-order with typed-ID confirmation
- feat(submit-mobile): eager server draft + per-line confirm + abandoned-draft cleanup
- feat(submit-desktop): eager server draft + per-line confirm to inventory
- feat(submit-mobile): inline AI capture, visible image, confidence-gated autofill
- feat(submit-desktop): show captured image, gate AI autofill by confidence
- feat(api): draft-order, confirm-line, delete-order client helpers
- feat(ai): confidence floor, honest stub, RAM prompt enums match catalog
- feat(orders): guarded DELETE /api/orders/:id (Draft-only, owner/manager, not sold)
- feat(orders): POST /api/orders/draft for eager draft persistence
- feat(catalog): restrict RAM brands, complete rank/speed lists
- feat(warehouses): link manager to a DB user via manager_user_id FK
- feat(frontend): customers panel — structured contact/address, Active/Archived only, auto-hide archived
- feat(backend): structured contact + address on customers route
- feat(orders-ui): guarded delete-order with typed-ID confirmation
- feat(submit-mobile): eager server draft + per-line confirm + abandoned-draft cleanup
- feat(warehouses): persisted active flag; archived hidden from all UI
- feat(backend): drop customer terms from sell-order responses
- feat(submit-desktop): eager server draft + per-line confirm to inventory
- feat(backend): drop paymentTerms from /api/lookups
- feat(backend): migration for structured customer contact/address; drop terms, credit_limit, payment_terms
- feat(submit-mobile): inline AI capture, visible image, confidence-gated autofill
- feat(submit-desktop): show captured image, gate AI autofill by confidence
- feat(api): draft-order, confirm-line, delete-order client helpers
- feat(ai): confidence floor, honest stub, RAM prompt enums match catalog
- feat(orders): guarded DELETE /api/orders/:id (Draft-only, owner/manager, not sold)
- feat(orders): POST /api/orders/draft for eager draft persistence
- feat(catalog): restrict RAM brands, complete rank/speed lists
- feat(frontend): wire Settings categories + workspace to real APIs
- feat(backend): review fixes + backend-line reconciliation
- feat(frontend): warehouse detail fields on Warehouse type
- feat(backend): seed warehouses with detail fields
- feat(backend): warehouses GET/POST/PATCH round-trip detail fields
- feat(backend): warehouse_details columns + backfill (migration 0009)
- feat(desktop): deep-linkable order URLs (CC-5)
- feat(mobile): profile + sheets parity (S7-S9)
- feat(mobile): orders search + deep-link + scroll-shadow polish (S4-S6)
- feat(mobile): unified line-edit page + camera polish (S3)
- feat(mobile): login picker + dashboard parity (S1, S2)
- feat(mobile): cross-cutting infra for design parity
- feat(api): cursor pagination + sort allowlist on /api/orders
- feat(audit): DB trigger blocks UPDATE/DELETE on inventory_events
- feat(notifications): per-notification mark-read endpoint
- feat(notify): submitters notified when sell order closes
- feat(inventory): low_margin / sub_cost_sell warnings + notification
- feat(notify): managers notified when an order goes to In Transit
- feat(workspace): settings store + manager-only PATCH
- feat(commission): tier table + routes + pure calc lib
- feat(sell-orders): clamp qty + require sellable status on create
- feat(sell-orders): status transitions with note/attachment evidence + inventory lock on Done
- feat(attachments): CRUD endpoints + 10MB/MIME validation
- feat(categories): table + CRUD + enforce on order create
- feat(inventory): aggregate-by-part-number endpoint for QuickView
- feat(orders): POST /:id/advance with role-aware lifecycle transitions

### Fixes
- fix(orders): persist lifecycle stage on save in desktop editor
- fix(orders): scope mobile PO list to the signed-in user
- fix(pwa): hold SW update until user taps, no skip-waiting on install
- fix(analysis): filter-aware inventory analysis, moved under Inventory tab
- fix(vendor): remount bid detail per id so promote customer resets
- fix(orders): use hyphen for intra-component spaces in synthetic part number
- fix(orders): sanitize synthetic part-number components to a single token
- fix(fe): theme-aware install prompt styling + drop aria-live on dialog
- fix(fe): point Orders shortcut at /sell-orders, drop premature share_target manifest
- fix(vendor): show Reviewing inventory in the vendor catalog
- fix(review): correctness, perf, and dedup fixes from 50-commit review
- fix(dashboard): scope to own work in manager rolePreview=as_purchaser
- fix(order-edit): cap item table & activity log at 10 rows, kill footer dead gap
- fix(submit-mobile): autosave a line added to an existing order, not just drafts
- fix(submit-mobile): never silently drop a line's autosave; await draft id
- fix(be): added lines inherit parent PO's lifecycle, not hard-coded "In Transit"
- fix(be): derive PO status from orders.lifecycle, not line aggregation
- fix(db): backfill RAM/SSD catalog groups missing on migration-only deploys
- fix(ai): trust the printed speed verbatim — drop the JEDEC snap/divide
- fix(ai): tell the model to strip suffixes from PCx-NNNN... codes
- fix(ai): trust the speed printed on the label — drop the PC-code divide-by-8
- fix(ai): recover RAM speed extraction lost to "don't guess" rubric
- fix(fe): refetch PO list on role-preview toggle, surface unreadable PO 403
- fix(fe): drop redundant Cancel button in Close/Reopen dialogs
- fix(fe): SO status tiles grid adapts to lookup length
- fix(be): widen status_meta CHECK to admit Closed, restore upsert
- fix(fe): surface inventory edit lock with a workflow banner
- fix(fe): drop email from Contributor leaderboard rows
- fix(fe): stop vertical text clipping on filter selects
- fix(fe): make Market pencil button always visible (was opacity:0 baseline)
- fix(be): seed disables sell_order_events delete trigger during reset
- fix(mcp): drop top-level oneOf from get_market_value schema
- fix(oauth): make DCR + authorize handlers MCP-compatible
- fix(inventory): treat speed filter as text, not int
- fix(oauth): expose OAUTH_* env vars to Hono context
- fix(inventory): sub-filter fold actually collapses
- fix(oauth): startup guard + RFC 6749 error redirects + admin POST enum validation
- fix(oauth+mcp+frontend): RFC alignment + consent JSON branch + cross-client revoke test
- fix(oauth): harden /token — catch Basic decode errors, block public c_c, drop any casts
- fix(oauth): bind consent to parked req + populate user_id_from_cookie
- fix(oauth): drop hardcoded token_endpoint_auth_method from DCR 201 body
- fix(oauth): throw on missing issuer + cosmetic cleanups on discovery metadata
- fix(oauth): thread env into rotateRefreshToken + export keyKid
- fix(market): drop dead String fallback and broaden formatRefPrice test
- fix(vendor): aggregate bid qty by inventory_id before cap check
- fix(backend): add FK indexes on oauth_authorization_codes per project convention
- fix(backend): drop dated spec reference + redundant OAUTH_* compose block
- fix(fe): guard sell-order stats against unknown status
- fix(fe): give desktop page content breathing room from edges
- fix(fe): add mobile-web-app-capable meta to silence Chrome deprecation
- fix(fe): preserve stored value in line-drawer dropdowns
- fix(fe): surface silent errors with global toast + disabled-reason hints
- fix(dashboard): wire KPI trend chips to real prev-period data
- fix(metrics): wrap timing in try/finally, correct routePath comment
- fix(prod-readiness): apply audit findings across the stack
- fix(vendor-bids): rate-limit public bids (429), block re-decide of promoted lines
- fix(vendor-bids): gate promote on saved decisions, bound qty input, drop unused key
- fix(vendor-bids): recover from fetch errors, surface 409 unavailable items
- fix(vendor-bids): bound accepted price, validate decision, cover decline/partial/404/400
- fix(vendor-bids): bound my-offers queries, add cross-link isolation test
- fix(vendor-bids): bound note, reject overflow price, strengthen bid tests
- fix(vendor-bids): drop dead imports, cap public catalog, harden tests
- fix(inventory): feed grouped-view selections into bulk actions
- fix(inventory): exclude Draft from product status buckets
- fix(compose): add back caps cap_drop:ALL strips from postgres/caddy
- fix(orders): gate PO edit on lifecycle, not the 'Mixed'-prone status
- fix(commission): GET /api/orders/:id returns commissionRate
- fix(orders): persist RAM generation from mobile submit/edit flow
- fix(commission): NaN-safe commission rate input in PO detail
- fix(commission): reject non-finite commissionRate (400) so it can't poison aggregates
- fix(infra): policies[].resources must be jsonencode'd string (cloudflare provider v5)
- fix: race-safe human ids (id_counters) + CORS allowlist
- fix: dashboard commission, deactivated-user auth, discount clamp, seed columns + 401 handling
- fix(docs/build): correct VITE_API_BASE, refresh env example, publish backend port
- fix(frontend): ScanResponse.provider matches backend (stub|openrouter); drop dead workers-ai/cloudflare literals
- fix(backend): seed deletes children before parents (FK-safe re-seed)
- fix(build): root .dockerignore so .env/secrets aren't baked into images
- fix(frontend): transfer status chip tones, stable load/onClose, cleaner errors
- fix(backend): lock order_lines in reopen guard; dedupe test constant
- fix(backend): row-lock transfer order on receive (TOCTOU); test nits
- fix(fe): remaining RAM labels (dashboard/orders) use generation
- fix(fe): RAM labels in inventory list pages use generation
- fix(db): schema-qualify 0027 guard predicates + doc sync
- fix(orders): editing an existing order updates it instead of creating a duplicate
- fix: show receiving warehouse on received events; dedup receive ids
- fix(frontend): correct stale status names in inventory page subtitle
- fix(ocr): allow DDR2 in RAM type enum to match PC-code rule
- fix(desktop): scope AI photo tile to edit mode; stop lightbox backdrop bubbling
- fix(ui): stop X-button click bubbling to ImageLightbox backdrop
- fix(orders): derive lifecycle order statically, not from dropped workflow_stages
- fix(inventory): unclip status filter, fix In Transit, drop workflow_stages
- fix(orders): persist scan image id/confidence via addLines; drop unused confirmOrderLine
- fix(orders-ui): Escape dismisses delete modal; stop modal-shell propagation
- fix(submit-mobile): preserve edit-existing-order path (regression from draft flow)
- fix(submit-desktop): carry health/rpm, guard double-submit, real draft id, validate confirm
- fix(submit-mobile): gate inline AI capture to RAM only
- fix(submit-desktop): neutral notice for low-confidence scan (not red error)
- fix(catalog): map SK Hynix part-number prefix after brand rename
- fix(orders-ui): Escape dismisses delete modal; stop modal-shell propagation
- fix(submit-mobile): preserve edit-existing-order path (regression from draft flow)
- fix(submit-desktop): carry health/rpm, guard double-submit, real draft id, validate confirm
- fix(submit-mobile): gate inline AI capture to RAM only
- fix(submit-desktop): neutral notice for low-confidence scan (not red error)
- fix(catalog): map SK Hynix part-number prefix after brand rename
- fix(backend): clear the 6 pre-existing typecheck errors
- fix(frontend): review fixes for working-tree UI
- fix(mobile): final-review gaps in design parity
- fix(dashboard): scope leaderboard financials + strip unit_cost from recent for purchasers
- fix(inventory): strip cost/profit/margin from purchaser payloads
- fix(orders): default new orders to lifecycle=draft, lines=Draft
- fix(test): migrate vitest config to v4 (fileParallelism replaces nested poolOptions)
- fix(backend): db:reset drops all public-schema tables (dev only)

### Refactors
- refactor(fe): DRY vendor-path check, log SW registration errors, simplify applyPwaUpdate return type
- refactor(transfers): merge duplicate api import
- refactor(fe): move AI label dropzone into the RAM LineDrawer
- refactor(ai): desensitize OCR confidence — tiered rubric + coverage floor
- refactor: collapse sell-order close reasons to fixed enum + submit defaults
- refactor(be): lift diff helper into services/auditDiff.ts
- refactor(be): scraper batch writes ref_price_events instead of history blob
- refactor(market): extract row→DTO formatter for reuse by MCP tools
- refactor(env): collapse to a single repo-root .env
- refactor(inventory): share filter query + true quick-view for grouped lots
- refactor(inventory): extract canonical part-number into shared helper
- refactor(orders): drop the items sub-bar, fold totals into card-head
- refactor(commission): delete /api/commission route, tier calc, its test
- refactor(commission): drop commission_rate from members service
- refactor(backend): db reads DATABASE_URL only (no Hyperdrive)
- refactor(backend): drop Workers AI OCR provider (OpenRouter only)
- refactor(backend): Env holds plain config, not CF bindings
- refactor(backend): accurate /transfer-orders comment; hoist test imports
- refactor(backend): clarify TO-id/from-warehouse intent; drop no-op test skip
- refactor(scan): store label images in R2, not Cloudflare Images
- refactor(frontend): harden CSV download for Firefox; drop unused i18n keys
- refactor: replace hardcoded data with DB-backed values
- refactor(api): reference canonical payment union, clarify confirm-line comment
- refactor(ai): scope stub canned lookup, clarify STUB_LOW_CONF default
- refactor(api): reference canonical payment union, clarify confirm-line comment
- refactor(ai): scope stub canned lookup, clarify STUB_LOW_CONF default
- refactor(backend): reject non-string warehouse fields, hoist val helper
- refactor(backend): backfill warehouses by id, add banner
- refactor(dashboard): use commission tiers, honor range, scope commission visibility

### Performance
- perf(fe): exclude apple-splash images from SW precache (~10MB)

### Other
- chore: ignore root-level ad-hoc SQL scratch and PO data backups
- docs: changelog — general vendor links
- docs(vendor): implementation plan for customer-less general links
- docs(vendor): spec for customer-less general vendor links
- docs: refresh PWA smoke matrix with final automatable checks + M10 device checklist
- docs(changelog): record PWA, release tooling, and git hooks under Unreleased
- docs: PWA smoke matrix for release verification
- chore(hooks): enforce secret scan (commit) + audit/changelog/static-security (push)
- chore(fe): add workbox-window as devDep (peer of vite-plugin-pwa virtual register)
- docs(scan): spec for client-side MozJPEG compression before upload
- chore(fe): rephrase vite-plugin-pwa comments (rot-resistant)
- chore(fe): add vite-plugin-pwa (disabled), scaffold config
- docs(release): spec for standardized release + versioned docker images
- docs(transfers): note empty-TO discard diverges from reopen intentionally
- test(transfers): cover mixed-source discard; comment NULL origin intent
- docs(transfers): implementation plan for discarding a Pending transfer order
- docs(transfers): design for discarding a Pending transfer order
- i18n(submit): keys for new-vs-existing PO submit choice (EN+ZH)
- docs(submit): implementation plan for new-vs-existing PO choice
- docs(submit): design for desktop new-vs-existing PO choice
- chore(fe): allow inventory.jinhu.us in dev Vite host check
- docs(plan): multi-currency vendor bids implementation plan
- docs(spec): multi-currency vendor bids design
- test(be): cover DELETE status-meta attachment audit event
- docs(plan): reconcile audit plan with close/reopen work landed on main
- test(be): red tests for sell-order close + reopen workflow
- docs(plan): sell-order audit / change history implementation plan
- docs(plan): sell-order close + reopen workflow
- docs(spec): sell-order close + reopen workflow design
- docs: pin two recurring CLAUDE.md tripwires (vitest filter, 500 sink)
- docs(spec): sell-order audit / change history design
- types(fe): RefPrice gains lastPrice/lastPriceAt/lastPriceSource/recentPrices
- test(be): align maxBuy assertions with last_price basis
- docs: implementation plan for Market manual price + last-price freshness
- docs: spec for Market manual price entry + last-price freshness
- test(be): sell_order_events append-only trigger fires on UPDATE
- test(be): concurrent archive guarded by row lock
- test(be): sell-order archive edge cases (draft, 409, role, events)
- test(be): strengthen archivedAt assertions (typeof string)
- chore: gitignore recycle_erp_dump.sql and *.sql.dump
- docs(db): correct sell_order_events trigger comment
- docs: market-value MCP setup guide
- chore(data): legacy RAM inventory PO push script
- docs(plan): sell-order archive implementation plan
- docs(spec): sell-order archive design
- docs+settings: market-value MCP README + Copy button + drop unused redirectUris
- chore(backend): add jose + @modelcontextprotocol/sdk deps and OAUTH env surface
- docs(plan): market-value MCP + OAuth + write endpoint implementation plan
- docs(spec): market-value MCP + write endpoint design
- test(sell-orders): use freeSellableLine to stabilize discount-removal test
- docs(claude-md): conventions & quirks for future contributors
- docs(metrics): document :9090 scrape endpoints in README
- docs(env): point R2 vars at terraform outputs in apps/backend/.env.example
- test(metrics): drop redundant CSRF-exempt case (covered by main test)
- docs(plan): system metrics sidecar implementation plan
- chore(compose): one-command boot, bind-mounted DB, default admin user
- docs(spec): system metrics sidecar design
- docs(plan): product-centric changelog & edit implementation plan
- docs(spec): product-centric changelog & edit design
- test(vendor-bids): promote excludes declined lines; forbidden for non-managers
- docs(vendor-bids): implementation plan
- docs(inventory): product-grouped inventory implementation plan
- docs(vendor-bids): design spec for vendor inventory & bidding link
- docs(inventory): product-grouped inventory design spec
- style(orders): make Add-line button accent-colored, right-aligned
- test(commission): assert PATCH members ignores stray commissionRate
- test(commission): assert profile commission = profit x rate (positive case)
- docs(commission): fix dashboard comments invalidated by per-order rate; harden test
- test(commission): static getTestDb import + assert draft→null branch
- docs: implementation plan — per-order commission rate
- docs: design spec — per-order commission rate (remove tiers/rules/default)
- docs(infra): note R2 token secret-in-state caveat in token.tf
- chore(infra): scaffold terraform dir + gitignore
- docs: Cloudflare Terraform module implementation plan + spec corrections
- docs: Cloudflare Terraform module design spec
- docs: R2_ATTACHMENTS_PUBLIC_URL must match how the custom domain serves (/<bucket> path)
- test(backend): scan-r2 isolate-safe via @aws-sdk doMock (real r2.ts, no spy)
- test(backend): make aws-sdk mocks isolate-safe (survive isolate:false)
- build(backend): dotenv is a runtime dep; index.ts comment not 'Worker'
- docs(backend): drop stale Hyperdrive mention in db.ts comment
- docs: README quick-start — Node backend + postgres-only compose up
- docs: README reflects Docker deployment, not Cloudflare
- build: full-stack docker compose (postgres + backend + web)
- build(frontend): Dockerfile + Caddy (static SPA + /api proxy)
- build(backend): Dockerfile (node22 + tsx, migrate-then-serve)
- test(backend): reopen 404 + empty-set 409; docs: lateral invariant, manifest i18n
- build(backend): tsx dev/start, .env config, drop wrangler.toml
- test(backend): scan-r2 asserts S3 contract, not Worker binding
- i18n: transfer-orders strings (en + zh)
- build(backend): add node-server + aws s3 sdk, drop wrangler deps
- docs: implementation plan for Docker migration
- docs: spec for Cloudflare Workers -> Docker backend migration
- docs(maintenance): deferred plan for orphan label-scan sweep
- test(backend): scope transfer_orders schema check to public schema
- docs(transfer-orders): implementation plan
- docs(ram): 2nd T12 gap — Dashboard/Orders/DesktopOrders RAM labels
- docs(ram): T12 gap — Inventory/DesktopInventory RAM labels
- docs(ram): T8 covers DraftLine (4th RAM line shape)
- test(ai): stub RAM has generation + device type
- docs(transfer-orders): design for first-class transfer orders + manifest + reopen
- docs(ram): make 0027 migration idempotent (runner re-applies all)
- docs(ram): implementation plan for type/generation split + Gemma 3 OCR
- docs(ram): design for type/generation split + Gemma 3 OCR
- i18n: transfers page strings (en + zh)
- docs(inventory): implementation plan for receive / transfers page
- docs(inventory): clarify /transfers excludes purchase-origin In Transit lines
- docs(inventory): design for receive / transfers page
- docs: move verified-complete plans to finished/
- chore(backend): db connection scoping + node:async_hooks ambient types
- docs(ocr): document OpenRouter provider, remove STUB_OCR
- style(frontend): single-line sell-order route branch for consistency
- test(frontend): add vitest + failing sell-order route tests
- docs: implementation plan for sell order share link
- docs: design spec for sell order share link
- test(ocr): cover all stub categories + low-conf category passthrough
- docs: implementation plan for OpenRouter OCR provider
- docs: design spec for OpenRouter OCR provider
- docs(frontend): drop payment terms from lookups cache comment
- docs(frontend): fix stale customer-status comment
- docs: implementation plan for customer page refinements
- docs: spec for customer page refinements (contact/address, drop terms+credit+payment_terms)
- docs: archive finished plans; record Task 8 done + branch retained
- docs: record reconciliation execution status (pushed; branch retained)
- chore: reconciliation plan doc, gitignore, lockfile
- docs: RAM line submission refinements implementation plan
- docs: RAM line submission refinements design spec
- docs(plan): AI scan image preview implementation plan
- docs: inventory transfer design spec
- docs: implementation plan for warehouse detail fields
- docs: spec for server-backed user preferences
- docs: spec for persisted warehouse detail fields
- docs(spec): disk health + HDD category design
- docs(spec): AI scan image preview in order-line edit
- docs: mobile design parity implementation plan
- docs: extend order deep-linking to desktop in mobile parity spec
- docs: mobile design parity spec
- test(backend): app/auth helpers + smoke test
- test(backend): db reset + migration helper
- chore(backend): vitest config
- chore(backend): add vitest + tsx for test harness
- chore(backend): stabilize typecheck baseline (4 pre-existing errors)
- docs: PRD + implementation plan

## [0.1.0] - 2026-06-05

First tagged point in the repository's history — tagged retroactively at the
last commit that carried version 0.1.0. The work it covers predates per-version
sections and is catalogued in full under [0.1.1] below, so it is not repeated
here.

## Pre-v1.52 detail (unversioned)

Written against a permanent `## [Unreleased]` heading between 2026-07 and
2026-08-06, before per-version sections resumed. These describe work shipped
between v1.17 and v1.51 and therefore **overlap** the generated bullets above
rather than replacing them; they are kept because the prose carries reasoning
the commit subjects do not, and it cannot be split across versions
automatically — it was rewritten from the subjects, so it no longer matches
them.


### Removed
- feat(purchase-orders)!: remove the per-PO commercial invoice PDF — `GET /api/orders/:id/invoice`, `lib/pdf.ts`, the `pdfkit` dependency and the download buttons on both the desktop orders table and the mobile order detail. The spreadsheet is the only PO download now, and it's the one that carries the line detail. Keeping both meant every new line field had to be added to two divergent `order_lines` queries, and the PDF's had already fallen behind — it never learned `chip_number` or `sell_price`. Also clears the dead `invoice*` / `invStamp*` i18n vocabulary (31 EN + 29 ZH keys, zero call sites) stranded by the client-side print-to-PDF component that route replaced

### Features
- feat(inventory): the grouped inventory spreadsheet (`GET /api/inventory/export?view=grouped`) drops its five money-and-people columns — `Cost min`, `Cost avg`, `Cost max`, `Sell price` and `Submitted by`. Grouped is the sheet that gets sent outward to vendors and buyers, and it carried per-part cost spreads on every row; the flat export keeps `Unit cost`, `Sell price`, `Profit`, `Margin %` and `Submitted by` for internal use. The grouped tail is now purely stock shape: warehouses, qty, in stock / in transit / reviewing, PO and lot counts. The desktop grouped *view* is unchanged — it reads a different endpoint (`/api/inventory/products`) and still shows the cost range to managers
- feat(inventory): serial numbers are now first-class in search — the activity feed (`GET /api/inventory/events/all`) matches on `serial_number` like the inventory list and product views already did, and both search placeholders say so ("Search part #, serial #, brand, ID…" / 「搜索零件号、序列号…」), because a searchable column nobody knows about isn't searchable
- feat(purchase-orders): serial-number rules on order lines — a DDR5 RAM line must carry serial numbers, and once serials are entered on **any** line their count must equal the line qty. Both rules live once in `@recycle-erp/shared` (`serialIssue`) and gate every save path: the desktop submit drawer, the desktop edit-order page, the mobile submit form (a blocking dialog names the offending line and keeps the form open — nothing reaches the DB), and the backend `POST /api/orders` / `PATCH /api/orders/:id` (400). Existing rows predating the rules stay editable for price/status; the check only fires when an edit actually changes serials, qty or generation. The serial field's label drops "optional" for a red "required for DDR5" tag the moment DDR5 is picked
- feat(purchase-orders): the PO spreadsheet drops the composed `Item` label column — once every attribute has its own cell it only repeated them back as an unsortable string. `poLineLabel` goes with it, leaving no composed-label code on the PO export path. The download control in the orders table's Actions column and on the mobile order detail also switches from the document glyph to the download arrow: it was the PDF button that used to carry the download icon, so removing the PDF left the surviving Excel button looking like "view file" rather than "download"
- feat(purchase-orders): the PO spreadsheet's Line items tab carries the order category's full spec set — RAM gets Part #, Chip #, Brand, Capacity, Gen, Type, Class, Rank and Speed; SSD and HDD get Interface, Form factor, Health % and RPM — instead of collapsing every attribute into one composed `Item` string. It was the last export still doing that: inventory and the vendor price template moved to per-attribute columns in July, so you could sort stock by rank but not the PO that bought it. A `Serial #` column joins the cost tail, and the `Category` column goes — an order is single-category by construction, and the Payment tab already names it. The category→column table now lives in `lib/categoryColumns.ts` shared with the inventory export, which had been its sole owner; the vendor bid sheet keeps its own copy on purpose, because its header text is load-bearing for the round-trip price import parser and must not move when this one does
- feat(purchase-orders): the manager-only stage filter moves out of its own card and into the orders table's toolbar as a row of status chips, so category, status, archived, search and columns are finally one control cluster instead of two. It was the largest element on the page and the least-used control on it; as a card it also repeated the KPI row's label-over-big-number pattern directly above the KPI row. Per-stage revenue is dropped from view (it survives in each chip's tooltip) because selecting a stage already recomputes the whole KPI row from the filtered set — the card was previewing a number the row below it was about to show. The separate "Clear filter" button goes with it: an `All` chip clears, and clicking the active chip toggles off. Chip dots now take their colour from `statusTone(label)` rather than `WorkflowStage.tone`, which disagreed on Reviewing (green vs the Status column's amber) — invisible when the two sat 200px apart, glaring once the chips landed directly above that column. That leaves `tone`, `short` and `icon` unused on `WorkflowStage`, so the type is down to `{id, label}`
- feat(activity): the Activity register loads as you scroll instead of making you press "Load older" — an IntersectionObserver on a sentinel after the last row fetches the next page a screenful early, so rows are usually already there when you reach the bottom. The column head and day rules now stay pinned while the rows move: `.ac-scroll` sets `overflow-x`, which forces `overflow-y: auto` and makes it a scroll container, so `position: sticky` inside it was clamping to a box that the *page* was scrolling away — the register now owns a bounded height and is the real scroller, matching how `.page-history` already works
- feat(activity): new manager-only **Activity** page (`/activity`, under a new "Oversight" group in the sidebar) — the first place the four audit ledgers are readable together. `order_events`, `sell_order_events`, `inventory_events` and `ref_price_events` union into one reverse-chronological register, filterable by area (purchase orders / sell orders / inventory / pricing), by action, by person, by date range, and by free text over record ids, part numbers and names. Raw kinds are normalised to one vocabulary — `advanced`, `status_changed` and `status` all read as "Status" — via a map in `@recycle-erp/shared` so the query and the labels can't drift. Laid out as an aligned five-lane register rather than the dot-and-rail timeline the per-record panels use: the diff gets its own column, so a run of price changes can be scanned straight down. Each row carries a 3px stripe naming its source ledger, which collapses to one colour as you filter. Migration 0079 adds the `created_at DESC, id DESC` feed indexes every ledger was missing — all four only indexed `(parent_id, created_at)`, so a global feed would have seq-scanned every one of them. Vendor bids and member/permission changes are absent because they write no audit rows anywhere today
- feat(purchase-orders): Chip # on a RAM line is required only for **Micron** and **Other** brands — the two whose part numbers don't identify the module on their own. Samsung, SK Hynix and Kingston lines no longer block Confirm/submit on a blank Chip #, and the label drops its `*` as soon as the brand says so. The rule lives in one place (`chipNumberRequired` in `lib/ramRequired.ts`) so the desktop drawer, the mobile submit form and both validation gates can't drift apart. A blank brand asks for nothing — brand itself is already flagged. An off-catalog brand value (legacy or imported, rendered as a one-off option) counts as neither, so it isn't asked for either
- feat(purchase-orders): "Confirm line" in the desktop edit-order drawer now writes that line straight to the database instead of parking it in local state until Save — the drawer already had the hook, the edit page just never passed it, so the button silently only closed the drawer. Confirming a brand-new line stores the id the backend hands back, and `save()`'s delete diff now tracks the live set of persisted ids rather than the page-load snapshot: without that, a line added, confirmed, then removed would have been left orphaned in the order
- feat(purchase-orders): the Submission attachments dropzone on the submit page accepts spreadsheets (`.xlsx`, `.csv`) alongside receipts, so a vendor's lot manifest or price list lands on the PO instead of staying in someone's inbox. Migration 0077 widens the stored `upload_allowed_mime` setting — `getUploadLimits` intersects it with the hard allowlist, so the constant alone would have changed nothing on a deployed database. Legacy `.xls` and macro-enabled `.xlsm` stay refused; the attachments bucket is public, so non-image uploads are now served `Content-Disposition: attachment`
- feat(inventory): the SSD capacity dropdown offers `1TB` in place of `1024GB` (migration 0078 plus the matching `seed.mjs` list). Existing records holding `1024GB` still display — the catalog select renders an unknown stored value as a one-off option
- feat(purchase-orders): the PO timeline records **everything** on a draft, with nothing filtered out. Reverses the v1.32.0 decision to drop a lone `total_cost` change while drafting — every move is logged, so building a PO shows one "Total cost" row per line appended. Also closes two paths that recorded nothing at all: `POST /api/orders/draft` (the empty-draft "New order" button) now writes its `created` event, and `serial_number` — patchable but missing from the audit allow-list — is now diffed like every other line column

### Fixes
- fix(sell-orders): the vendor bid sheet's sorting and header filter, shipped straight to `main` as hotfixes on 2026-08-06, are finally on `dev` — without this back-port the next `dev`→`main` release would have reverted both in production. Rows now leave pre-sorted the way the desk reads them (capacity, then rank, speed, brand, numerically collated so 8GB sorts below 128GB and specless manual lines sink), the packing tabs use that same order so a picker and a manager find a product in the same place, and every category tab carries an autofilter across the full header row. The workbook now ships with **no sheet protection at all** — the prod version kept a passwordless lock that a manager had to lift through Review > Unprotect before reshaping a long sheet, and it was never security. Nothing downstream cares: the import parser re-locates columns by header text and matches rows by part number, never by position
- fix(mcp): a tool that runs and fails now answers with a normal result carrying `isError: true` and a text explanation, instead of a JSON-RPC error. Only genuine protocol failures — unknown tool, `insufficient_scope`, a malformed request — stay error responses. ChatGPT relays a JSON-RPC error to the model as `{"type":"json_rpc_error",…}`, which reads as a dead endpoint rather than a bad argument: one `set_market_price` call against a part number that didn't match returned `-32602 not_found`, and ChatGPT concluded the write tool had been "disabled by the system" and refused to write prices for the rest of the session, including for parts that would have matched. `not_found` now names the part number back so the two cases are distinguishable, and both write tools' descriptions tell the client to fix its arguments and retry. Every tool also ships MCP `annotations`: without them a client applies the spec defaults (`readOnlyHint: false`, `destructiveHint: true`, `openWorldHint: true`), which is why ChatGPT's connector panel labelled all five — the three read-only ones included — "public write / destructive / open world" and gated reads behind elevated-risk confirmations
- fix(version): the settings footer and the mobile About sheet showed a 40-character commit sha next to the version — unreadable, and it answers a question nobody in the office asks. Both now show the date the running build was made ("v1.48.1 · Aug 5, 2026"), with the sha kept on the footer's hover title for deploy checks. `/api/health` gains a `builtAt` field, stamped into the image by `apps/backend/Dockerfile` (a `RUN date` layer after the source COPY) because no builder hands one in — Railway, which builds every prod deploy, passes no build args and injects no build timestamp. A host `pnpm dev` run has no image and so no date: the field is null and the UI shows the version alone
- fix(workspace): `GET /api/workspace` 403'd every non-manager, which silently pinned purchasers to the SPA's hardcoded fallbacks instead of the configured values — `auth.tsx` catches the rejection with a `console.warn` and carries on, so a manager setting `low_health_pct` to 70 left purchasers still seeing 50, the exact divergence migration 0025 existed to remove. The endpoint now filters by key (`PURCHASER_VISIBLE_SETTINGS`) rather than rejecting the request; pricing, upload and notification keys stay manager-only, so the original security intent holds
- fix(auth): a 401 from `POST /api/auth/login` was treated as an expired access cookie, so every mistyped password fired a refresh that could never succeed and then dispatched `auth:unauthorized`, wiping the already-loaded lookups/workspace caches. `isSessionEndpoint()` now exempts login alongside refresh — an endpoint whose job is to *establish* a session must never be wrapped by the 401→refresh→retry path
- fix(purchase-orders): receipt auto-rename shipped every non-PDF attachment to OpenRouter as image bytes — it guarded on `application/pdf` rather than checking for an image. A spreadsheet upload would have paid for a guaranteed-useless vision call; the guard is now a positive `image/*` check
- fix(purchase-orders): the Activity panel was permanently empty on Draft POs — every audit write was gated behind `lifecycle !== 'draft'`, a guard that existed only because the append-only trigger used to block the draft-delete cascade (migration 0038 fixed that long ago). Drafts are now audited like every other stage, so edits, line add/remove and Done-evidence show up while the order is still being prepared

- fix(purchase-orders): building a draft no longer buries its timeline — the submit form re-sends the running `totalCost` with every line it appends, which logged one "Total cost: $X → $Y" row per line (a 30-line PO produced 29). A lone `total_cost` delta on a draft is that recompute, not a decision, so it isn't recorded
- fix(frontend): an order event whose kind the loaded bundle doesn't know (backend and Worker deploy independently) rendered `undefined` and threw, unmounting the whole app — there is no ErrorBoundary. Unknown kinds now degrade to the raw kind name

### Features
- feat(purchase-orders): every PO timeline now opens with an "Order created" entry (category, line count, units); migration 0076 backfills it for existing orders so historical POs no longer start mid-story. Backfilled rows show the category only — their counts reflect backfill time, not creation time — and `db:seed` writes the event too so a fresh dev DB doesn't look broken

### Fixes
- fix(inventory): malformed ids in the selection export 400 instead of 500 (Postgres uuid cast blew up on junk tokens); junk mixed into a valid selection is dropped
- fix(health): /api/health reported version "dev" / commit "unknown" on Railway (dev AND prod) — release-time build args are never passed there and the Dockerfile baked "dev"/"unknown" defaults into env; args now default empty and health falls back to the root package.json version and RAILWAY_GIT_COMMIT_SHA, so the frontend version display and deploy checks show the real release

### Features
- feat(inventory): Export honors the row selection — with rows checked it exports exactly those lines (count badge on the button); with none it keeps exporting the full filtered set
- feat(exports): designed workbook styling across all Excel exports — dark header band with white bold text (matching the bid sheet), zebra-striped data rows, taller frozen header

### Features
- feat(inventory): the inventory export (flat and grouped views) now splits into one worksheet per category (RAM / SSD / HDD / Other) with granular spec columns per tab — same format as the sell-order download; the composed Spec string and Category column are gone

### Fixes
- fix(sell-orders): packing-checklist tabs renamed to "Pack - <warehouse>" (e.g. Pack - DEN) so they read apart from the category bid tabs

### Features
- feat(purchase-orders): RAM lines in the PO submit form require Brand, Capacity, Generation, Type, Class, Rank, Speed, Chip # and Part Number on both desktop and mobile — save/confirm/submit block with a message naming the missing fields

### Features
- feat(sell-orders): price template gains per-warehouse packing-checklist tabs after the category tabs (stacked per-category sections, tickable "Packed ✓" column, qty subtotals + warehouse total; price-free by design and invisible to the price-import parser)

### Features
- feat(sell-orders): price template splits into one worksheet per category (RAM / SSD / HDD / Other tabs, each with only its own spec columns); the price-import parser now reads every tab, so prices filled on any sheet round-trip

### Features
- feat(sell-orders): price template gains a RAM `Chip #` spec column and a blank vendor-editable `Note / 备注` column (unlocked like Unit Price, never pre-filled); download menu collapses to a single Price template button

### Removed
- feat(sell-orders)!: remove the packing-list PDF and per-order spreadsheet exports — the vendor price template is the only sell-order download; the sectioned-sheet primitive leaves lib/xlsx.ts with them

### Features
- feat(sell-orders): vendor price template carries per-attribute spec columns for the categories on the order (RAM Gen/Type/Class/Rank/Speed, SSD/HDD Interface/Form factor/Health/RPM) and links item photos as clickable Image URL cells instead of embedded thumbnails

### Fixes
- fix(sell-orders): restore per-attribute spec columns in the spreadsheet (Type / Rank / Speed etc. each in its own column) — v1.24.1 collapsed them into one composed Spec field on a misread request

### Features
- feat(sell-orders): per-category spreadsheet export — Summary tab split into stacked RAM/SSD/HDD/Other sections (one aggregated row per part number, bold subtotals, order total) with category-specific spec columns; one detail tab per category replaces the per-warehouse tabs (Warehouse becomes a column)
- feat(sell-orders): order-summary card with Subtotal / Adjustment / Total breakdown rows (signed delta + pct chip) replacing the strikethrough; adjust-total is never disabled — in the edit modal the negotiated total joins the draft and applies on Save (PATCH → adjust → status)

### Fixes
- fix(sell-orders): show the adjust-final-total pencil in the edit modal too (disabled with a "save first" hint while the draft has unsaved changes); adjusted badge stays visible in edit mode until the line set diverges

### Features
- feat(sell-orders): negotiated final-price adjustment — POST /:id/adjust-price prorates a buyer's counter-offer across line prices (Draft/Shipped/Awaiting payment); Total box shows struck-through original + "Adjusted −X%" chip with who/when; price_adjusted history event; list-row marker
- feat(sell-orders): vendor price round-trip — downloadable bid-sheet XLSX (item photos, bilingual instructions, locked except Unit Price) + drag-and-drop import with client-side precheck, part-number matching, and preview-before-apply

### Fixes
- fix(sell-orders): bid-sheet photos on dev — fall back to the scan's public delivery URL when the stored R2 key isn't in this environment's bucket (dev DB is a prod copy, keys point at the prod bucket)

