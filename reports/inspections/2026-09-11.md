# Daily production-health inspection — 2026-09-11

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (60th consecutive day). `dev` advanced one more release since yesterday; `main`/production unchanged.**

Every path specified in this routine's task was checked again today and
found unavailable, unchanged from every report since
[2026-07-14](./2026-07-14.md):

1. **Railway MCP server tools** — `ToolSearch` for `"get_logs
   list_deployments service_metrics http_error_rate"` and for
   `"railway"` returned no Railway tool, only unrelated GitHub / task /
   design tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in
   this session's environment (`env | grep -i railway` and `printenv |
   grep -i railway` both empty) and no `.env` file exists anywhere in
   this worktree, the main checkout, or the filesystem (`find / -iname
   ".env*"` only turns up the two `.env.example` templates checked into
   the repo).
3. **Direct health-check probe** as a partial substitute — `curl` to
   `https://erp.recycleservers.com/api/health`,
   `https://inventory.recycleservers.com/api/health`, and
   `https://backboard.railway.com/graphql/v2` all failed identically:
   `CONNECT tunnel failed, response 403`. `curl -sS
   $HTTPS_PROXY/__agentproxy/status` confirms all three hosts are logged
   under `recentRelayFailures` as `connect_rejected` (`gateway answered
   403 to CONNECT`); `noProxy` still covers only package registries and
   the Anthropic API — no path to Railway or the production domains.
4. `ListConnectors` filtered on `"railway"` returned an empty list.
5. No `railway` CLI binary is present (`which railway` empty) and no
   `~/.railway` credentials directory exists in this environment.

No Railway or production API calls were attempted with fabricated or
guessed credentials.

**What's different from yesterday:** `origin/dev` moved from **v1.136.0**
to **v1.137.0** — one feature PR merged (2026-09-10T18:27–18:29 UTC):

- **#297 / v1.137.0** (RS-037) — archiving a purchase order now takes its
  goods out of stock. Every non-Sold line on an archived PO moves to a new
  `Archived` line status (dropping it out of every stock/sellable view the
  way `Sold` does); unarchive reads the line's prior status back off its
  audit trail. Archiving is refused if an open sell order still names one
  of the PO's lines (`code: committedLines`) until those lines are removed
  from the sell order first. An archived PO is frozen against edits,
  deletes, stage moves, carrier polling, and inventory line edits.
  Migration `0121` backfills lines on already-archived POs.

`origin/main` (production) is **unchanged at v1.132.0** — no new
`dev → main` PR is open, no new commits landed there, so **no new
production deploy** happened in this window; the `dev` work above,
including migration `0121`, is not yet live. All three CI runs for the
#297 push (`version-check` run 543, `backend-tests` run 109,
`deploy-frontend` run 212) completed with `conclusion: success`; the
follow-up ticket-close commit's `version-check` (run 545) also succeeded.
Nothing stuck, failed, or anomalous.

Because `main` didn't move, this is the routine's ordinary blind spot
again: zero visibility into whether `backend`, `Postgres`, `db-sync`, or
`pg-r2-backup` logged any errors, and zero visibility into `backend`'s
actual HTTP error rate, over the last 24 hours — but at least there is no
elevated risk from an unverified same-day production release, since
production didn't change.

## Access failure — details

See "Summary verdict" above; this is now the 60th run in a row unable to
reach Railway by any sanctioned path. The blocking conditions are
unchanged from the [2026-09-10 report](./2026-09-10.md).

### How to fix (unchanged ask, now open 60 reports / ~8.5 weeks)

- Register a Railway MCP server for this scheduled session's environment
  (exposing `get_logs`, `list_deployments`, `http_error_rate`,
  `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a
  Railway **project token** or **team API token** scoped to
  `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`)
  with read access to deployments, logs, and metrics for `backend`,
  `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach
  `erp.recycleservers.com` / `inventory.recycleservers.com` so
  `/api/health` can be polled as a cheap liveness/version signal even
  without full log access.

If Railway visibility for this routine is not actually a priority,
consider disabling it rather than letting it accumulate unread reports —
but if it stays enabled, one of the three options above is the only
thing that will make it useful.

## Errors found

Not collected by this routine — see access failure above. No
independent production-log data point is available for today's 24-hour
window.

## Root-cause analysis

Not applicable — no error data was available to analyze independently
this run.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" above) — this remains
   the sole blocking issue for this routine's entire purpose.
2. Once access is restored, prioritize a check against the pending `dev`
   work (v1.134.0–v1.137.0, PRs #291/#293/#294/#297) at the point it
   merges to `main` and deploys. Of these, PR #297 (v1.137.0, migration
   `0121`) is the one most worth a first-deploy log check: it backfills
   line statuses on already-archived POs and changes what the stock and
   sellable-goods readers (`services/orderGoodsTotal.ts` and the stock
   views that never join `orders`) return, so a first-production-run
   check on backfill row counts and any unexpected `committedLines`
   rejection spikes would be the highest-value use of restored log
   access.
3. No application code changes are recommended by this run — nothing was
   observed to diagnose independently.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to
  this routine — no deployment status, log, or error-rate data could be
  retrieved directly.
- **GitHub / release state:** `origin/main` (production) unchanged at
  **v1.132.0** (PR #288, merged 2026-09-08T00:40 UTC) — no new commits,
  no open `dev → main` PR, so no production deploy in this window.
  `origin/dev` advanced from v1.136.0 to **v1.137.0** via PR #297
  (merged 2026-09-10T18:27–18:29 UTC), now five releases ahead of
  production — a growing pending-release queue, though still ordinary
  pre-release state, not itself a health issue. All CI runs visible in
  the last 24h (`version-check`, `backend-tests`, `deploy-frontend`)
  completed with `conclusion: success`; none stuck or anomalous.
- **Frontend (Cloudflare Worker):** out of scope per task instructions;
  `deploy-frontend` ran on the `dev` push per normal workflow config (it
  deploys the dev-tracking preview, not production, since `main` didn't
  move) — no anomaly to report.
