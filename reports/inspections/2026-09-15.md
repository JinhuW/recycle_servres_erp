# Daily production-health inspection — 2026-09-15

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (64th consecutive day).** Unlike several recent reports, **no new release landed in this blind window**: `origin/main` is unchanged since yesterday's report (still `53334a8`, v1.140.1), and `origin/dev` has not advanced past `611bdfd` (v1.143.0) either. So today's gap in coverage is smaller in scope than yesterday's, but the routine still has no independent way to confirm the four Railway services (`backend`, `Postgres`, `db-sync`, `pg-r2-backup`) are healthy right now, or that the four deploys already flagged as unverified in prior reports came up clean.

Every path specified in this routine's task was checked again today and found unavailable, unchanged from every report since [2026-07-14](./2026-07-14.md):

1. **Railway MCP server tools** — `ToolSearch` for `"railway logs deployments metrics"` and for `"railway"` alone returned no Railway tool, only unrelated GitHub / task / connector / Monitor tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment (confirmed against the full `env` variable-name listing) and no `.env` file exists anywhere in this worktree (only the checked-in `.env.example` template); `ListConnectors` (filtered on `"railway"`) returned `[]`.
3. **Direct health-check probe** as a partial substitute — `curl` to `https://erp.recycleservers.com/api/health`, `https://inventory.recycleservers.com/api/health`, and `https://backboard.railway.com/graphql/v2` all failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms all three hosts as `connect_rejected` (`organization policy` / gateway 403 to `CONNECT`); `noProxy` still covers only package registries and the Anthropic API, not either production host or Railway's API.
4. No `railway` CLI binary is present (`which railway` empty) and no `~/.railway` credentials directory exists.

No Railway or production API calls were attempted with fabricated or guessed credentials.

**What's different from yesterday — no new release, so the blind window did not grow today:**

- `origin/main` (production): still `53334a8` / v1.140.1, exactly as reported yesterday. No new `dev`→`main` merge occurred.
- `origin/dev`: still `611bdfd` / v1.143.0 (through PR #330), exactly as reported yesterday. No new commits landed on `dev` since 2026-09-14T12:02:14-04:00 (checked with `git log --since` against both branches — zero new commits on either).
- No new files were added under `docs/debug-notes/` since yesterday.

This means the set of deploys still awaiting independent verification is unchanged from the 2026-09-14 report: 2026-09-11T20:25 UTC, 2026-09-13T09:51/15:09 UTC, and 2026-09-14T11:58 UTC.

## Access failure — details

See "Summary verdict" above; this is the 64th run in a row unable to reach Railway by any sanctioned path. The blocking conditions are unchanged from the [2026-09-14 report](./2026-09-14.md).

### How to fix (unchanged ask, now open 64 reports / over 9 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window.

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. Carried over from prior reports, still unverified once access is restored:

- `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — additive-only (`ON CONFLICT DO NOTHING`), lowest priority to verify.
- The frontend pagination fix in v1.140.1 (RS-049, PR #324, `apps/frontend` desktop Purchase-orders page) — worth a spot-check of `/api/orders` request volume/latency post-deploy, since it now pages through the full result set instead of one bare request.
- `apps/backend/src/routes/orders.ts` (the v1.138.5 backward-move guard fix) and `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`, exercised by migration `0124`) — still no confirmation either behaved correctly in production.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" above) — still the sole blocking issue for this routine's entire purpose, now on its tenth week.
2. As soon as access is restored, check production logs/metrics across all four still-outstanding blind-window deploys (2026-09-11T20:25 UTC, 2026-09-13T09:51/15:09 UTC, and 2026-09-14T11:58 UTC) for any anomalies, with particular attention to `/api/orders` request volume after v1.140.1 and any residual risk from the v1.138.x archive/sell-order-line migrations (`0121`–`0124`).
3. PO-1370 (flagged in the [2026-09-13 report](./2026-09-13.md) from a committed historical debug note) remains an open, not-yet-resolved manual-data task for a human to action — repeating the flag here since it has not been reported as closed.
4. No application code changes are recommended by this run — no new release landed in this window, and nothing new was observed to diagnose independently.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly.
- **GitHub / release state:** `origin/main` (production) unchanged at **v1.140.1** (`53334a8`) since yesterday — no new release. `origin/dev` unchanged at **v1.143.0** (`611bdfd`, through PR #330) — no new commits on either branch in the last 24 hours.
- **Frontend (Cloudflare Worker):** out of scope per task instructions; no deploy activity to report since none was triggered (no push to `main`).
