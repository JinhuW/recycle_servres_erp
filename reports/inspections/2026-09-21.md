# Daily production-health inspection — 2026-09-21

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (70th consecutive day).** Every path this routine can use to reach Railway (MCP tools, the GraphQL API fallback, a direct `/api/health` probe) is blocked exactly as it has been since [2026-07-14](./2026-07-14.md). Since yesterday's report, production `main` advanced by **nineteen releases in roughly 23 hours** (v1.157.1 → v1.171.2, RS-080 through RS-094) — the full purchase-order hand-off / status-surface / commission rework that this routine has flagged as the system's largest unverified surface for over three weeks is now **entirely promoted to production**, and `origin/dev` and `origin/main` are back in sync at v1.171.2. This routine still cannot independently confirm any of it from logs, error rates, or deployment status — that requires the access this report has been asking for since week one.

### Access check (repeated from every report since 2026-07-14)

1. **Railway MCP server tools** — `ToolSearch` for `"railway logs deployments metrics"` and `"mcp__railway"` returned no Railway tool, only unrelated GitHub Actions, Monitor, and artifact-related tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment; no `.env` file present in the worktree or main checkout.
3. **Direct probe** — `curl` to `https://backboard.railway.com/graphql/v2` and `https://erp.recycleservers.com/api/health` both failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms both as `connect_rejected` ("gateway answered 403 to CONNECT — policy denial or upstream failure") at 2026-09-21T16:05:40Z / 16:07:15Z — same signature as every prior day.

No Railway or production API calls were attempted with fabricated or guessed credentials.

### What shipped to production since yesterday's report

`origin/main`'s `package.json` moved from **1.157.1** to **1.171.2**. `origin/dev` is at the same version — fully promoted, nothing outstanding.

The whole span is one connected body of work on the purchase-order lifecycle (RS-080 → RS-094), landed 2026-09-20 05:15 → 2026-09-21 04:32 (UTC-4):

| Version(s) | Ticket / PR | Change |
|---|---|---|
| v1.158.0–1.161.0 | RS-080 / #369–372 | Hand-off rebuilt as a single checkpoint with one shared readiness rule; desktop and phone PO pages rebuilt around order status |
| v1.161.1–1.161.2 | RS-081/RS-082 / #374–375 | PO page opens the tab matching the current stage, tab strip stops scrolling; Payment tab goes two-column |
| v1.162.0 | RS-083 / #376 | Phone PO page's sections become the desktop tabs as folds, one sheet per stage |
| v1.163.0–1.166.0 | RS-084–087 / #378, 379, 380, 381 | Commission tab records how commission was paid (screenshot-only, no method/txn-id columns after #131 walked that back); a fully-sold Done PO becomes `sold`; new POs default to 50% commission; camera picker asks which item |
| v1.167.0–1.170.1 | RS-088/RS-091/RS-092/RS-089/RS-090 / #382–387 | Cost-payment PayPal screenshot kept on the PO, bank payments link back; screenshot file name carries the PayPal txn id; any manager can review/move a PO regardless of warehouse; phone Review screen simplified; line photo ordering fix |
| v1.171.0 | RS-093 / #386 | Realized profit gets its own column toggle on the desktop PO list |
| v1.171.1 | RS-094 / #388 | **Max-effort pre-release review of the whole `main..dev` span** — see below |
| v1.171.2 | RS-094 / #389 | Follow-up: phone `useOrderEvents` tagged events by order id, fixing a bug where switching POs on the reused `OrderDetail` view could show the previous order's activity/timeline/stage look-back under the new one |

**RS-094 in detail** — before promoting v1.158.0–1.171.0 to `main`, a `max`-effort review of `origin/main...origin/dev` returned **fifteen verified defects**, all fixed pre-release (commit `2b707466`), covering: `handoffOrderTx` not clearing the PayPal id on a Self/Cash hand-off; an empty-body hand-off from a deactivated collector writing a nameless event instead of refusing; `setOrderPackageTx` box-adoption and re-registration edge cases; `unlinkOrderPackagesTx` refusing once a box is delivered (causing duplicate-PO creation via create-po); `missingWarehouse` not being enforced uniformly past Draft; plus a dozen frontend readiness/fold/label fixes. Tests were reported green (backend 1671/1671, frontend 461/461, typecheck clean) before the merge. Because the review ran pre-promotion, none of these fifteen defects independently reached production as separate incidents — same discipline as RS-076 (2026-09-19) and RS-068 (2026-09-18) before it.

**Migrations landed in this window** (`apps/backend/migrations/`):
- `0129_po_commission_payment.sql` — added `commission_method`/`commission_txn_id` columns + widened the `order_status_meta`/`order_status_attachments` status check to include `'Commission'`.
- `0130_po_sold_lifecycle.sql` — **data migration**, not additive-only: flips existing `done` orders whose every line is `Sold` to `lifecycle = 'sold'` and backfills an `order_events` audit row for each flip. Idempotent per its own comment, but this is the one migration in the window that mutates existing rows rather than just adding schema — worth a specific check that it completed and how many rows it touched, once log access exists.
- `0131_po_commission_payment_screenshot_only.sql` — drops the two columns `0129` added (design walked back to screenshot-only mid-span); dev-only columns, no data loss.
- `0132_po_commission_rate_default.sql` — sets a column default only.

No status-guard route files were touched beyond `apps/backend/src/routes/orders.ts` itself (510 lines changed — the PO status/hand-off rework), which is the expected owner of this logic per `CLAUDE.md`'s "extend the existing guard" rule; `sellOrders.ts` picked up a 5-line change. `apps/backend/src/services/orderHandoff.ts` changed substantially (289 insertions / 64 deletions) as the direct target of RS-080/RS-094.

The "Company pay" / "Self pay" fixed commission-payment-type enum (`CLAUDE.md`'s Fixed business rules) is unrelated to this window's new `commission_method` (`paypal`/`cash`, how the *purchaser* was reimbursed) — no conflict with that closed set.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window.

## Root-cause analysis

Not applicable in the usual sense — no error data was available to analyze independently this run. By code-reading alone, this is the largest single-window production change this routine has seen in its run (19 releases, one connected feature area, fully promoted): the `orders.ts` route and `orderHandoff.ts` service — already flagged as the system's most-changed, least-verified surface for the past three weeks (RS-050, RS-071/074/076, RS-080) — just had that entire consolidation land in production at once. The mitigating factor is RS-094's max-effort pre-merge review, which caught and fixed fifteen real defects in exactly this code before release; the residual risk is defects that review missed, and the one data-mutating migration (`0130`).

Carried over from prior reports, still unverified due to the same access gap:
- `apps/backend/src/routes/orders.ts` / `apps/backend/src/services/orderHandoff.ts` — now the single largest unverified change in this routine's history, per above. Top priority once logs are reachable: watch for 4xx/5xx on `/api/orders/:id/handoff`, `/api/orders/:id/package`, and PATCH order-status endpoints since 2026-09-20 05:15 UTC-4.
- `apps/backend/migrations/0130_po_sold_lifecycle.sql` — the one data-mutating migration in this window; confirm it ran cleanly on the prod DB.
- `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — additive-only, lowest priority, carried for completeness.
- `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`) — still no confirmation it behaved correctly in production.
- RS-063's 3.5-hour live guard-bug window (2026-09-18 01:49–05:20 UTC, fixed at v1.149.4) — whether any of the 43 legacy `$0` POs were actually hit remains unconfirmed.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" below) — the sole blocking issue for this routine's entire purpose, now on its tenth week and at its highest-stakes point yet: the full PO hand-off/status/commission rework (RS-080–RS-094, 19 releases) is now live in production with zero independent confirmation.
2. As soon as access is restored, prioritize, in order: (a) the RS-080–RS-094 span on `orders.ts`/`orderHandoff.ts` — check error rates and logs on hand-off, package, and status-transition endpoints since 2026-09-20 05:15 UTC-4; (b) confirm `0130_po_sold_lifecycle.sql`'s row-flip completed without error; (c) the RS-063 guard-bug window from the 2026-09-18 report, still open; (d) RS-057's depressed `/api/payments*` volume window (2026-09-15/16), still outstanding.
3. PO-1370 (flagged in the [2026-09-13 report](./2026-09-13.md)) remains an open, not-yet-resolved manual-data task for a human to action — repeating the flag since it has not been reported as closed.
4. No application code changes are recommended by this run — this routine diagnoses, it does not fix, and no error evidence was available to diagnose independently. All fifteen RS-094 review findings are already fixed on `main` as of v1.171.1.

### How to fix the access gap (unchanged ask, now open 70 reports / exactly 10 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly.
- **GitHub / release state:** `origin/main` (production) advanced from **v1.157.1** to **v1.171.2** — nineteen releases across PRs #369–#389 (RS-080 through RS-094), landed in a roughly 23-hour window. `origin/dev` is fully in sync with `main` at **v1.171.2** — nothing outstanding to promote.
- **Frontend (Cloudflare Worker):** out of scope per task instructions, but note the `main`→prod pushes above each auto-trigger `.github/workflows/deploy-frontend.yml`; no independent confirmation that any of these Worker deploys succeeded. This window's changes span both frontend (phone/desktop PO page rebuilds, commission tab, realized-profit column, the RS-094 `useOrderEvents` fix) and backend (`orders.ts`/`orderHandoff.ts` rework, four migrations), so both a Railway backend deploy and a Worker deploy are implicated — neither independently confirmed.
