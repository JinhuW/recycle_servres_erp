# Daily production-health inspection — 2026-09-14

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (63rd consecutive day). A full production release landed in the blind window since yesterday's report: `main` advanced from v1.138.5 to v1.140.1 (PR #326, merged 2026-09-14T11:58:57Z UTC).** CI is green on the merge commit and the one new migration is a pure additive catalog insert (`ON CONFLICT DO NOTHING`, no existing-row mutation) — the lowest-risk migration this routine has seen in this window — but this routine still has no independent way to confirm any of that against the live system.

Every path specified in this routine's task was checked again today and found unavailable, unchanged from every report since [2026-07-14](./2026-07-14.md):

1. **Railway MCP server tools** — `ToolSearch` for `"get_logs list_deployments service_metrics http_error_rate"`, for `"deploy service metrics logs backboard graphql"`, and for `"railway"` alone returned no Railway tool, only unrelated GitHub / task / connector / Monitor tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment (confirmed against the full `env` variable-name listing) and no `.env` file exists anywhere in this worktree (only the checked-in `.env.example` templates); `ListConnectors` (filtered on `"railway"`) returned `[]`.
3. **Direct health-check probe** as a partial substitute — `curl` to `https://erp.recycleservers.com/api/health`, `https://inventory.recycleservers.com/api/health`, and `https://backboard.railway.com/graphql/v2` all failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms all three hosts as `connect_rejected` (`organization policy` / gateway 403 to `CONNECT`); `noProxy` still covers only package registries and the Anthropic API, not either production host or Railway's API.
4. No `railway` CLI binary is present (`which railway` empty) and no `~/.railway` credentials directory exists.

No Railway or production API calls were attempted with fabricated or guessed credentials.

**What's different from yesterday — a full `dev`→`main` release happened, and CI/process evidence is reassuring:**

`origin/main` moved **586c6f8 → 53334a8** via one merge commit after yesterday's inspection ran, landing PR **#326** ("release: v1.140.1 to prod", merged 2026-09-14T11:58:57Z, base `main`@586c6f8, head `dev`@0ab38b2, 9 commits, +2404/-180 across 31 files). Per its own body, it carries every version shipped to `dev` since the last release:

- **v1.138.6** (#318) — the Group-with picker on the Payments page was clipped off the table.
- **v1.139.0** (#321, RS-047) — the RAM_RANK catalog gains dual-die (`nDRxm`) and 3DS-stack (`nSmRxk`) notations; migration `0125_ram_rank_3ds_options.sql` backfills the two missing notation families as five new `catalog_options` rows (`4DRx4`, `8DRx4`, `2S2Rx4`, `2S4Rx4`, `4S2Rx4`), appended at positions 13–17 so no existing row is re-ranked, using `ON CONFLICT ("group", value) DO NOTHING` — purely additive, no update/delete path, and the PR notes production already held three `order_lines` typed past the dropdown with the now-added `4DRx4` value.
- **v1.140.0** (#282, RS-048) — Facebook tracker gains fleet accounts, phrases, and coverage fields.
- **v1.140.1** (#324, RS-049) — the desktop Purchase-orders page previously made one bare `/api/orders` request and silently read only the first 50-row keyset page; every in-browser figure (stage chips, KPI cards, search, sort, the Done prune) was computed over that truncated slice for any org with more than 50 POs. Fixed to page through the full result set. This is the change the PR body says was explicitly requested to reach prod ("the change Jinhu asked to push to prod").

All three CI workflows on the actual merge commit (`53334a8`) completed `conclusion: success`: `version-check` (run 607), `backend-tests` (run 143), `deploy-frontend` (run 225) — all started and finished within ~4 minutes of the merge (11:58:59Z–12:02:45Z). This routine could not retrieve a Railway-sourced commit status keyed to the merge commit itself (GitHub's per-commit status API surface used in past reports returns statuses attached to individual PR-head commits, and the one queried today — `0ab38b2`, a docs-only commit — read `backend: success / "No deployment needed - watched paths not modified"`, which is correct for that single docs-only commit but does not speak to the deploy triggered by the merge into `main`). No Railway-originated signal for the actual production deploy was obtainable today.

## Access failure — details

See "Summary verdict" above; this is the 63rd run in a row unable to reach Railway by any sanctioned path. The blocking conditions are unchanged from the [2026-09-13 report](./2026-09-13.md).

### How to fix (unchanged ask, now open 63 reports / ~9 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

A full `dev`→`main` release has now landed in this blind window (v1.138.5 → v1.140.1, three days after the previous release window on 2026-09-13). The process evidence (additive-only migration, three green CI runs on the merge commit) is reassuring, but it remains entirely secondhand — this routine still cannot independently confirm the migration applied cleanly on the production database, that no error-rate spike followed the deploy, or that the four Railway services are healthy right now.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window, including for the v1.140.1 deploy window itself (2026-09-14T11:58:57Z).

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. For context on what to check once access is restored:

- `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — confirm it ran and inserted the 5 new `catalog_options` rows without conflict; this is additive and idempotent, so the lowest-priority item to verify.
- The frontend pagination fix in v1.140.1 (RS-049, PR #324) — worth a spot-check of the desktop Purchase-orders page's request volume/latency post-deploy, since it now issues multiple paged `/api/orders` requests per page load instead of one; check for any new rate-limiting or elevated request-count patterns on that endpoint.
- Carried over from the still-unverified 2026-09-13 window: `apps/backend/src/routes/orders.ts` (the v1.138.5 backward-move guard fix) and `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`, exercised by migration `0124`) — no confirmation has yet been possible that either behaved correctly in production.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" above) — this remains the sole blocking issue for this routine's entire purpose, and a third production deploy landing blind in five days makes the gap's cost recurring, not hypothetical.
2. As soon as access is restored, check production logs/metrics across all three blind-window deploys (2026-09-11T20:25 UTC, 2026-09-13T09:51/15:09 UTC, and 2026-09-14T11:58 UTC) for any anomalies, with particular attention to `/api/orders` request volume after v1.140.1 (see Root-cause analysis above) and any residual risk from the v1.138.x archive/sell-order-line migrations (`0121`–`0124`).
3. PO-1370 (flagged in the [2026-09-13 report](./2026-09-13.md) from a committed historical debug note) remains an open, not-yet-resolved manual-data task for a human to action — repeating the flag here since it has not been reported as closed.
4. No application code changes are recommended by this run — nothing was observed to diagnose independently, and the one new migration and the one requested-for-prod fix in this window both went through normal PR review with green CI.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly, and no Railway-sourced signal (not even secondhand) was obtainable for the actual production deploy commit today.
- **GitHub / release state:** `origin/main` (production) advanced from **v1.138.5 to v1.140.1** via PR #326 (merged 2026-09-14T11:58:57Z UTC) — a full release. All three CI workflows on the merge commit (`version-check` run 607, `backend-tests` run 143, `deploy-frontend` run 225) completed with `conclusion: success`. `origin/dev` has since advanced further to **v1.143.0** (through at least PR #330, a PO-number search feature), not yet merged to `main`.
- **Frontend (Cloudflare Worker):** out of scope per task instructions; `deploy-frontend` ran successfully on today's `main` release push — no anomaly to report.
