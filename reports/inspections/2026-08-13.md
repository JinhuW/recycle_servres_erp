# Daily production-health inspection — 2026-08-13

## Summary verdict

⚠️ **INSPECTION INCOMPLETE — Railway access unavailable (31st consecutive day)**

This run could not reach Railway by any of the three sanctioned paths, so no
logs, deployment statuses, or HTTP error rates were collected for `backend`,
`Postgres`, `db-sync`, or `pg-r2-backup`. This is a report about the
inspection tooling, not a verdict on application health.

This is the same failure mode as every report from
[2026-07-14](./2026-07-14.md) through [2026-08-12](./2026-08-12.md) —
thirty straight prior runs, now thirty-one with today's. Nothing about the
environment changed overnight to fix it.

No new commit landed on `main` in this run's lookback window
(2026-08-12T16:14:39Z–2026-08-13T16:40:50Z) — `main` is still at PR #123 /
v1.62.6, unchanged since 2026-08-09T20:55:34-04:00 (2026-08-10T00:55:34Z, now
over 3 days ago). Today's blind spot does not stack a *new* unverified
release on top; it carries forward the two already unverified from prior
windows (v1.62.0-era on `prod`, v1.62.6 on `main`).

## What was tried

1. **Railway MCP server** — `ToolSearch` with `"railway logs deployments
   metrics"` and bare `"railway"`. No matching tools found in either query.
2. **Connector check** — `ListConnectors` filtered to `"railway"` returns an
   empty list. No Railway connector registered for this account/session.
3. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`)
   — checked for `RAILWAY_API_TOKEN` via `env | grep -i railway` (no
   output), a broader `env | grep -iE "token|api_key|key"` sweep (only
   unrelated tokens: GitHub, GCloud, AWS, Claude session tokens — nothing
   Railway-scoped), and the worktree's `.env` (file absent in this session's
   checkout, same as prior days). Probed the endpoint anyway:
   ```
   curl -sS http://127.0.0.1:36047/__agentproxy/status
   → "recentRelayFailures": [{
       "kind": "connect_rejected",
       "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
       "host": "backboard.railway.com:443"
     }]
   ```
   Confirmed via the agent proxy's own status endpoint that the proxy itself
   is healthy (`enabled: true`) — the failure is a deliberate policy denial
   on `backboard.railway.com`, not a proxy outage or transient blip.
   Identical failure signature to every prior day.
4. **Railway CLI / cached credentials** — not present on `PATH`
   (`which railway` → exit 1); not pursued further since the network-policy
   block above would stop a CLI call the same way regardless of credentials.
5. **Conclusion** — neither access path is configured in this environment,
   for the thirty-first day running.

## Secondary signal (GitHub, not Railway — informational only)

Does **not** substitute for backend log/error-rate visibility:

- `list_commits` on `main` since 2026-08-12T16:14:39Z (end of yesterday's
  window) returns **zero** commits. `main` remains at `9c3b34a` (PR #123,
  v1.62.6, merged 2026-08-10T00:55:34Z) — no new `dev`→`main` release has
  shipped in over 3 days.
- `prod` branch: still stale at v1.20.0-era commits (last change
  2026-07-20/21), unchanged from every prior report — consistent with
  `CLAUDE.md`'s note that `prod` is retired and `main` is the real deploy
  source for the Railway production environment.
- Net effect: today does not add a *new* unverified release, but the two
  already-shipped-blind releases from prior windows (v1.62.0-era on `prod`,
  v1.62.6 on `main`) are still unconfirmed as of this run.

## Errors found

Not collected this run (no data source available).

## Root-cause analysis

N/A — no logs were retrieved, so no error triage was possible this run.
Carried over from prior reports, still the highest-value places to check
once access is restored (unchanged since no newer release has shipped):

- **`apps/backend/src/services/sellableInventory.ts`** (`availableQty`
  around line 92) — PR #122's fix to per-line quantity reservation on Draft
  sell orders. Worth confirming no regression in lot availability math
  (over-reservation or negative `availableQty`) now that it's had ~4 days in
  production.
- **`apps/backend/migrations/0090_backfill_other_item_type.sql`** and the
  classification logic in `apps/backend/src/routes/inventory.ts` /
  `apps/backend/src/routes/itemTypes.ts` — PR #121's backfill of `item_type`
  for legacy `Other` inventory rows. Worth confirming the migration applied
  cleanly against prod's row count once DB access exists.
- Migrations `0088`–`0090` are still the newest in the tree — worth
  confirming the `_migration_ledger` on prod Postgres shows all three
  applied without error.

None of this is a claim that these are broken — only that they remain the
newest, most invasive changes in the recent history, so they're the first
place to look once Railway access exists.

## Recommended fixes

Not application code — this is infra/session configuration for the
inspector routine itself, and it is now a repeat item for the **thirty-first
consecutive day**:

- Add a Railway MCP server to this session's environment (or the pool it
  runs in), **or** set `RAILWAY_API_TOKEN` as an environment variable
  available to this routine's session, scoped to the
  `recycle-erp-experiment` Railway project
  (`e811d744-769f-4418-9f22-f371fd889923`).
- The outbound proxy for this session/environment still returns `403`
  (policy denial) on `CONNECT` to `backboard.railway.com:443`, confirmed
  again today via the proxy's own `/__agentproxy/status` endpoint, and
  identical to every prior day's result. Whoever owns the environment's
  egress policy needs to add `backboard.railway.com` (or `*.railway.com` /
  `*.railway.app`) to the allowed-hosts list, or this routine will keep
  failing even after a token is supplied.
- Once both the network policy and a token are in place, verify with a
  minimal query against `https://backboard.railway.com/graphql/v2` before
  relying on it for the full 24h log pull.
- **This is now the thirty-first consecutive scheduled run with zero
  visibility — more than a full calendar month.** Two production releases
  (v1.62.0-era, v1.62.6) remain unverified from prior windows. Recommend a
  manual check of the `backend` service's Railway dashboard (deployment
  status + error rate) until access is restored, and prioritizing the
  environment fix itself — at this point the inspection routine has
  provided no production-health signal for longer than the interval between
  the two unverified releases it's meant to be watching.

## Infra status

Unknown — not observed this run. No conclusions should be drawn about
`backend`, `Postgres`, `db-sync`, or `pg-r2-backup` deployment health, crash
loops, or error rates from this report. With thirty-one straight days of
zero visibility (2026-07-14 through 2026-08-13), and two production releases
(v1.62.0-era, v1.62.6) still unconfirmed from that blind window, this
remains an open risk for the inspection routine itself to flag until infra
access is fixed.
