# Daily production-health inspection — 2026-08-11

## Summary verdict

⚠️ **INSPECTION INCOMPLETE — Railway access unavailable (29th consecutive day)**

This run could not reach Railway by any of the three sanctioned paths, so no
logs, deployment statuses, or HTTP error rates were collected for `backend`,
`Postgres`, `db-sync`, or `pg-r2-backup`. This is a report about the
inspection tooling, not a verdict on application health.

This is the same failure mode as every report from
[2026-07-14](./2026-07-14.md) through [2026-08-10](./2026-08-10.md) —
twenty-eight straight prior runs, now twenty-nine with today's. Nothing
about the environment changed overnight to fix it.

Unlike yesterday's report, **no new commit landed on `main`** in this run's
lookback window (2026-08-10T16:14:13Z–2026-08-11T16:09:09Z) — `main` is
still at PR #123 / v1.62.6 from yesterday. So today's blind spot does not
stack a *new* unverified release on top; it carries forward the two already
unverified from prior windows (v1.62.0, v1.62.6).

## What was tried

1. **Railway MCP server** — `ToolSearch` with `"railway logs deployments
   http_error_rate service_metrics"`, `"deploy deployment service project
   environment"`, and bare `"railway"`. No matching tools found. Also
   checked `ListConnectors` (no filter): only Gmail, Google Calendar, Google
   Drive, Notion, two ambiguously-named connectors ("Recycle" / "recycle",
   both generic/unrelated to Railway and not enabled in this chat anyway),
   and Webull are registered for this account — no Railway connector.
2. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`)
   — checked for `RAILWAY_API_TOKEN` via `env | grep -i railway` (no
   output), a broader `find / -iname "*railway*"` (nothing), and the
   worktree's `.env` (no `RAILWAY` entries; file itself absent in the
   session's checkout). Probed the endpoint anyway:
   ```
   curl -sS -m 10 -o /dev/null -w "HTTP %{http_code}\n" \
     -X POST https://backboard.railway.com/graphql/v2 \
     -H "Content-Type: application/json" -d '{"query":"query{me{id}}"}'
   → curl: (56) CONNECT tunnel failed, response 403 (HTTP 000)
   ```
   Confirmed via the agent proxy's own status endpoint
   (`$HTTPS_PROXY/__agentproxy/status`) that the proxy is up and healthy
   (`enabled: true`, no `recentRelayFailures`) — the 403 is a deliberate
   policy denial on `backboard.railway.com`, not a proxy outage or
   transient blip. Identical to every prior day.
3. **Railway CLI / cached credentials** — not present on `PATH`; not
   pursued further since the network-policy block above would stop a CLI
   call the same way regardless of credentials.
4. **Conclusion** — neither access path is configured in this environment,
   for the twenty-ninth day running.

## Secondary signal (GitHub, not Railway — informational only)

Does **not** substitute for backend log/error-rate visibility:

- `list_commits` on `main` since 2026-08-10T16:14:13Z (end of yesterday's
  window) returns **zero** commits. `main` remains at PR #123 (v1.62.6,
  merged 2026-08-10T00:55:34Z) — no new `dev`→`main` release shipped in
  today's lookback window.
- `prod` branch: still stale at v1.20.0-era commits (last change
  2026-07-21), unchanged from every prior report — consistent with
  `CLAUDE.md`'s note that `prod` is retired and `main` is the real deploy
  source.
- Net effect: today does not add a *new* unverified release, but the two
  already-shipped-blind releases from prior windows (v1.62.0, v1.62.6) are
  still unconfirmed as of this run.

## Errors found

Not collected this run (no data source available).

## Root-cause analysis

N/A — no logs were retrieved, so no error triage was possible this run.
Carried over from the last report, still the highest-value places to check
once access is restored (unchanged since no newer release has shipped):

- **`apps/backend/src/services/sellableInventory.ts`** (`availableQty` at
  line 92) — PR #122's fix to per-line quantity reservation on Draft sell
  orders. Worth confirming no regression in lot availability math
  (over-reservation or negative `availableQty`) now that it's had ~40h in
  production.
- **`apps/backend/migrations/0090_backfill_other_item_type.sql`** and the
  classification logic in `apps/backend/src/routes/inventory.ts` /
  `apps/backend/src/routes/itemTypes.ts` — PR #121's backfill of `item_type`
  for legacy `Other` inventory rows. Worth confirming the migration applied
  cleanly against prod's row count once DB access exists.
- Migrations `0088`–`0090` are still the newest in the tree — worth
  confirming the `_migration_ledger` on prod Postgres shows all three
  applied without error.

None of this is a claim that these are broken — only that they remain the
newest, most invasive changes in the recent history, so they're the first
place to look once Railway access exists.

## Recommended fixes

Not application code — this is infra/session configuration for the
inspector routine itself, and it is now a repeat item for the
**twenty-ninth consecutive day**:

- Add a Railway MCP server to this session's environment (or the pool it
  runs in), **or** set `RAILWAY_API_TOKEN` as an environment variable
  available to this routine's session, scoped to the
  `recycle-erp-experiment` Railway project
  (`e811d744-769f-4418-9f22-f371fd889923`).
- The outbound proxy for this session/environment still returns `403`
  (policy denial) on `CONNECT` to `backboard.railway.com:443`, confirmed
  again today via direct `curl` and cross-checked against the proxy's own
  `/__agentproxy/status` (proxy itself is healthy — this is an allowed-hosts
  gap, not an outage). Whoever owns the environment's egress policy needs
  to add `backboard.railway.com` (or `*.railway.com` / `*.railway.app`) to
  the allowed-hosts list, or this routine will keep failing even after a
  token is supplied.
- Once both the network policy and a token are in place, verify with a
  minimal query against `https://backboard.railway.com/graphql/v2` before
  relying on it for the full 24h log pull.
- **This is now the twenty-ninth consecutive scheduled run with zero
  visibility.** Two production releases (v1.62.0, v1.62.6) remain
  unverified from prior windows. Recommend a manual check of the `backend`
  service's Railway dashboard (deployment status + error rate) until access
  is restored.

## Infra status

Unknown — not observed this run. No conclusions should be drawn about
`backend`, `Postgres`, `db-sync`, or `pg-r2-backup` deployment health,
crash loops, or error rates from this report. With twenty-nine straight
days of zero visibility (2026-07-14 through 2026-08-11), and two
production releases (v1.62.0, v1.62.6) still unconfirmed from that blind
window, this remains an open risk for the inspection routine itself to
flag until infra access is fixed.
