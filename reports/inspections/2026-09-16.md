# Daily production-health inspection — 2026-09-16

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (65th consecutive day), and today's blind window is unusually large.** Every path this routine can use to reach Railway (MCP tools, the GraphQL API fallback, a direct `/api/health` probe) is blocked exactly as it has been since [2026-07-14](./2026-07-14.md). Unlike several recent reports where nothing shipped in the gap, **five releases landed on production `main` since yesterday's report** — v1.141.0 through v1.145.0 — including a substantial new feature (`POST /api/orders/:id/handoff`, a new order-status-transition endpoint) and two new migrations. None of it can be independently verified as healthy in production today.

### Access check (repeated from every report since 2026-07-14)

1. **Railway MCP server tools** — `ToolSearch` for `"railway logs deployments metrics"` returned no Railway tool, only unrelated GitHub/task/Monitor tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment; no `~/.railway` credentials directory; no `railway` CLI binary on `PATH`; `ListConnectors` filtered on `"railway"` returned `[]`.
3. **Direct health-check probe** — `curl` to `https://erp.recycleservers.com/api/health`, `https://inventory.recycleservers.com/api/health`, and `https://backboard.railway.com/graphql/v2` all failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms all three as `connect_rejected` (policy denial), same as every prior day.

No Railway or production API calls were attempted with fabricated or guessed credentials.

### What's different from yesterday — the blind window grew substantially

`origin/main` (production) moved from `53334a8` (v1.140.1, unchanged as of yesterday) to **`02255c09` (v1.145.0)**. `origin/dev` is one release further ahead, at v1.145.1 (not yet on `main`). Deploy-triggering commits on `main` since the last report, in UTC:

| Commit | Time (UTC) | Version | Change |
|---|---|---|---|
| `170cc35` | 2026-09-15 17:58 | v1.141.0 – v1.143.0 (merged as a batch, PRs #327/#328/#330) | Leaderboard rank toggle (RS-051); Draft→In Transit hand-off dialog + `POST /api/orders/:id/handoff` (RS-050); PO-number inventory search (RS-052) |
| `f7ae755` | 2026-09-15 21:23 | v1.144.0 (#332) | Menu items / record references become real `<a>` links (RS-053) |
| `379271288` | 2026-09-16 03:19 | v1.144.1 (#334) | Removing a PO line no longer blocked by an archived sell order (RS-054); migration 0127 |
| `031219e` | 2026-09-16 03:29 | v1.144.2 (#335) | Phone Inventory shows sell price to purchasers (RS-055) |
| `98a85f7` / `02255c09` | 2026-09-16 05:36 – 05:50 | v1.145.0 (#336) | Phone tab bar restores Market for purchasers (RS-056) |

Two new migrations shipped to production in this window: `apps/backend/migrations/0126_po_handoff.sql` (adds `orders.source`, `handoff_method`, `handoff_by`, `payment_method`, all additive/nullable) and `0127_sell_order_lines_inventory_set_null.sql` (changes the `sell_order_lines.inventory_id` FK from no-action to `ON DELETE SET NULL`). Both are additive/backward-compatible by design per their own header comments; neither is destructive.

`origin/dev` also carries **v1.145.1** (`5f97db8`, "payments rail no longer calls `window.stop()` on every click", RS-057) which has **not** reached `main`/production yet — nothing to verify there today.

No new files were added under `docs/debug-notes/` since the [2026-09-13 report](./2026-09-13.md) (still just the PO-1370 reconciliation note).

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window.

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. Of the newly-shipped code, the highest-risk surface for future triage (should access be restored and errors surface) is the new hand-off endpoint, since it touches the order status-guard/transaction conventions this repo is strict about:

- `apps/backend/src/services/orderHandoff.ts` — new file; the Draft→In Transit hand-off runs as one `sql.begin` transaction wrapping field writes, package creation from a tracking number, and the existing `advanceOrderTx` guard chain (`orderAdvance.ts`), consistent with the repo's "multi-table writes must be atomic" convention. `HandoffRefused` is thrown inside the transaction to unwind it. No structural issue observed in the parts read; a full review was out of scope for this routine.
- `apps/backend/src/routes/orders.ts` (+247 lines) — largest diff in the window; carries both the new `/handoff` route and the archived-sell-order-line removal fix (v1.144.1). Both are status-guard-adjacent changes per `CLAUDE.md`'s "Status guards" convention — worth first attention if any order-transition errors appear once logs are reachable.
- Carried over from prior reports, still unverified:
  - `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — additive-only, lowest priority.
  - The v1.140.1 frontend pagination fix (RS-049) on desktop Purchase-orders — worth a spot-check of `/api/orders` request volume/latency once access returns.
  - `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`) — still no confirmation it behaved correctly in production.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" below) — the sole blocking issue for this routine's entire purpose, now on its tenth week and worsening in impact as unverified releases accumulate.
2. As soon as access is restored, prioritize checking the new hand-off endpoint (`POST /api/orders/:id/handoff`, migrations 0126/0127) and the archived-sell-order-line FK change over the older carried-over items — this is the largest single batch of order-transition logic to ship blind since this routine's access outage began.
3. PO-1370 (flagged in the [2026-09-13 report](./2026-09-13.md)) remains an open, not-yet-resolved manual-data task for a human to action — repeating the flag since it has not been reported as closed.
4. No application code changes are recommended by this run — this routine diagnoses, it does not fix, and no error evidence was available to diagnose independently.

### How to fix the access gap (unchanged ask, now open 65 reports / over 9 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly.
- **GitHub / release state:** `origin/main` (production) advanced from **v1.140.1** (`53334a8`) to **v1.145.0** (`02255c09`) — five releases (v1.141.0–v1.145.0) across PRs #327, #328, #330, #332, #334, #335, #336. `origin/dev` is one release ahead at **v1.145.1** (`5f97db8`, #338), not yet promoted to `main`.
- **Frontend (Cloudflare Worker):** out of scope per task instructions, but note the `main`→prod pushes above each auto-trigger `.github/workflows/deploy-frontend.yml`; no independent confirmation that any of those Worker deploys succeeded.
