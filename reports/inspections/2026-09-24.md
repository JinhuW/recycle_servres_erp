# Daily production-health inspection — 2026-09-24

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (73rd consecutive day).** Every path this routine can use to reach Railway (MCP tools, the GraphQL API fallback, a direct `/api/health` probe) is blocked exactly as it has been since [2026-07-14](./2026-07-14.md). Since yesterday's report, **production `main` has not moved** — `origin/main` is still at `a08d5d1` / v1.172.2, unchanged for three days running now. Meanwhile `origin/dev` has advanced to v1.175.0 with three more merged tickets, growing the backlog of code that will ship unverified whenever the next `dev`→`main` release happens.

### Access check (repeated from every report since 2026-07-14)

1. **Railway MCP server tools** — `ToolSearch` for `"railway logs deployments error rate service metrics"` and a bare `"railway"` query returned no Railway tool (only unrelated `mcp__github__*`, `Monitor`, `ArtifactComments`/`ArtifactData`, `ExitWorktree`, `ReadMcpResourceDirTool`, `SendMessage`, `TaskUpdate`, `WebFetch`, `mcp__Claude_Code_Remote__*` matches). `ListConnectors` with keyword `"railway"` returned an empty list.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment and absent from the worktree's `.env` (checked directly: no `RAILWAY`-prefixed or other `*TOKEN*` env var resolves to it — only `GH_TOKEN`, `GITHUB_TOKEN`, `CLOUDSDK_AUTH_ACCESS_TOKEN`, and two Claude-Code-internal tokens are present).
3. **Direct probe** — `curl` to `https://backboard.railway.com/graphql/v2` and `https://erp.recycleservers.com/api/health` both failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms both as `connect_rejected` ("gateway answered 403 to CONNECT — policy denial or upstream failure") at 2026-09-24T16:12:43.958Z / 16:12:44.213Z — same signature as every prior day.

No Railway or production API calls were attempted with fabricated or guessed credentials.

### What shipped to production since yesterday's report

Nothing. `origin/main`'s `package.json` is unchanged at **1.172.2**, and its HEAD (`a08d5d1`, "Merge pull request #394 from JinhuW/dev") is identical to the last three reports.

`origin/dev`, however, moved forward three tickets since the last check, now at **1.175.0** (`ed09c09`):

- **RS-099** (`44cbbe2`, v1.174.0) — a sold PO and its sell orders now link to each other. Touches `apps/backend/src/routes/orders.ts` / `apps/backend/src/routes/sellOrders.ts`.
- **RS-101** (`0d8b034`, v1.174.1) — fixes the inventory lot-picker keeping stale selections under an earlier search term. Frontend-only (`DesktopInventory.tsx`, `inventorySelection.ts`).
- **RS-100** (`f5440c5`, v1.175.0) — adds an Inventory selection flow to an existing sell order, the largest of the three (new `SellOrderPickerDialog.tsx`, `openSellOrders.ts`, changes to `StagePanel.tsx`, `OrderDetail.tsx`, `DesktopSellOrders.tsx`).

None of this is in production yet — it only widens the eventual unverified-change surface once `dev`→`main` merges, on top of the existing backlog below.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window.

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. The standing unverified-change backlog carried from prior reports is unchanged (all still live in production since 2026-09-21, still unconfirmed against real logs):

- `apps/backend/src/routes/orders.ts` / `apps/backend/src/services/orderHandoff.ts` — the largest unverified change in this routine's history (RS-080–RS-094). Top priority once logs are reachable: watch for 4xx/5xx on `/api/orders/:id/handoff`, `/api/orders/:id/package`, and PATCH order-status endpoints since 2026-09-20 05:15 UTC-4.
- `apps/backend/migrations/0130_po_sold_lifecycle.sql` — the one data-mutating migration in that window; confirm it ran cleanly on the prod DB.
- `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — additive-only, lowest priority, carried for completeness.
- `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`) — still no confirmation it behaved correctly in production.
- RS-063's 3.5-hour live guard-bug window (2026-09-18 01:49–05:20 UTC, fixed at v1.149.4) — whether any of the 43 legacy `$0` POs were actually hit remains unconfirmed.

New from today: once RS-099/RS-100/RS-101 reach `main`, add their endpoints/components to this list — particularly RS-099's linkage between sold POs and sell orders (a two-sided write worth checking for partial-failure states) and RS-100's new `SellOrderPickerDialog` inventory-selection path.

## Recommended fixes

1. **Restore Railway access for this routine** — the standing blocker, now 73 days running with zero mitigation attempted (no token added, no connector enabled, no proxy allow-rule added). Either provision `RAILWAY_API_TOKEN` in this session's environment, install/enable a Railway MCP connector, or add an explicit allow-rule for `backboard.railway.com` / `erp.recycleservers.com` in the agent-proxy policy. Until one of these lands, every daily report from this routine is code-review-only and cannot see actual production errors, deployment health, or HTTP error rates. Given the length of this outage, this is worth escalating directly rather than waiting for it to self-resolve.
2. Once access is restored, prioritize the full 2026-09-20–2026-09-24 window against the endpoints listed under Root-cause analysis, since that is the highest-risk unverified surface accumulated across the last several reports.
3. Before the next `dev`→`main` merge, give RS-099's PO/sell-order linkage and RS-100's inventory-selection dialog a closer look (or at least flag them for extra post-deploy log attention) given their size relative to the rest of the current `dev` branch.

## Infra status

- **Deployments**: unknown — no Railway access (see above).
- **HTTP error rate (`backend`)**: unknown — no Railway access.
- **Anomalies**: none observable from this routine's available data. `origin/main` is unchanged at v1.172.2 for a third consecutive day. `origin/dev` has diverged further (now v1.175.0, three tickets ahead) — nothing broken, but the gap between what's merged and what's deployed keeps growing.
