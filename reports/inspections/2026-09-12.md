# Daily production-health inspection — 2026-09-12

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (61st consecutive day), and for the first time in that streak, a real production release landed in the blind window: `main` advanced from v1.132.0 to v1.137.1 (PR #301, merged 2026-09-11T20:25 UTC), carrying the archive-takes-stock-out feature (migrations 0121–0123) that the previous report flagged as the highest-value thing to check on first deploy.**

Every path specified in this routine's task was checked again today and found unavailable, unchanged from every report since [2026-07-14](./2026-07-14.md):

1. **Railway MCP server tools** — `ToolSearch` for `"railway get_logs list_deployments http_error_rate service_metrics"` and for `"railway"` alone returned no Railway tool, only unrelated GitHub / task / connector tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment (`env | grep -i railway` empty) and no `.env` file exists anywhere in this worktree or the main checkout (only the two `.env.example` templates checked into the repo).
3. **Direct health-check probe** as a partial substitute — `curl` to `https://erp.recycleservers.com/api/health`, `https://inventory.recycleservers.com/api/health`, and `https://backboard.railway.com/graphql/v2` all failed identically: `CONNECT tunnel failed, response 403`. The agent proxy status endpoint confirms all three hosts under `recentRelayFailures` as `connect_rejected` (`gateway answered 403 to CONNECT`); `noProxy` still covers only package registries and the Anthropic API.
4. `ListConnectors` filtered on `"railway"` returned an empty list.
5. No `railway` CLI binary is present (`which railway` empty) and no `~/.railway` credentials directory exists.

No Railway or production API calls were attempted with fabricated or guessed credentials.

**What's different from yesterday — an actual production deploy happened:**

`origin/main` moved **d9db206 → b5d7a29** via PR #301 ("release: v1.133.0 – v1.137.1 to production"), merged 2026-09-11T20:25:45Z, after yesterday's inspection had already run. This brought six releases onto production at once:

- **v1.133.0** (#289, RS-033) — payment link picker/direct-link naming.
- **v1.134.0** (#291, RS-034) — desktop PO pages hold together in a narrow window.
- **v1.135.0** (#293, RS-035) — assign the sell-order payment receiver in any status.
- **v1.136.0** (#294, RS-036) — a note on a payment.
- **v1.137.0** (#297, RS-037) — **archiving a PO now takes its goods out of stock**: every non-Sold line on an archived PO moves to a new `Archived` status; unarchive restores the prior status from the line's audit trail; archiving is refused (`committedLines`) if an open sell order still names one of the PO's lines. Migration `0121` (`apps/backend/migrations/0121_archived_po_lines.sql`) backfills already-archived POs' lines the same way.
- **v1.137.1** (#299, RS-038) — a same-day, pre-prod-cut review pass on the v1.133–v1.137 diff found **ten issues**, mostly in the archive work, and fixed them before this release cut: a purchaser-vs-manager privilege gap in the archive/removeFromSellOrders conflict path (`routes/orders.ts`), an inventory PATCH guard that keyed off line status instead of the parent order, letting a `Sold` line on an archived PO re-enter stock (`routes/inventory.ts`), missing Archived-line exclusion in analysis aggregates (`lib/sellCommitment.ts`), migration `0121` itself stranding lines that were mid-transfer at `Archived` instead of `In Transit` (fixed by new migration `0122_archived_lines_on_pending_transfer.sql`), and a bank-txn note not copying across a paired transaction (new migration `0123_bank_txn_note_across_pair.sql`, `banktx/sync.ts`).

All three CI runs for the PR #301 merge (`version-check` run 551, `backend-tests` run 113, `deploy-frontend` run 214) completed with `conclusion: success`. `origin/dev` has since advanced two more releases (v1.138.0 — PO-list-links-to-payments, #303; v1.138.1 — payments row button-affordance fix, #302) that are **not yet on `main`**.

**Why this matters for this routine specifically:** this is exactly the deploy the 2026-09-11 report called out as worth a first-run log check — three new migrations (`0121`, `0122`, `0123`) ran against the production database, and a stock-visibility rule (archived lines dropping out of every stock/sellable view) is now live for real purchase orders. This routine has **no way to confirm** the migrations applied cleanly, that backfill row counts looked sane, or that no `committedLines`-rejection spike or 500 followed the deploy — because Railway access is still down. That gap is no longer hypothetical; it now covers an actual, non-trivial schema and behavior change that already shipped.

## Access failure — details

See "Summary verdict" above; this is the 61st run in a row unable to reach Railway by any sanctioned path. The blocking conditions are unchanged from the [2026-09-11 report](./2026-09-11.md).

### How to fix (unchanged ask, now open 61 reports / ~8.7 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

Given that a real production release with new migrations landed during this blind window, closing this gap has moved from a standing housekeeping ask to something with a concrete missed opportunity attached: today's deploy is the first real test of whether this routine can add value.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window, including for the v1.133.0–v1.137.1 deploy window itself.

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. For context on what to check once access is restored, see the migration and guard changes cited above (`apps/backend/migrations/0121_archived_po_lines.sql`, `0122_archived_lines_on_pending_transfer.sql`, `0123_bank_txn_note_across_pair.sql`; `apps/backend/src/routes/inventory.ts`, `routes/orders.ts`, `services/orderAdvance.ts`, `lib/sellCommitment.ts`, `banktx/sync.ts`).

## Recommended fixes

1. Close the Railway access gap (see "How to fix" above) — this remains the sole blocking issue for this routine's entire purpose, and today's deploy makes the cost of the gap concrete rather than theoretical.
2. As soon as access is restored, prioritize checking the production database and logs for the effects of the v1.133.0–v1.137.1 deploy (merged 2026-09-11T20:25 UTC): confirm migrations `0121`, `0122`, and `0123` applied without error, spot-check backfilled `Archived` line counts and the `inventory_events` audit rows they wrote, and look for any `committedLines`-rejection spike, 500s from the inventory PATCH guard, or bank-sync errors in the hours following the deploy.
3. No application code changes are recommended by this run — nothing was observed to diagnose independently, and the archive feature already went through an explicit pre-prod review pass (#299) that fixed ten issues before this release cut.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly.
- **GitHub / release state:** `origin/main` (production) advanced from **v1.132.0 to v1.137.1** via PR #301 (merged 2026-09-11T20:25:45Z) — a real production deploy in this window, carrying six releases including the archive-takes-stock-out feature and its pre-prod review fixes. All CI runs for the merge (`version-check` run 551, `backend-tests` run 113, `deploy-frontend` run 214) completed with `conclusion: success`; none stuck or anomalous. `origin/dev` has since advanced further to **v1.138.1** (PRs #303, #302), not yet merged to `main`.
- **Frontend (Cloudflare Worker):** out of scope per task instructions; `deploy-frontend` ran successfully on both the `dev` pushes and the `main` release push — no anomaly to report.
