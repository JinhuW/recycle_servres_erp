# Daily production-health inspection — 2026-09-13

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (62nd consecutive day). A second production release landed in the blind window since yesterday's report: `main` advanced from v1.137.1 to v1.138.5 (PRs #314 and #317, merged 2026-09-13T09:51:54Z and 15:09:01Z UTC).** Unlike the previous deploy, this one is lower-risk on paper — CI is green on both merges and the one new migration was pre-verified as a prod no-op — but this routine still has no independent way to confirm any of that against the live system.

Every path specified in this routine's task was checked again today and found unavailable, unchanged from every report since [2026-07-14](./2026-07-14.md):

1. **Railway MCP server tools** — `ToolSearch` for `"railway get_logs list_deployments http_error_rate service_metrics"` and for `"railway"` alone returned no Railway tool, only unrelated GitHub / task / connector / Monitor tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment (`env | grep -i railway` empty) and no `.env` file exists anywhere in this worktree (only the checked-in `.env.example` templates).
3. **Direct health-check probe** as a partial substitute — `curl` to `https://erp.recycleservers.com/api/health`, `https://inventory.recycleservers.com/api/health`, and `https://backboard.railway.com/graphql/v2` all failed identically: `CONNECT tunnel failed, response 403`. The agent proxy status endpoint confirms all three hosts under `recentRelayFailures` as `connect_rejected` (`gateway answered 403 to CONNECT`); `noProxy` still covers only package registries and the Anthropic API, not either production host or Railway's API.
4. `ListConnectors` (all, and filtered on `"railway"`) returned no Railway connector — only Gmail, Google Calendar, Google Drive, Notion, two `recycle`-named connectors (one `connected` but `enabledInChat: false`, one `needs_reconnect`), and Webull, none of which expose Railway data.
5. No `railway` CLI binary is present (`which railway` empty) and no `~/.railway` credentials directory exists.

No Railway or production API calls were attempted with fabricated or guessed credentials.

**What's different from yesterday — a second production deploy happened, and this time CI/process evidence is genuinely reassuring:**

`origin/main` moved **b5d7a29 → 586c6f8** via two merges after yesterday's inspection ran:

- **PR #314** (v1.138.0 – v1.138.4, merged 2026-09-13T09:51:54Z) — carries RS-039 (PO list links to its payments), RS-040 (payment detail actions), RS-041 (archived PO items never show in inventory), RS-042 (launcher bypass flag), RS-043 (vendor price import records market values), and RS-044 (a same-day dev→main review pass, #312, fixing issues found in the batch before the prod cut). The PR body states the pre-release checks explicitly: dev backend health reported 1.138.4, and **migration `0124`'s prod scope was verified read-only before merge — 3 archived POs, 0 target lines, 0 on any open sell order — a no-op in prod**, recorded in ticket RS-044. Backend 1572 / frontend 397 tests green on #312.
- **PR #317** (v1.138.5, merged 2026-09-13T15:09:01Z) — a single prod-only bugfix: a Draft sell order was blocking a PO's move back from "Ready to Pay" to "Reviewing" (`apps/backend/src/routes/orders.ts` guard treated Draft the same as Shipped/Awaiting-payment for the backward-move check). Fixed to key the sell-status set by landing status. No migration, no prod data change, per the PR body.

Migration `0124_archived_po_lines_on_open_sell_orders.sql` (the only schema change in this window) extends the archive-takes-stock-out feature that shipped in yesterday's window: it releases any line an open sell order still names when its parent PO is archived (dropping it from the sell order with a `line_removed` audit event, resetting the sell order's negotiated total) and finishes moving the line to `Archived` status — closing a gap `0121`/`0122` deliberately left open. Its idempotency and the "0 target lines in prod" pre-check (both stated in the PR, not independently verified by this routine) make it the lowest-risk migration of the two release windows this routine has seen.

All CI runs on both merge commits completed `conclusion: success`: `version-check` (runs 578, 586), `backend-tests` (run 127 — attempt 1 was `cancelled`, attempt 2 `success`; run 132 — single attempt `success`), `deploy-frontend` (runs 219, 221). The Railway/db-sync commit statuses recorded on the PR #314 head (`45e2c97`) also read `success`/"No deployment needed - watched paths not modified" for `backend` and `uptime-monitor`, and `success` for `db-sync` — the only Railway-sourced signal available to this routine all run, and it is secondhand (a GitHub commit status written by Railway's integration, not a log or metric pulled directly).

Separately, a historical debug note was committed today (not a new incident): [`docs/debug-notes/2026-08-06-denver-count-reconciliation-po-1370-overlap.md`](../../docs/debug-notes/2026-08-06-denver-count-reconciliation-po-1370-overlap.md), written 2026-08-06 during a manual production inventory-count reconciliation and only just found and committed during a 2026-09-13 sweep of unmerged work. It documents a one-time manual data fix (PO-1371 added, 12 parts retargeted, 4 lines retired) and a still-open trap (draft PO-1370 needs 5 of its 13 lines dropped before it can be advanced, or Denver RAM stock overcounts by 34 units). This is over a month old and outside this routine's 24-hour error window, but is noted here since it describes an **open, not-yet-resolved** data-integrity task that a human should still action on PO-1370.

## Access failure — details

See "Summary verdict" above; this is the 62nd run in a row unable to reach Railway by any sanctioned path. The blocking conditions are unchanged from the [2026-09-12 report](./2026-09-12.md).

### How to fix (unchanged ask, now open 62 reports / ~8.9 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

Two production releases have now landed in this blind window in three days (v1.133.0–v1.137.1 on 2026-09-11, v1.138.0–v1.138.5 on 2026-09-13), each carrying a schema migration touching the same archive/stock-status feature area. The process evidence (pre-verified migration scope, green CI, passing Railway commit statuses) is reassuring both times, but it is all secondhand — this routine still cannot independently confirm a migration applied cleanly, that no error-rate spike followed a deploy, or that the four services are healthy right now.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window, including for the v1.138.0–v1.138.5 deploy window itself.

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. For context on what to check once access is restored:

- `apps/backend/migrations/0124_archived_po_lines_on_open_sell_orders.sql` — the new migration; confirm it ran and its target-row counts matched the pre-merge prod check (0 target lines expected).
- `apps/backend/src/routes/orders.ts` — the backward-move guard fixed in v1.138.5 (RS-045); confirm no 409 regression on Reviewing→In-Transit/Draft transitions for POs with genuinely committed (non-Draft) sell orders.
- `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`) — the code path `0124` mirrors; worth a log check for `committedLines` rejections or `line_removed` event volume in the hours after each deploy.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" above) — this remains the sole blocking issue for this routine's entire purpose, and two production deploys in three days without any independent verification capability makes the gap's cost recurring, not hypothetical.
2. As soon as access is restored, check production logs/metrics across both blind-window deploys (2026-09-11T20:25 UTC and 2026-09-13T09:51/15:09 UTC) for any `committedLines` rejections, 500s from the order-status guards, or anomalies following the archive/sell-order-line migrations (`0121`–`0124`).
3. Flag PO-1370 (see "What's different" above) to a human for the still-open manual line-drop it needs before it can be safely advanced — not a code bug, but an open operational task surfaced by today's commit.
4. No application code changes are recommended by this run — nothing was observed to diagnose independently, and both releases in this window went through explicit pre-merge review/verification steps recorded in their PR bodies and tickets.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly. The only Railway-sourced signal seen was secondhand: GitHub commit statuses from Railway's integration on the PR #314 head, all `success`.
- **GitHub / release state:** `origin/main` (production) advanced from **v1.137.1 to v1.138.5** via PR #314 (merged 2026-09-13T09:51:54Z) and PR #317 (merged 2026-09-13T15:09:01Z) — two real production deploys in this window. All CI runs on both merge commits (`version-check` runs 578/586, `backend-tests` runs 127/132, `deploy-frontend` runs 219/221) completed with `conclusion: success`; the one `cancelled` attempt (`backend-tests` run 127, attempt 1) was superseded by a successful attempt 2 on the same commit, not a failure. `origin/dev` has since advanced further to **v1.140.0** (PRs #320, #321, #322, #323 — a Group-with picker fix, RAM rank catalog coverage, and a new Facebook fleet-tracker coordinator feature), not yet merged to `main`.
- **Frontend (Cloudflare Worker):** out of scope per task instructions; `deploy-frontend` ran successfully on both `main` release pushes today — no anomaly to report.
