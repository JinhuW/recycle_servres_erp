# Daily production-health inspection — 2026-08-17

## Summary verdict

⚠️ **INSPECTION INCOMPLETE — Railway access unavailable (35th consecutive day) — a new production release shipped yesterday, unverified**

This run could not reach Railway by any of the three sanctioned paths, so no
logs, deployment statuses, or HTTP error rates were collected for `backend`,
`Postgres`, `db-sync`, or `pg-r2-backup`. This is a report about the
inspection tooling, not a verdict on application health.

This is the same failure mode as every report from
[2026-07-14](./2026-07-14.md) through [2026-08-16](./2026-08-16.md) —
thirty-four straight prior runs, now thirty-five with today's.

**What's different today:** `origin/main` moved for the first time since the
[2026-08-15](./2026-08-15.md) report — `eaaa89f` (v1.65.0) → `41de37d`
(v1.67.0), merged 2026-08-16 16:35 -0400, PRs #129–#133. That release
includes a security-relevant OAuth scope change (role ceiling on interactive
grants) that this routine has zero log visibility to confirm behaves as
intended in prod. `origin/dev` has moved further still, to v1.69.0 (PRs
#134–#135), adding a new external shipping-label integration (ShipSaving)
that is **not yet in `main`** — flagged here so it's on the radar before it
ships into a blackout that already has one unconfirmed release sitting in it.

## What was tried

1. **Railway MCP server** — `ToolSearch` with `"railway logs deployments
   http_error_rate service_metrics"` and bare `"railway"`. No matching tools
   found in either query (same as every prior day).
2. **Connector check** — `ListConnectors(keywords=["railway"])` returns an
   empty list. No Railway connector registered for this account/session.
3. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`)
   — swept environment variables for anything token/key-shaped
   (`printenv | grep -iE "token|api_key|key"`): only unrelated tokens
   (GitHub, GCloud, AWS, Claude session/messaging) — nothing Railway-scoped.
   Checked for a `.env` file at the repo root — absent (only `.env.example`,
   which references an unrelated `TRACKER_API_URL` placeholder). Probed the
   endpoint anyway:
   ```
   curl -sS -o /dev/null -w "http_code:%{http_code}\n" \
     https://backboard.railway.com/graphql/v2 --max-time 15
   → curl: (56) CONNECT tunnel failed, response 403
   → http_code:000
   ```
   Identical signature to every prior day — a policy-level `CONNECT` denial
   on `backboard.railway.com`, not a transient network error.
4. **Railway CLI** — not present on `PATH` (`which railway` → exit 1); not
   pursued further since the network-policy block would stop a CLI call the
   same way regardless of credentials.
5. **Conclusion** — neither access path is configured in this environment,
   for the thirty-fifth day running.

## Secondary signal (GitHub, not Railway — informational only)

Does **not** substitute for backend log/error-rate visibility, but is the
only thing this run could observe:

- `origin/main` advanced from `eaaa89f` (2026-08-14) to `41de37d`
  (2026-08-16 16:35 -0400) — PRs **#129, #130, #131, #132, #133**.
  `package.json` moved 1.65.0 → **1.67.0**. This is the first production
  movement since the [2026-08-14](./2026-08-14.md) report; the blackout has
  now swallowed a full release.
- `origin/dev` is further ahead again, at `036c2b3` / **v1.69.0** (PRs
  #134, #135) — not yet merged to `main`, so not yet in production. Flagged
  below as an upcoming risk item, not a current one.
- The `prod` branch itself is stale at v1.20.0 and unrelated to current
  deploys — confirmed via `chore/prod-tracks-main` (`3ea424a`, "retire the
  prod branch"), which **is** an ancestor of `origin/main`. This matches
  CLAUDE.md's note that `prod` was retired 2026-07-20 and the Railway
  production environment now tracks `main` directly. Noted only because this
  routine's own task brief still describes "the production Railway
  environment deploys from the `prod` git branch" — that line is stale and
  should be corrected in the routine's configuration so a future reader
  doesn't go looking at the wrong branch.

## Errors found

Not collected this run (no data source available).

## Root-cause analysis

N/A — no logs were retrieved, so no error triage was possible this run.
Highest-value places to check once access is restored, in priority order:

- **New since yesterday, first full day in prod today —**
  `apps/backend/src/oauth/tokens.ts` (`restrictScopesToRole`, ~line 103) and
  `apps/backend/src/oauth/server.ts` (`/authorize` and `/authorize/consent`
  handlers) — PR #131 renamed `dropWriteUnlessManager` →
  `restrictScopesToRole` and tightened the non-manager ceiling from
  "any scope except `:write`" to "`market:read` only," dropping
  `sellorder:read` for non-managers too. Worth confirming no non-manager
  connector session got an unexpected `invalid_scope` / scope-narrowing
  surprise on its next refresh rotation, and that no elevated error rate
  shows up on `/oauth/authorize` or `/oauth/token`.
  `apps/backend/src/routes/market.ts` (PR #132, new sort-key whitelist for
  the desktop table's clickable column headers) is lower risk — pure
  whitelist addition, same fallback (`updated_at DESC`) as before.
- **Upcoming, not yet in prod —** `apps/backend/src/shipping/index.ts`
  (`pickShippingClient`) on `dev`/PR #134: the ShipSaving integration falls
  back to a stub provider whenever `SHIPSAVING_API_TOKEN` /
  `SHIPSAVING_API_URL` are unset, logging a warning rather than refusing to
  boot — the same silent-fallback shape CLAUDE.md already flags for the OCR
  provider. When #134/#135 merge to `main`, this is worth a deliberate check
  (not just log-watching) that both vars are set in the `backend` Railway
  service before real customers start clicking "buy label," or it'll quietly
  hand out demo labels. New migrations `0091`–`0093` also land with it.
- Carried over from prior reports, still unconfirmed (one day older each,
  no new signal on any of these):
  - **`apps/backend/src/routes/tracker.ts`** (PR #125/#127) — now 3 days in
    production; confirm `TRACKER_API_URL`/`TRACKER_API_TOKEN` are set if the
    tracker feature is meant to be live, and check for `502`/`501` volume
    from `/api/tracker/*` if they aren't.
  - **`apps/backend/src/services/sellableInventory.ts`** (`availableQty`
    around line 92, PR #122) — ~8 days in production, still unconfirmed.
  - **`apps/backend/migrations/0090_backfill_other_item_type.sql`** /
    `routes/inventory.ts` / `itemTypes.ts` (PR #121) — still unconfirmed
    against prod's row count.
  - Migrations `0088`–`0090` — worth confirming `_migration_ledger` on prod
    Postgres shows all applied cleanly.

None of this is a claim that anything is broken — only that the backlog of
unconfirmed production changes grew by one real release today, with no new
signal available to resolve or escalate any individual item.

## Recommended fixes

Not application code — this is infra/session configuration for the
inspector routine itself, now a repeat item for the **thirty-fifth
consecutive day**:

- Add a Railway MCP server to this session's environment (or the pool it
  runs in), **or** set `RAILWAY_API_TOKEN` as an environment variable
  available to this routine's session, scoped to the
  `recycle-erp-experiment` Railway project
  (`e811d744-769f-4418-9f22-f371fd889923`).
- The outbound proxy still returns `403` (policy denial) on `CONNECT` to
  `backboard.railway.com:443`, confirmed again today, identical to every
  prior day. Whoever owns the environment's egress policy needs to allow
  `backboard.railway.com` (or `*.railway.com` / `*.railway.app`), or this
  routine keeps failing even after a token is supplied.
- Separately: correct this routine's own task brief, which still states
  production deploys from the `prod` branch — `prod` has been retired since
  2026-07-20 and production now tracks `main` (see Secondary signal above).
  Low priority next to the access blackout, but it will mislead whoever next
  edits this routine's instructions.
- **This is now the thirty-fifth consecutive scheduled run with zero
  visibility — five weeks — spanning four production releases (v1.62.0-era,
  v1.62.6, v1.65.0, and now v1.67.0) that remain unverified against real
  logs or error rates, with a fifth (v1.69.0, adding a new external shipping
  integration) queued behind it on `dev`.** The inspection routine has now
  gone longer without signal than the interval between most of the releases
  it exists to watch. Fixing the environment access path remains the single
  highest-leverage action available — until then, each day's report can
  only restate and extend the same gap.

## Infra status

Unknown — not observed this run. No conclusions should be drawn about
`backend`, `Postgres`, `db-sync`, or `pg-r2-backup` deployment health, crash
loops, or error rates from this report. With thirty-five straight days of
zero visibility (2026-07-14 through 2026-08-17) and now four production
releases unconfirmed, this remains an open risk for the inspection routine
itself to flag until infra access is fixed.
