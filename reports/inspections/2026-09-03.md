# Daily production-health inspection — 2026-09-03

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (52nd consecutive day), and a full production release shipped yesterday that this routine cannot verify.**

This run could reach Railway by neither sanctioned path, continuing the
streak documented in every report from
[2026-07-14](./2026-07-14.md) through [2026-09-02](./2026-09-02.md) — 51
straight prior runs, now 52 with today's. Production health (backend,
Postgres, db-sync, pg-r2-backup) remains **unverified**, as it has been for
over seven weeks.

**What changed since yesterday:** unlike the last several reports, this is
not a quiet day on the code side. `dev` → `main` merged (PR #246, commit
`a1e9a67`, 2026-09-02 16:38 UTC) carrying `main` from **v1.114.1 to
v1.119.1** — five releases (#228, #233, #235, #236, #241, #242, #244) that
had accumulated on `dev` since the last production cut mentioned in the
[2026-09-01 report](./2026-09-01.md). Per this repo's deploy model, a
`dev`→`main` merge is a full Railway production release. Two new migrations
ship in it (`0115_po_company_txn_cutoff.sql`,
`0116_internal_transactions.sql`) that run automatically on backend
container startup. **This routine has no way to confirm the deploy
succeeded, the migrations applied cleanly, or the running container is
serving without errors** — the same access gap that has blocked every
report since July 14 now sits directly in front of the first real
production release in weeks.

## Access failure — details

Both access paths specified for this task were checked and found
unavailable in this session, same as every prior run since 2026-07-14:

1. **Railway MCP server tools** (`get_logs`, `list_deployments`,
   `http_error_rate`, `service_metrics`, etc.) — `ToolSearch` found no
   Railway-related MCP tools registered in this session (searched both
   `"railway"` and the specific tool names).
2. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`
   via `curl` with `RAILWAY_API_TOKEN`) — `RAILWAY_API_TOKEN` is not set in
   this session's environment (`env | grep -i TOKEN` shows no Railway
   token), not present in the worktree's `.env`, and not referenced in
   `.env.example`.
3. **New this run — direct health-check probe as a partial substitute**: a
   plain `curl` to the production API's `/api/health` (both
   `erp.recycleservers.com` and `inventory.recycleservers.com`) was
   attempted as a best-effort way to at least confirm the release deployed
   and read its reported version/commit. Both were blocked by this
   session's egress proxy (`CONNECT tunnel failed, response 403 —
   connect_rejected: organization policy`), so even that fallback is
   closed. No connector (`ListConnectors`) exposes Railway or the
   production API either.

No Railway or production API calls were attempted with fabricated or
guessed credentials.

### How to fix (unchanged ask, now open 52 reports / 7+ weeks)

- Register a Railway MCP server for this scheduled session's environment
  (exposing `get_logs`, `list_deployments`, `http_error_rate`,
  `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a
  Railway **project token** or **team API token** scoped to
  `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`)
  with read access to deployments, logs, and metrics for `backend`,
  `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach
  `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health`
  can be polled as a cheap liveness/version signal even without full log
  access.

This has been the same recommendation since the first report; it has not
been actioned in 52 daily runs. Today raises the stakes on it: a five-release
production deploy landed with schema migrations, and there is currently zero
automated visibility into whether it is healthy. If Railway visibility is
not actually a priority, consider disabling this routine rather than letting
it accumulate reports that don't get read — but if it stays enabled, today's
release is the strongest case yet for closing this gap.

## Errors found

Not collected — see access failure above.

## Root-cause analysis

Not applicable — no error data was available to analyze. The only
code-level finding this run is descriptive, not diagnostic: the release
merged to `main` (v1.115.0–v1.119.1) touches purchase-order/payment linking
and shipping-package validation, areas identified by exact paths for
reference if a future run does regain Railway access and needs to correlate
errors against this deploy:

- `apps/backend/src/routes/packages.ts` — package delivery now requires and
  propagates a PayPal transaction ID onto the minted PO (v1.116.0/v1.118.0).
- `apps/backend/src/banktx/sync.ts` — `autoLink()` and the shared claim
  function now also handle human-typed transaction IDs, not just the
  6-hourly sync (v1.118.0).
- `apps/backend/migrations/0115_po_company_txn_cutoff.sql`,
  `apps/backend/migrations/0116_internal_transactions.sql` — new schema
  landed in this release; worth checking `schema_migrations` on the
  production DB once access exists to confirm both applied.
- Payments read-path changes for half-linked pairs (v1.119.1 fix) — the
  CHANGELOG entry describes a prior 500 on assigning/unlinking a payment
  whose two legs were linked asymmetrically; if that pattern recurs
  post-deploy it would appear as `owner assignment` or `unlink` errors on
  the payments routes.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" above) — this is the
   blocking issue for every item below and for this routine's entire
   purpose.
2. Once access is restored, as a first check: confirm `backend`'s current
   deployment is running commit `a1e9a67` / v1.119.1 (not a stale
   v1.114.1 container), and confirm both new migrations recorded in
   `schema_migrations`.
3. No application code changes are recommended by this run — nothing was
   observed to diagnose.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown — no
  deployment status, log, or error-rate data could be retrieved.
- **GitHub / release state:** `origin/main` advanced today to `a1e9a67`
  (v1.119.1, merge of PR #246) — a real production release, first since the
  period covered by the [2026-09-01 report](./2026-09-01.md). `origin/dev`
  is at the same commit (`02d8d00`) as `main`'s merge base, i.e. `dev` and
  `main` are currently in sync with nothing outstanding to release.
- **Frontend (Cloudflare Worker):** out of scope per task instructions;
  not checked.
