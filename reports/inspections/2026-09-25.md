# Daily production-health inspection — 2026-09-25

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (74th consecutive day).** Every path this routine can use to reach Railway (MCP tools, the GraphQL API fallback, a direct `/api/health` probe) is blocked exactly as it has been since [2026-07-14](./2026-07-14.md), so this report is code-review-only again: no real production error, deployment, or HTTP-error-rate data.

The one substantive change since yesterday: **`main` caught up to `dev`.** Three releases landed since yesterday's report (PRs #399, #402, #407 — the last only ~1.5 hours before this run, at 2026-09-25 10:48 -0400), taking production from v1.172.2 to **v1.177.3**. This clears the four-day-old unverified-backlog concern from prior reports, but it also means the RS-106/RS-107 manager-only-data-exposure fix, the new Mercury IO card-sync integration (RS-103/RS-104), and four other tickets are now live in production with zero log visibility to confirm they behave as intended.

### Access check (repeated from every report since 2026-07-14)

1. **Railway MCP server tools** — `ToolSearch` for `"railway logs deployments metrics error rate"` and a bare `"railway"` query returned no Railway tool (only unrelated matches: `mcp__github__get_job_logs`, `Monitor`, `ArtifactComments`, `ExitWorktree`, `ReadMcpResourceDirTool`, `SendMessage`, `TaskUpdate`, `mcp__Claude_Code_Remote__*`). `ListConnectors` with keyword `"railway"` returned an empty list.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment (no `RAILWAY`-prefixed env var; the only `*TOKEN*` vars present are `CLAUDE_CODE_MESSAGING_TOKEN`, `CLAUDE_SESSION_INGRESS_TOKEN_FILE`, `CLOUDSDK_AUTH_ACCESS_TOKEN`, `GH_TOKEN`, `GITHUB_TOKEN`). No repo-root `.env` file exists in this checkout to fall back to.
3. **Direct probe** — `curl` to `https://backboard.railway.com/graphql/v2` and `https://erp.recycleservers.com/api/health` both failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms both as `connect_rejected` ("gateway answered 403 to CONNECT — policy denial or upstream failure") at 2026-09-25T16:12:17.765Z / 16:13:21.999Z — same signature as every prior day.

No Railway or production API calls were attempted with fabricated or guessed credentials.

### What shipped to production since yesterday's report

`origin/main` moved from `a08d5d1` (v1.172.2) to **`dc5cec0` (v1.177.3)** across three merges from `dev`:

- **PR #399** (2026-09-24 14:16 -0400) — RS-099 (a sold PO and its sell orders now link to each other), RS-101 (inventory lot-picker no longer keeps stale selections under an earlier search term), RS-100 (new "Add Inventory to an existing sell order" flow), RS-102 (that picker now pages through every open order instead of stopping at the first page).
- **PR #402** (2026-09-24 20:18 -0400) — RS-103 (Mercury IO credit-card sync, pending charges included — a new external-integration write path) and RS-104 (the sync now survives a `/credit` endpoint failure).
- **PR #407** (2026-09-25 10:48 -0400, ~1.5h before this run) — RS-105 (payments ignore-rules, plus retroactively ignoring links unlinked before August 2026) and the two-part RS-106/RS-107 security sweep.

**RS-106/RS-107 in detail**: purchaser-facing PO reads were nulling manager-only fields (`finalSellPrice`, `finalSoldQty`, `realized`, `sellOrders`) instead of omitting the keys — a null-valued key still tells an unauthorized caller the feature exists. RS-106 fixed `routes/orders.ts`; RS-107 swept seven more endpoints (`GET /api/orders`, `GET /api/orders/:id`, `GET /api/orders/:id/events`, `PATCH /api/orders/:id`, `GET /api/dashboard`, `GET /api/warehouses`, `GET /api/inventory/:id/sell-orders`), and a same-day `/code-review max` follow-up (v1.177.3) caught six more instances (`paymentsLinked` on PATCH/handoff, an unguarded refusal message, a stale doc claim, a test gap). Full detail: `docs/tickets/RS-107-manager-only-data-is-absent-from-every-non-manager-a.md`. This was a real (low-severity — information disclosure of null placeholder keys and internal ids behind existing auth, not raw records) information-disclosure bug, now fixed in production.

`origin/main` and `origin/dev` are now fully in sync (`dev`'s tip `66fa572` is an ancestor of `main`'s tip `dc5cec0`, differing only by merge commits) — the divergence flagged in the last several reports is resolved.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window, and none of the three releases above (spanning roughly the last 20 hours) can be confirmed against real traffic.

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. With today's releases, the prior reports' unverified-backlog items (RS-080–RS-094's order-handoff work, migrations 0125/0130, `archiveOrderLinesTx`, RS-063's guard-bug window) are now several releases behind the current `main` and lower priority than the fresh surface below, once Railway access is restored:

- **RS-106/RS-107's endpoint sweep** (now live) — touches response shapes on `orders.ts`, `dashboard`, `warehouses.ts`, and `inventory` routes simultaneously. Functionally low-risk for authorized callers (keys were previously null, now absent), but worth checking for any client code path — desktop, mobile, or the MCP tools — that branched on a now-removed key being present-but-null rather than checking for absence. Also the highest-value thing to confirm first: that no *authorized* manager or purchaser lost data they're supposed to see.
- **RS-103/RS-104's Mercury IO card sync** (now live) — a brand-new external-integration write path with pending-charge handling and a same-day fix for surviving a `/credit` call failure. Watch for sync-job errors, timeouts, or partial-failure states against the Mercury API.
- **RS-105's payments ignore-rules retroactive unlink** — a one-time backfill-shaped change (ignoring links unlinked before August 2026); confirm it didn't silently re-ignore a link a user re-linked since then.
- **RS-099's PO/sell-order linkage and RS-100's inventory-selection dialog** — two-sided writes (`orders.ts` / `sellOrders.ts`) worth a partial-failure check.

## Recommended fixes

1. **Restore Railway access for this routine** — the standing blocker, now 74 days running with zero mitigation attempted (no token added, no connector enabled, no proxy allow-rule added). Either provision `RAILWAY_API_TOKEN` in this session's environment, install/enable a Railway MCP connector, or add an explicit allow-rule for `backboard.railway.com` / `erp.recycleservers.com` in the agent-proxy policy. Until one of these lands, this routine cannot see actual production errors, deployment health, or HTTP error rates — and today underscores the cost: three releases, including a security fix and a new payment-provider integration, shipped in the last 20 hours with no way to confirm they're healthy.
2. Once access is restored, prioritize the **last ~20 hours of logs** (since PR #399 at 2026-09-24 14:16 -0400) against the endpoints listed under Root-cause analysis — this is now the highest-risk unverified surface, well ahead of the older backlog from prior reports.
3. Specifically check for any 401/403/unexpected-null errors on the endpoints RS-106/RS-107 touched, since removing a key from a response shape is the kind of change that reads clean in tests but can surface as a frontend crash if some caller wasn't null-safe.

## Infra status

- **Deployments**: unknown — no Railway access (see above). From git history alone: three `dev`→`main` merges landed in the last ~20 hours (PRs #399, #402, #407), the most recent at 2026-09-25 10:48 -0400 (~1.5 hours before this run), each of which should have triggered a Railway deploy per the `main`-tracking production environment.
- **HTTP error rate (`backend`)**: unknown — no Railway access.
- **Anomalies**: `main` and `dev` are back in sync after several reports of growing divergence — a positive change, not a concern. The concern is inverted from prior reports: instead of an aging unverified backlog, there's now a burst of very recent production changes (including a security fix and a new external integration) with no log-based confirmation available.
