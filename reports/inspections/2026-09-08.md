# Daily production-health inspection — 2026-09-08

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (57th consecutive day), and today a production release shipped that this routine cannot verify.**

Every path specified in this routine's task was checked again today and
found unavailable, unchanged from every report since
[2026-07-14](./2026-07-14.md):

1. **Railway MCP server tools** — `ToolSearch` for `"railway"`,
   `"railway logs deployment metrics"`, and
   `"get_logs list_deployments http_error_rate service_metrics"` all
   returned no matching deferred tools.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is not set in
   this session's environment (`env | grep -i railway` empty); no `.env`
   exists in this worktree or the main checkout to source one from (only
   `.env.example`, which contains no token).
3. **Direct health-check probe** as a partial substitute — `curl` to
   `https://erp.recycleservers.com/api/health`,
   `https://inventory.recycleservers.com/api/health`, and
   `https://backboard.railway.com/graphql/v2` all failed identically:
   the egress proxy's own status endpoint records `connect_rejected`,
   "gateway answered 403 to CONNECT (policy denial or upstream failure)"
   for all three hosts, timestamped today. `noProxy` still covers only
   package registries and the Anthropic API — no path to Railway or the
   production domains.
4. `ListConnectors` filtered on `"railway"`, `"infra"`, `"ops"`,
   `"deployment"` returned an empty list.

No Railway or production API calls were attempted with fabricated or
guessed credentials.

**What's different today — a full production release shipped since
yesterday's report, unlike the last several "no change" days:**
`origin/main` advanced from **v1.128.0 to v1.132.0** via PR #288 ("dev
release: v1.132.0 — Ready to Pay, warehouse-manager review, owner picker,
bid sheet grouping"), merged **2026-09-08T00:40 UTC** — about 15.5 hours
before this report. This closes the gap noted in every report since
2026-08-31: production was one-to-several releases behind `dev`, and
today it caught back up in one jump, carrying v1.129.0 (RS-028, bid
sheet RAM grouping), v1.130.0 (RS-029, packing-list split), v1.131.0
(RS-030, PO owner picker), and v1.132.0 (RS-031/RS-032, a new
`ready_to_pay` order lifecycle stage with warehouse-manager transition
gating) all at once. `version-check`, `backend-tests`, and
`deploy-frontend` all completed `conclusion: success` on that push (run
IDs 34174059109, 34174059105, 34174059167). No new migration files
shipped in this release (`apps/backend/migrations` unchanged between
`origin/main~1` and `origin/main`), so this is an application-logic
change against the existing schema, not a schema migration.

`origin/dev` has since moved one release further ahead, to **v1.133.0**
(RS-033, PR #289, merged 2026-09-08T00:59 UTC; "Link… always opens the
picker" — a payments-linking UX change, ticket closed via #290 at
01:00 UTC). No open `dev → main` PR — normal pending-release state, one
release of headroom.

This is the first release day in this routine's 57-day blind streak
where the risk of the blindness is concrete rather than theoretical: the
v1.132.0 release includes a new order-status guard
(`advanceOrderTx` gating `reviewing`/`ready_to_pay` transitions to the
assigned warehouse's manager, per the PR body) and a line-status PATCH
validator — exactly the kind of change that would surface as 400s or
guard-rejection errors in backend logs if a warehouse assignment or role
edge case wasn't covered by its tests. This routine has no way to check
whether that happened. GitHub CI passing confirms the code the tests
cover works; it says nothing about production traffic since the deploy.

## Access failure — details

See "Summary verdict" above; this is now the 57th run in a row unable to
reach Railway by any sanctioned path. The blocking conditions are
unchanged from the [2026-09-07 report](./2026-09-07.md) — only the
stakes (a same-day production release) are new.

### How to fix (unchanged ask, now open 57 reports / 8 weeks)

- Register a Railway MCP server for this scheduled session's environment
  (exposing `get_logs`, `list_deployments`, `http_error_rate`,
  `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a
  Railway **project token** or **team API token** scoped to
  `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`)
  with read access to deployments, logs, and metrics for `backend`,
  `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach
  `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health`
  can be polled as a cheap liveness/version signal even without full log
  access.

Given today's release, the third (cheapest) option would already answer
the one question that matters most right now: did `backend` deploy
v1.132.0 successfully and is it serving without an elevated error rate.

If Railway visibility for this routine is not actually a priority,
consider disabling it rather than letting it accumulate unread reports —
but if it stays enabled, this is the only thing that will make it useful.

## Errors found

Not collected by this routine — see access failure above. No independent
production-log data point is available for today's window, including for
the v1.132.0 release window itself (2026-09-08T00:40 UTC onward).

## Root-cause analysis

Not applicable — no error data was available to analyze independently
this run. For reference, if errors do surface from today's release, the
most likely code paths (per the PR body, not verified against logs) are:

- `apps/backend/src/routes` — wherever `advanceOrderTx` lives: the
  warehouse-manager gate on `reviewing`/`ready_to_pay` transitions, which
  refuses any manager not assigned to the PO's warehouse and refuses the
  move entirely when no warehouse or an inactive assignee is set.
- The inventory line status PATCH handler: now refuses any status outside
  the line vocabulary, where it previously may have been more permissive.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" above) — this remains
   the blocking issue for this routine's entire purpose, and today's
   release makes it acutely relevant rather than a standing backlog item.
2. Once access is restored, first check `backend`'s current deployment
   version and health, and specifically look for 400/403 spikes on
   purchase-order transition endpoints and the inventory line PATCH route
   in the hours after 2026-09-08T00:40 UTC — the two code paths named
   above are the most plausible new sources of user-facing errors from
   today's release.
3. No application code changes are recommended by this run — nothing was
   observed to diagnose independently.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this
  routine — no deployment status, log, or error-rate data could be
  retrieved directly.
- **GitHub / release state:** `origin/main` advanced today to
  **v1.132.0** (PR #288, merged 2026-09-08T00:40 UTC) — the first
  `dev → main` release since v1.128.0 on 2026-09-04. `origin/dev` is one
  release ahead at **v1.133.0** (PR #289/#290, merged 2026-09-08T00:59–
  01:00 UTC). No open `dev → main` PR — normal pending-release state.
  All `version-check`, `backend-tests`, and `deploy-frontend` GitHub
  Actions runs on the `main` push completed with `conclusion: success`;
  no stuck or anomalous workflow runs found.
- **Frontend (Cloudflare Worker):** out of scope per task instructions;
  not checked beyond confirming `deploy-frontend` reported success on
  today's `main` push.
