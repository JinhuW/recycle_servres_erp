# Daily production-health inspection — 2026-08-24

## Summary verdict

⚠️ **NEEDS ATTENTION (unchanged) — Railway access still unavailable (42nd consecutive day). No production logs, deployments, or error rates could be collected. No production-affecting repo movement since yesterday; `dev` continues to pull ahead of `main`.**

This is an infrastructure/configuration gap in the inspection routine itself,
not a detected production incident. This run could reach Railway by none of the
sanctioned paths, continuing the streak documented in every report from
[2026-07-14](./2026-07-14.md) through [2026-08-23](./2026-08-23.md) — 41 straight
prior runs, now 42 with today's. Production health (backend, Postgres, db-sync,
pg-r2-backup) is therefore **unverified**, as it has been for six weeks.

**What changed since yesterday:** `origin/main` (the production deploy source)
is unchanged at `85f74ad` (v1.70.3, PR #141, 2026-08-18) — no new release shipped,
now 6 days stale. `origin/dev` advanced from `f722264` to `add0c04` with five
merges: #156 `feat(orders): managers can reopen a Done PO back to Reviewing`,
#157 `fix(scripts): prune's merged check compares against dev history, not its
tip`, #158 `feat(orders): managers can submit a PO on behalf of a purchaser`
(v1.82.0), #159 `fix(orders): chip numbers get one upper-case spelling; RAM
form stops offering an empty photo slot` (v1.82.1), and #160 `fix: max-effort
review of the dev→prod release diff — shipping/orders/packages fixes`
(v1.82.2, current root `package.json`). `dev` is now **19 commits ahead of
`main`** (up from 14 yesterday) — none of this has reached production yet, so
it does not change today's Railway-observability gap. Notably, #160's title
("max-effort review of the dev→prod release diff") suggests a dev→main
release may be imminent; if it lands, this routine will need to re-baseline
which commit is actually serving production. The retired `origin/prod` branch
is unchanged at `deeeea9` (v1.20.0, 2026-07-20). Migration head on `main` is
unchanged at `0093_shipments_seller_fill.sql`; `dev` has advanced to
`0096_chip_number_upper.sql` (new `0096` on top of yesterday's `0094`/`0095`
— none of these three are in production).

## What was tried

1. **Railway MCP server** — `ToolSearch` for `"get_logs list_deployments
   http_error_rate service_metrics"` and for `"railway"` / `"backboard
   graphql project service"` alone. No matching tools; no Railway MCP server
   is connected to this session (only `github` and `claude-code-remote` MCP
   servers, plus unrelated productivity connectors — Gmail, Google
   Calendar/Drive, Notion, Webull — appear in `ListConnectors`; none is
   Railway).
2. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`)
   — checked the environment for a credential: **no `RAILWAY_API_TOKEN`** set
   (confirmed via `printenv`/`env | grep -iE "token|api_key|railway"`, which
   returned only unrelated GitHub/Claude Code/gcloud tokens). Probed the
   endpoint regardless:
   ```
   curl -sS -m 20 -X POST https://backboard.railway.com/graphql/v2 \
     -H "Content-Type: application/json" -d '{"query":"{ me { id } }"}'
   → curl: (56) CONNECT tunnel failed, response 403
   ```
   The egress proxy's status endpoint confirms the cause as a policy denial,
   not a transient blip:
   ```
   "recentRelayFailures": [{
     "ts": "2026-08-24T16:13:20.568Z",
     "kind": "connect_rejected",
     "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
     "host": "backboard.railway.com:443"
   }]
   ```
   `backboard.railway.com` remains absent from the session's `noProxy`
   allowlist (`selective`/`toolScoped` both `false`). Per proxy policy, a 403
   CONNECT denial must be reported, not routed around.
3. **Railway CLI** — not on `PATH`; the network-policy block would stop a CLI
   call identically regardless, so not pursued further.
4. **Conclusion** — neither sanctioned access path is configured in this
   environment, for the 42nd day running. The blocker is identical in kind
   and cause to every prior day's report.

## Errors found

Not collected this run — no data source available. This is an absence of
visibility, **not** an all-clear. The report cannot assert production is
healthy; it can only assert that this routine could not observe it.

## Root-cause analysis

Root cause is confined to the inspection routine's own environment, unchanged
from prior reports:

1. **No Railway API reachability.** The agent proxy denies `CONNECT` to
   `backboard.railway.com:443` (HTTP 403, policy) — a deterministic allowlist
   denial, identical every day, not a network flake.
2. **No Railway credential.** Even if the host were reachable, there is no
   `RAILWAY_API_TOKEN` in the environment and no Railway MCP server connected,
   so neither the GraphQL fallback nor the preferred MCP path could
   authenticate.

No source-code fault in `apps/` or `packages/` is implicated — nothing in the
application repo can grant this routine egress or a token. Per `CLAUDE.md`,
production deploys from `origin/main` (currently v1.70.3, unchanged since
2026-08-18, now 6 days and 19 `dev` commits behind); the `prod` branch named
in this routine's infra brief was retired 2026-07-20 and its head has not
moved since, consistent with prior reports.

## Recommended fixes

*(Diagnosis only — no application code or Railway config changed by this run.)*

To make this routine functional, the environment it runs in needs **both**:

1. **Allowlist `backboard.railway.com` (port 443) in the session's egress
   policy**, or connect a **Railway MCP server** to the session (the task's
   preferred path). Until the host is permitted, every Railway API call fails
   with a 403 CONNECT denial regardless of credentials — this is the primary
   blocker.
2. **Provide a `RAILWAY_API_TOKEN`** (project- or account-scoped for
   `recycle-erp-experiment`, project id `e811d744-769f-4418-9f22-f371fd889923`)
   to the routine's environment. Without it the GraphQL fallback cannot
   authenticate even once the host is reachable.

Both are configuration changes to the scheduled task's environment, outside
what this inspector is permitted to modify. This gap has now persisted 42
consecutive days with no change — it warrants direct escalation to whoever
owns this routine's environment configuration, since the daily report alone
has not resulted in a fix and cannot fix its own access.

Separately, and outside Railway observability: `dev` is now 19 commits / ~12
tagged versions ahead of `main` with no release since 2026-08-18. PR #160's
title references a "dev→prod release diff" review, which may mean a release
to `main` is being prepared — worth confirming with whoever owns release
cadence, since it also means production has *not* received any of the
orders/shipping/packages work merged since 2026-08-18 (including #153's
mobile shipping and #154's edge-redirect fix), so any user reports about
those features do not yet apply to what's actually running in prod.

## Infra status

| Item | Status |
| --- | --- |
| Railway MCP server | Not connected |
| Railway GraphQL API (`backboard.railway.com`) | Blocked — 403 CONNECT (egress policy) |
| `RAILWAY_API_TOKEN` | Absent |
| Backend deployments / HTTP error rate | **Unknown** — not collectable |
| Postgres / db-sync / pg-r2-backup logs | **Unknown** — not collectable |
| `origin/main` (production source) | `85f74ad`, v1.70.3 — unchanged since 2026-08-18 (6 days stale) |
| `origin/dev` | `add0c04`, v1.82.2 — 5 merges since 2026-08-23 (#156–#160) |
| `origin/prod` (retired 2026-07-20) | `deeeea9`, v1.20.0 — unchanged |
| Migration head on `main` | `0093_shipments_seller_fill.sql` — unchanged |
| Migration head on `dev` | `0096_chip_number_upper.sql` — advanced from `0095` (new `0096`) |

Nothing anomalous can be reported from Railway because nothing from Railway
could be read. Repo state shows normal `dev` feature/fix activity, plus a
signal (#160) that a dev→main release may be in preparation; none of it has
reached `main`/production since 2026-08-18.
