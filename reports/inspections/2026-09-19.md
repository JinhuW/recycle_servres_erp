# Daily production-health inspection — 2026-09-19

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (68th consecutive day).** Every path this routine can use to reach Railway (MCP tools, the GraphQL API fallback, a direct `/api/health` probe) is blocked exactly as it has been since [2026-07-14](./2026-07-14.md). Since yesterday's report, production `main` advanced by **six releases** (v1.149.4 → v1.155.1, RS-069–RS-076), all landed via the same pre-prod-review discipline as yesterday's RS-068 batch: a `high`-effort review of the `dev` → `main` diff (RS-076) caught three findings in the hand-off/payment-proof flow before the cut, so the buggy intermediate versions never independently reached production — `main` jumped straight from v1.149.4 to v1.155.1, post-fix. `origin/dev` is one release further ahead, at v1.156.0 (RS-077), not yet promoted to `main`. This routine still cannot independently confirm any of this from logs, error rates, or deployment status — that requires the access this report has been asking for since week one.

### Access check (repeated from every report since 2026-07-14)

1. **Railway MCP server tools** — `ToolSearch` for `"railway logs deployment metrics get_logs list_deployments http_error_rate service_metrics"` returned no Railway tool, only unrelated GitHub Actions and Monitor tools. `ListConnectors` filtered to `"railway"` returns an empty list — no Railway connector installed for this session.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment; no `~/.railway` credentials directory; no `railway` CLI binary on `PATH`.
3. **Direct health-check probe** — `curl` to `https://erp.recycleservers.com/api/health`, `https://inventory.recycleservers.com/api/health`, and `https://backboard.railway.com/graphql/v2` all failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms all three as `connect_rejected` ("gateway answered 403 to CONNECT — policy denial or upstream failure") at 2026-09-19T16:04:42–43Z — same signature as every prior day.

No Railway or production API calls were attempted with fabricated or guessed credentials.

### What shipped to production since yesterday's report

`origin/main`'s `package.json` moved from **1.149.4** to **1.155.1**. `origin/dev` is one release ahead, at **1.156.0** (`abc739f`, PR #362, RS-077), not yet promoted to `main`.

| Version | Date (UTC) | Ticket / PR | Change |
|---|---|---|---|
| v1.150.0 | 2026-09-18 13:13 | RS-069 / #354 | A company-paid PO's PayPal transaction ID must match a real synced PayPal transaction, not just be present |
| v1.151.0 | 2026-09-18 14:26 | RS-070 / #355 | PO payment suggestions now gate on the payment's assigned owner |
| v1.152.0 | 2026-09-18 20:37 | RS-072 / #356 | Unit cost is now enforced at line-save time, not only at leave-Draft |
| v1.153.0 | 2026-09-18 20:48 | RS-071 / #357 | One shared payment-method picker across all six PO surfaces; cash requires a screenshot of the amount paid (new migration `0128_po_cash_shot_rule.sql`) |
| v1.154.0 | 2026-09-18 21:12 | RS-074 / #358 | Phone PO view split into an order screen and a products screen; readiness list added |
| v1.155.0 | 2026-09-19 00:58 | RS-075 / #359 | Desktop In Transit chip names the carrier and links to tracking |
| v1.155.1 | 2026-09-19 11:40 | RS-076 / #360 | Pre-prod review fix (see below) — released same window as the diff it reviews |

(Times converted from the commits' recorded `-04:00` offsets to UTC.)

**RS-076 in detail** (ticket `docs/tickets/RS-076-dev-to-main-review-findings-before-the-prod-cut.md`, on `dev`/`main`, not present on this branch): before promoting v1.150.0–v1.155.0 to `main`, a `high`-effort review of `origin/main...origin/dev` returned three findings, all in the RS-071/RS-074 payment-readiness flow — none blocking, all fixed pre-release:

- The hand-off dialog and phone sheet kept their own copy of payment proof, seeded from the order as the page first loaded — a screenshot uploaded on the PO page (exactly where the new readiness list sends a purchaser) read as "missing" once the hand-off sheet opened, forcing a double upload.
- A saved-method payment moved to a different method with the dialog still open let the old method's exemption (`txnRequired`/`cashShotRequired`) carry over, so the client said ready and the server then refused.
- Submit's live PayPal Transaction Search pull ran against *any* Draft with an unrecognized id — archived orders and the fourteen `CASH`/`WAIT` placeholder strings included, none of which can ever match — instead of only a live Draft with an id in PayPal's actual shape.

Because the review ran before the `dev` → `main` promotion, **none of the three findings independently reached production** — `main` advanced straight from v1.149.4 to v1.155.1. This is the same discipline as yesterday's RS-068 batch (v1.147.1–v1.149.3 reviewed before promotion, four findings fixed in the same commit that first shipped them to `main`).

One new migration landed in this window: `apps/backend/migrations/0128_po_cash_shot_rule.sql` (RS-071, cash-payment proof rule), additive only. No status-guard route files (`purchase-orders`, `sell-orders`, `transfer-orders`, bids) were touched.

`origin/dev` carries one additional release not yet on `main` — **v1.156.0** (RS-077, PR #362): the In Transit chip gains a tracking-number link and ETA/delivered/exception state, plus a Shipment timeline block on the desktop PO page, plus a Total cost column on the PO list. Feature work, additive API fields (`tracking.status`, `tracking.trackingEta`, `package`, `goodsTotal`), no migration, no status-guard changes — lower priority to verify once access returns.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window.

## Root-cause analysis

Not applicable in the usual sense — no error data was available to analyze independently this run. Nothing in today's shipped batch looks structurally risky by code-reading alone: RS-069's PayPal-lookup rule and RS-071's cash-proof rule both follow the same "live only once synced/grandfathered by a cutoff timestamp" pattern used successfully in prior releases (RS-063, RS-068's fix), and RS-076 already closed the loop on the three review findings before they reached `main`.

Carried over from prior reports, still unverified due to the same access gap:
- `apps/backend/src/services/orderHandoff.ts` and the `/handoff` route (RS-050, v1.141.0) — largest recent order-transition change; no confirmation of production behavior. Now also the surface RS-071/RS-074/RS-076 iterated on most heavily this week — worth prioritizing once logs are reachable.
- `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — additive-only, lowest priority.
- `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`) — still no confirmation it behaved correctly in production.
- RS-063's 3.5-hour live guard-bug window (2026-09-18 01:49–05:20 UTC, fixed at v1.149.4) — whether any of the 43 legacy `$0` POs were actually hit remains unconfirmed.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" below) — the sole blocking issue for this routine's entire purpose, now on its eleventh week and worsening in impact as unverified releases accumulate (six more in this 24-hour window, on top of the backlog already unverified).
2. As soon as access is restored, prioritize, in order: (a) RS-071/RS-074/RS-076's payment-readiness flow — three real client/server-agreement bugs were caught by review this week alone in this exact area, meaning it is more bug-prone than most; check for 409s or repeated-upload complaints on the hand-off endpoints since v1.153.0 (2026-09-18 20:48 UTC); (b) the RS-063 guard-bug window from the 2026-09-18 report, still open; (c) RS-057's depressed `/api/payments*` volume window (2026-09-15/16), still outstanding from prior reports.
3. PO-1370 (flagged in the [2026-09-13 report](./2026-09-13.md)) remains an open, not-yet-resolved manual-data task for a human to action — repeating the flag since it has not been reported as closed.
4. No application code changes are recommended by this run — this routine diagnoses, it does not fix, and no error evidence was available to diagnose independently. All three RS-076 review findings are already fixed on `main` as of v1.155.1.

### How to fix the access gap (unchanged ask, now open 68 reports / over 9.5 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly.
- **GitHub / release state:** `origin/main` (production) advanced from **v1.149.4** to **v1.155.1** — six releases (v1.150.0–v1.155.1) across PRs #354–#360, plus review PR #360's own fixes. `origin/dev` is one release ahead at **v1.156.0** (`abc739f`, #362), not yet promoted to `main`.
- **Frontend (Cloudflare Worker):** out of scope per task instructions, but note the `main`→prod pushes above each auto-trigger `.github/workflows/deploy-frontend.yml`; no independent confirmation that any of these Worker deploys succeeded. RS-071/RS-074/RS-075/RS-076's changes span both frontend (payment picker UI, phone PO split, tracking chip) and backend (migration 0128, additive API fields), so both a Railway backend deploy and a Worker deploy are implicated this window — neither independently confirmed.
