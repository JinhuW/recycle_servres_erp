# Daily production-health inspection — 2026-09-22

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (71st consecutive day).** Every path this routine can use to reach Railway (MCP tools, the GraphQL API fallback, a direct `/api/health` probe) is blocked exactly as it has been since [2026-07-14](./2026-07-14.md). Since yesterday's report, production `main` advanced by **two small releases** (v1.171.2 → v1.172.2, RS-097/RS-098) — both frontend-only fixes, no migrations, no backend route changes. This is a much smaller and lower-risk window than yesterday's 19-release purchase-order consolidation. This routine still cannot independently confirm any of it from logs, error rates, or deployment status — that requires the access this report has been asking for since week one.

### Access check (repeated from every report since 2026-07-14)

1. **Railway MCP server tools** — `ToolSearch` for `"railway mcp get_logs list_deployments http_error_rate service_metrics"` and `"railway"` alone returned no Railway tool; `ListConnectors` with keyword `"railway"` returned an empty list. Only unrelated GitHub, Monitor, and artifact-related tools are available.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment.
3. **Direct probe** — `curl` to `https://backboard.railway.com/graphql/v2` and `https://erp.recycleservers.com/api/health` both failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms both as `connect_rejected` ("gateway answered 403 to CONNECT — policy denial or upstream failure") at 2026-09-22T16:05:07Z / 16:05:08Z — same signature as every prior day.

No Railway or production API calls were attempted with fabricated or guessed credentials.

### What shipped to production since yesterday's report

`origin/main`'s `package.json` moved from **1.171.2** to **1.172.2** — two releases, both frontend-only:

| Version | Ticket / PR | Change |
|---|---|---|
| v1.172.1 | RS-097 / #392 | Desktop PO page's Delivery tab: Source, Receiving warehouse, and the delivery-method fields each get their own row instead of sharing a three-column row (pure layout fix in the orders detail component). |
| v1.172.2 | RS-098 / #393 | Phone file-drop boxes (Cost Payment / Commission screenshots, hand-off dialog, line-photo "+" tile) drop the `capture="environment"` attribute on their file input, which was forcing iOS Safari / Android Chrome straight into the rear camera instead of offering the native Photo Library / Files chooser. `AttachmentDropzone` loses its `capture` prop; `PaymentFields`/`CommissionPaymentFields` lose the `phone` prop that only set it. |

Both are UI-only changes (no backend route, migration, or schema touched) — `git diff --stat v1.171.2 origin/main -- apps/backend/migrations/` shows no new migrations, and both commits' stats are confined to `apps/frontend/src`. This is a materially smaller and lower-risk window than yesterday's report (19 releases landing the entire PO hand-off/status/commission rework at once).

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window.

## Root-cause analysis

Not applicable — no error data was available to analyze independently this run. By code-reading alone, today's shipped changes are low-risk: two isolated frontend fixes (a CSS/layout change and a file-input attribute removal), neither touching state management, validation, or any backend endpoint. The bigger standing risk remains what carried over from prior reports — the large PO hand-off/status/commission consolidation (RS-080–RS-094) that fully reached production on 2026-09-21 — which this routine still has no log access to verify.

Carried over from prior reports, still unverified due to the same access gap:
- `apps/backend/src/routes/orders.ts` / `apps/backend/src/services/orderHandoff.ts` — the largest unverified change in this routine's history (RS-080–RS-094, fully in production since 2026-09-21). Top priority once logs are reachable: watch for 4xx/5xx on `/api/orders/:id/handoff`, `/api/orders/:id/package`, and PATCH order-status endpoints since 2026-09-20 05:15 UTC-4.
- `apps/backend/migrations/0130_po_sold_lifecycle.sql` — the one data-mutating migration in that window; confirm it ran cleanly on the prod DB.
- `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — additive-only, lowest priority, carried for completeness.
- `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`) — still no confirmation it behaved correctly in production.
- RS-063's 3.5-hour live guard-bug window (2026-09-18 01:49–05:20 UTC, fixed at v1.149.4) — whether any of the 43 legacy `$0` POs were actually hit remains unconfirmed.

## Recommended fixes

1. **Restore Railway access for this routine** — the standing blocker, now 71 days running. Either provision `RAILWAY_API_TOKEN` in this session's environment, install/enable a Railway MCP connector, or add an explicit allow-rule for `backboard.railway.com` / `erp.recycleservers.com` in the agent-proxy policy. Until one of these lands, every daily report from this routine is code-review-only and cannot see actual production errors, deployment health, or HTTP error rates.
2. Once access is restored, the first pass should specifically target the endpoints listed under Root-cause analysis above (`/api/orders/:id/handoff`, `/api/orders/:id/package`, order-status PATCH routes) over the full 2026-09-20–2026-09-22 window, since that is the highest-risk unverified surface accumulated across the last several reports.
3. No action needed on today's two releases (v1.172.1/v1.172.2) beyond normal log verification once access exists — they are small, isolated, frontend-only changes.

## Infra status

- **Deployments**: unknown — no Railway access (see above).
- **HTTP error rate (`backend`)**: unknown — no Railway access.
- **Anomalies**: none observable from this routine's available data. `origin/dev` and `origin/main` are in sync at v1.172.2 — nothing outstanding to promote.
