# Daily production-health inspection — 2026-09-20

## Summary verdict

⚠️ **NEEDS ATTENTION — Railway access still unavailable (69th consecutive day).** Every path this routine can use to reach Railway (MCP tools, the GraphQL API fallback, a direct `/api/health` probe) is blocked exactly as it has been since [2026-07-14](./2026-07-14.md). Since yesterday's report, production `main` advanced by **three releases** (v1.155.1 → v1.157.1, RS-077/RS-078/RS-079), and `origin/dev` is now four releases further ahead (v1.158.0 → v1.161.0, all RS-080), not yet promoted to `main`. This routine still cannot independently confirm any of this from logs, error rates, or deployment status — that requires the access this report has been asking for since week one.

### Access check (repeated from every report since 2026-07-14)

1. **Railway MCP server tools** — `ToolSearch` for `"railway logs deployments http_error_rate service_metrics"` and a bare `"railway"` query returned no Railway tool, only unrelated GitHub Actions and Monitor tools. `ListConnectors` filtered to `"railway"` returns an empty list — no Railway connector installed for this session.
2. **Railway GraphQL API fallback** — `RAILWAY_API_TOKEN` is unset in this session's environment; no `~/.railway` credentials directory; no `railway` CLI binary on `PATH`.
3. **Direct health-check probe** — `curl` to `https://erp.recycleservers.com/api/health`, `https://inventory.recycleservers.com/api/health`, and `https://backboard.railway.com/graphql/v2` all failed identically: `CONNECT tunnel failed, response 403`. The agent-proxy status endpoint confirms all three as `connect_rejected` ("gateway answered 403 to CONNECT — policy denial or upstream failure") at 2026-09-20T16:05:14–15Z — same signature as every prior day.

No Railway or production API calls were attempted with fabricated or guessed credentials.

### What shipped to production since yesterday's report

`origin/main`'s `package.json` moved from **1.155.1** to **1.157.1**. `origin/dev` is four releases ahead, at **1.161.0** (`4351a82`, PR #372, RS-080), not yet promoted to `main`.

| Version | Date (UTC) | Ticket / PR | Change |
|---|---|---|---|
| v1.156.0 | 2026-09-19 (carried over from dev, now on main) | RS-077 / #362 | In Transit chip shows box state, PO page shows shipment journey, PO list gains a Total cost column |
| v1.156.1 | — | (refactor) / #364 | One shared `StepTimeline` component, one newest-package lookup, package types corrected |
| v1.157.0 | — | RS-078 / #365 | Prepaid-label (ShipSaving) shipping flow removed; package tracking kept |
| v1.157.1 | 2026-09-20 04:01 | RS-079 / #366 | A failed PayPal transaction pull now says so explicitly, the pull is throttled, and the phone scan flow preserves concurrent edits |

(Times converted from the commits' recorded `-04:00` offsets to UTC.)

No new migrations landed in `apps/backend/migrations/` between yesterday's head (`7201d23`, v1.155.1) and today's `origin/main` head. No status-guard route files (`purchase-orders.ts`, `sell-orders.ts`, `transfer-orders.ts`) were touched by RS-077/078/079.

**`origin/dev` carries a larger, not-yet-promoted batch — RS-080** (4 PRs, v1.158.0 → v1.161.0, ticket already closed per `2f7c57a docs(tickets): close RS-080`): a rework of the purchase-order hand-off and status surfaces —
- v1.158.0 (#369): hand-off facts computed on PATCH, one blockers list surfaced on GET, partial-body hand-off requests supported.
- v1.159.0 (#370): the hand-off becomes a single checkpoint with one readiness rule shared across every surface (replacing the per-surface duplication RS-071/074/076 patched around last week).
- v1.160.0 (#371): the desktop PO page rebuilt around order status, "one home per fact."
- v1.161.0 (#372): the phone PO page gains the same stage panel and a Delivery fold.

This is the same area (`orderHandoff.ts` / the hand-off route / PO status surfaces) that RS-071/074/076 iterated on heavily last week and that this routine has flagged every day since as unverified in production. RS-080 appears to be the intended consolidation of that flow — worth prioritizing for verification first once access returns, ahead of the smaller RS-077/078/079 batch already on `main`. No new migrations accompany RS-080 either.

## Errors found

Not collected by this routine — see access failure above. No independent production-log data point is available for today's 24-hour window.

## Root-cause analysis

Not applicable in the usual sense — no error data was available to analyze independently this run. Nothing in today's shipped batch looks structurally risky by code-reading alone: RS-079's PayPal-pull throttling/error-surfacing follows the same defensive pattern as RS-076's fix, and RS-078 is a net removal (deleting the unused ShipSaving flow) rather than new surface area.

Carried over from prior reports, still unverified due to the same access gap:
- `apps/backend/src/services/orderHandoff.ts` and the `/handoff` route — now the subject of RS-080's full rework (v1.158.0–1.161.0) on `dev`, on top of RS-050/RS-071/RS-074/RS-076's earlier changes. This is the single most-changed, least-verified surface in the system right now — top priority once logs are reachable.
- `apps/backend/migrations/0125_ram_rank_3ds_options.sql` — additive-only, lowest priority.
- `apps/backend/src/services/orderAdvance.ts` (`archiveOrderLinesTx`) — still no confirmation it behaved correctly in production.
- RS-063's 3.5-hour live guard-bug window (2026-09-18 01:49–05:20 UTC, fixed at v1.149.4) — whether any of the 43 legacy `$0` POs were actually hit remains unconfirmed.

## Recommended fixes

1. Close the Railway access gap (see "How to fix" below) — the sole blocking issue for this routine's entire purpose, now on its tenth week and worsening in impact: RS-080's full hand-off/PO-status rework has now shipped to `dev` on top of last week's already-unverified payment-readiness changes, with zero independent production confirmation of any of it.
2. As soon as access is restored, prioritize, in order: (a) RS-080 once it promotes to `main` — it rewrites the exact hand-off/readiness logic that produced three review-caught bugs last week (RS-076); (b) RS-071/RS-074/RS-076's payment-readiness flow already on `main`, still unverified; (c) the RS-063 guard-bug window from the 2026-09-18 report, still open; (d) RS-057's depressed `/api/payments*` volume window (2026-09-15/16), still outstanding.
3. PO-1370 (flagged in the [2026-09-13 report](./2026-09-13.md)) remains an open, not-yet-resolved manual-data task for a human to action — repeating the flag since it has not been reported as closed.
4. No application code changes are recommended by this run — this routine diagnoses, it does not fix, and no error evidence was available to diagnose independently.

### How to fix the access gap (unchanged ask, now open 69 reports / nearly 10 weeks)

- Register a Railway MCP server for this scheduled session's environment (exposing `get_logs`, `list_deployments`, `http_error_rate`, `service_metrics`), **or**
- Set `RAILWAY_API_TOKEN` in this session's/environment's secrets — a Railway **project token** or **team API token** scoped to `recycle-erp-experiment` (project id `e811d744-769f-4418-9f22-f371fd889923`) with read access to deployments, logs, and metrics for `backend`, `Postgres`, `db-sync`, and `pg-r2-backup` is sufficient, **or**
- At minimum, allow this session's egress proxy to reach `erp.recycleservers.com` / `inventory.recycleservers.com` so `/api/health` can be polled as a cheap liveness/version signal even without full log access.

## Infra status

- **Railway (backend, Postgres, db-sync, pg-r2-backup):** unknown to this routine — no deployment status, log, or error-rate data could be retrieved directly.
- **GitHub / release state:** `origin/main` (production) advanced from **v1.155.1** to **v1.157.1** — three releases (v1.156.0–v1.157.1) across PRs #362, #364–#366. `origin/dev` is four releases ahead at **v1.161.0** (`4351a82`, #372, RS-080), not yet promoted to `main`.
- **Frontend (Cloudflare Worker):** out of scope per task instructions, but note the `main`→prod pushes above each auto-trigger `.github/workflows/deploy-frontend.yml`; no independent confirmation that any of these Worker deploys succeeded. RS-077/078/079's changes span both frontend (In Transit chip, PO list column) and backend (PayPal throttling), so both a Railway backend deploy and a Worker deploy are implicated this window — neither independently confirmed.
