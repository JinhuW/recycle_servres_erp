# Daily production-health inspection — 2026-08-29

## Summary verdict

⚠️ **NEEDS ATTENTION (unchanged) — Railway access still unavailable (47th consecutive day). No production logs, deployments, or error rates could be collected.** `main` (production deploy source) advanced from `v1.100.1` to `v1.102.1` since yesterday's report via PR #212 merging `dev` into `main` (four commits, two feature releases + one release-review fix release); `dev` itself has already moved 2 commits / 2 minor versions further to `v1.104.0`, plus 3 more migrations.

This is an infrastructure/configuration gap in the inspection routine itself,
not a detected production incident. This run could reach Railway by none of
the sanctioned paths, continuing the streak documented in every report from
[2026-07-14](./2026-07-14.md) through [2026-08-28](./2026-08-28.md) — 46
straight prior runs, now 47 with today's. Production health (backend,
Postgres, db-sync, pg-r2-backup) is therefore **unverified**, as it has been
for over six weeks.

**What changed since yesterday:** `origin/main` (the production deploy
source) advanced from `7f24198` (v1.100.1) to `a7b0ab5` (v1.102.1) via PR
#212 merging `dev` into `main`. That release carries three commits on top of
yesterday's `main`: part-number typeahead suggestions (#208, v1.101.0),
Shippo webhook-driven package tracking (#209, v1.102.0), and a
release-review fix pass on shipping/parts before the prod cut (#211,
v1.102.1). `dev` has since taken 2 more commits
(`d529169` v1.103.0 — fix for a saved warehouse shipping address hidden by a
stale cache, `11f27c6` v1.104.0 — part-number matching normalized across how
a part number was typed), so `main` is again 2 commits / 2 minor versions
behind `dev` as of this report. Migration heads differ by three files: `main`
is at `0108_package_tracking_registered.sql`, `dev` has already added
`0109_warehouse_address_from_ship.sql`, `0110_merge_separator_duplicate_parts.sql`,
and `0111_ref_prices_canon_part_idx_separators.sql` (not yet deployed). As
with every recent release, this routine has no way to confirm today's `main`
deploy landed cleanly on Railway.

## What was tried

1. **Railway MCP server** — `ToolSearch` for `"railway get_logs deployments
   service metrics"` and separately for `"railway"` alone. No matching
   tools in either search; no Railway MCP server is connected to this
   session (only `github` MCP and generic harness tools are available).
   `ListConnectors` (both filtered on `"railway"` and unfiltered, listing
   every connector on the account — Gmail, Google Calendar, Google Drive,
   Notion, two `Recycle`-named connectors, Webull) confirmed no Railway
   connector exists.
2. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`)
   — checked the environment for a credential: no `RAILWAY_API_TOKEN` set
   (checked via `printenv`), and no `.env` file present in this worktree
   (the session-start worktree script itself warned ".env not found in the
   main checkout"). Probed the endpoint anyway:
   ```
   curl -sS -m 15 -X POST https://backboard.railway.com/graphql/v2 \
     -H "Content-Type: application/json" -d '{"query":"{ me { id } }"}'
   → curl: (56) CONNECT tunnel failed, response 403
   ```
   The agent proxy reported the cause explicitly: `backboard.railway.com:443
   — connect_rejected (the egress proxy denied the CONNECT (organization
   policy) or could not reach the destination)`. Same failure class as every
   prior day (an egress-proxy allowlist denial), now with the proxy's own
   diagnostic confirming it's a policy block, not a network flake.
3. **Railway CLI** — not on `PATH`. Would be blocked by the same egress
   policy regardless, so not pursued further.
4. **Conclusion** — neither sanctioned access path is configured in this
   environment, for the 47th day running. The blocker is identical in kind
   and cause to every prior day's report.

## Errors found

Not collected this run — no data source available. This is an absence of
visibility, **not** an all-clear. The report cannot assert production is
healthy; it can only assert that this routine could not observe it — and
today that gap again coincides with a fresh release having just shipped.

## Root-cause analysis

Root cause is confined to the inspection routine's own environment,
unchanged from prior reports:

1. **No Railway API reachability.** The session's egress policy has no
   allow rule for `backboard.railway.com` — a deterministic allowlist
   denial (`CONNECT tunnel failed, response 403`), not a network flake.
2. **No Railway credential.** Even if the host were reachable, there is no
   `RAILWAY_API_TOKEN` in the environment and no Railway MCP server
   connected, so neither the GraphQL fallback nor the preferred MCP path
   could authenticate.

No source-code fault in `apps/` or `packages/` is implicated — nothing in
the application repo can grant this routine egress or a token. Per
`CLAUDE.md`, production deploys from `origin/main`, now `v1.102.1` (updated
2026-08-29 via #212); `dev` is already 2 commits ahead at `v1.104.0`. The
`prod` branch named in this routine's infra brief was retired 2026-07-20
and its head has not moved since, consistent with prior reports.

## Recommended fixes

*(Diagnosis only — no application code or Railway config changed by this run.)*

To make this routine functional, the environment it runs in needs **both**:

1. **Allowlist `backboard.railway.com` (port 443) in the session's egress
   policy**, or connect a **Railway MCP server** to the session (the task's
   preferred path). Until the host is permitted, every Railway API call is
   rejected regardless of credentials — this is the primary blocker.
2. **Provide a `RAILWAY_API_TOKEN`** (project- or account-scoped for
   `recycle-erp-experiment`, project id `e811d744-769f-4418-9f22-f371fd889923`)
   to the routine's environment. Without it the GraphQL fallback cannot
   authenticate even once the host is reachable.

Both are configuration changes to the scheduled task's environment, outside
what this inspector is permitted to modify. This gap has now persisted 47
consecutive days with no change — it continues to warrant direct escalation
to whoever owns this routine's environment configuration, independent of
today's specific findings. As with prior days, recommend someone with
Railway dashboard access manually spot-check the `backend` service's
deployment status and error rate for the last 24h, given the #212 release
to production today (v1.100.2–v1.102.1, three commits including Shippo
webhook package tracking and a pre-cut release-review fix pass).

## Infra status

| Item | Status |
| --- | --- |
| Railway MCP server | Not connected |
| Railway GraphQL API (`backboard.railway.com`) | Blocked — egress policy denies the host (CONNECT 403, proxy confirms `connect_rejected` / organization policy) |
| `RAILWAY_API_TOKEN` | Absent |
| Backend deployments / HTTP error rate | **Unknown** — not collectable |
| Postgres / db-sync / pg-r2-backup logs | **Unknown** — not collectable |
| `origin/main` (production source) | `a7b0ab5`, **v1.102.1** — updated 2026-08-29 via #212 (was v1.100.1) |
| `origin/dev` | `11f27c6`, v1.104.0 — 2 commits / 2 minor versions ahead of `main` |
| `origin/prod` (retired 2026-07-20) | unchanged |
| Migration head on `main` | `0108_package_tracking_registered.sql` |
| Migration head on `dev` | `0111_ref_prices_canon_part_idx_separators.sql` — three migrations ahead of `main` |

Nothing anomalous can be reported from Railway because nothing from Railway
could be read. Repo state shows another routine dev→main release landing
today; whether it deployed cleanly to production is unverified by this
routine.
