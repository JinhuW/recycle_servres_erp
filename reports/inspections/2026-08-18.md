# Daily production-health inspection — 2026-08-18

## Summary verdict

⚠️ **INSPECTION INCOMPLETE — Railway access unavailable (36th consecutive day) — no new production release since yesterday, but the unverified `dev` backlog keeps growing**

This run could not reach Railway by any of the three sanctioned paths, so no
logs, deployment statuses, or HTTP error rates were collected for `backend`,
`Postgres`, `db-sync`, or `pg-r2-backup`. This is a report about the
inspection tooling, not a verdict on application health.

This is the same failure mode as every report from
[2026-07-14](./2026-07-14.md) through [2026-08-17](./2026-08-17.md) —
thirty-five straight prior runs, now thirty-six with today's.

**What's different today:** `origin/main` has **not** moved since yesterday
— still `41de37d`, `package.json` version **1.67.0**, PRs #129–#133. The
security-relevant OAuth scope change (PR #131) flagged yesterday remains
one day older and still unconfirmed against real logs.

`origin/dev`, however, advanced three more merges — `036c2b3` (v1.69.0) →
`df805cd` (**v1.70.1**), PRs **#136, #137, #138** — all shipping-feature
follow-ups, all frontend-only:
- **#136** `fix(shipping): move the seller-link option inside the new-label
  form` — `apps/frontend/src/pages/desktop/ShippingPanel.tsx` reorders the
  "ask seller to fill in" vs. "enter it yourself" choice inside the create
  form. UI copy/layout only.
- **#137** `feat(shipping): shipping-first flow on a dedicated Shipping
  page (UI only)` — moves label creation off the PO page onto `#/shipping`,
  auto-creating a draft PO (`POST /api/orders/draft`) when a label is
  started from scratch, and advancing draft POs to In Transit
  (`POST /:id/advance`) when a shipment ships. Backend endpoints it calls
  already existed; this PR is routing/UI only per its own description.
- **#138** `fix(shipping): give label spend its own row in the PO cost
  breakdown` — `apps/frontend/src/components/CostTape.tsx` un-folds label
  costs from the `Other fees` cell client-side. Explicitly "Backend
  untouched" per the commit message.
- One new migration, **`0093_shipments_seller_fill.sql`**: drops `NOT NULL`
  on `shipments.from_*`/dimension columns (to allow an empty shell created
  before the seller fills in address/package via a tokenized `/s/<token>`
  link, mirroring the existing vendor-portal pattern) and adds a partial
  unique index on `seller_token`. None of this is in `main` yet.

None of #136–#138 change backend logic, so they don't add a new backend
risk item on their own — but they confirm the ShipSaving/shipments feature
(flagged since [2026-08-17](./2026-08-17.md)) is still actively being built
out on `dev`. The entire shipments/ShipSaving feature (PRs #134–#138)
remains on `dev` only — it has not shipped to production. The
unconfirmed-in-prod item from yesterday (PR #131, OAuth scope ceiling) is
still the only customer-facing change actually sitting in the blackout.

## What was tried

1. **Railway MCP server** — `ToolSearch` with `"railway logs deployments
   metrics error rate http_error_rate service_metrics"` and bare
   `"railway"`. No matching tools found in either query (same as every
   prior day).
2. **Connector check** — `ListConnectors(keywords=["railway"])` returns an
   empty list. No Railway connector registered for this account/session.
3. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`)
   — swept environment variables (`printenv | grep -i railway`, and a
   general scan): no `RAILWAY_API_TOKEN` or any Railway-scoped credential
   present. No `.env` file at the repo root (only `.env.example`, unrelated
   to Railway). Probed the endpoint anyway:
   ```
   curl -sS -o /dev/null -w "http_code:%{http_code}\n" \
     https://backboard.railway.com/graphql/v2 --max-time 15
   → curl: (56) CONNECT tunnel failed, response 403
   → http_code:000
   ```
   Identical signature to every prior day — a policy-level `CONNECT` denial
   on `backboard.railway.com`, not a transient network error, not a missing
   credential.
4. **Railway CLI** — not present on `PATH` (`which railway` → exit 1); not
   pursued further since the network-policy block would stop a CLI call the
   same way regardless of credentials.
5. **Conclusion** — neither access path is configured in this environment,
   for the thirty-sixth day running.

## Errors found

Not collected this run (no data source available).

## Root-cause analysis

N/A — no logs were retrieved, so no error triage was possible this run.
Highest-value places to check once access is restored, in priority order:

- **Still in production, still unconfirmed (2 days old today) —**
  `apps/backend/src/oauth/tokens.ts` (`restrictScopesToRole`, ~line 103)
  and `apps/backend/src/oauth/server.ts` (`/authorize` and
  `/authorize/consent` handlers), PR #131: tightened the non-manager scope
  ceiling to `market:read` only, dropping `sellorder:read` too. Confirm no
  non-manager connector session hit an unexpected `invalid_scope` /
  scope-narrowing surprise on its next refresh rotation, and that
  `/oauth/authorize` / `/oauth/token` show no elevated error rate.
  `apps/backend/src/routes/market.ts` (PR #132, sort-key whitelist for the
  desktop table's column headers) remains lower risk — pure whitelist
  addition, same fallback (`updated_at DESC`) as before.
- **Still queued on `dev`, not yet in `main` —**
  `apps/backend/src/shipping/index.ts` (`pickShippingClient`): falls back
  to a stub provider whenever `SHIPSAVING_API_TOKEN` / `SHIPSAVING_API_URL`
  are unset, logging a warning rather than refusing to boot — the same
  silent-fallback shape CLAUDE.md already flags for the OCR provider. The
  feature has grown three more PRs since first flagged
  ([2026-08-17](./2026-08-17.md)) and now includes a seller self-service
  fill flow (migration `0093`, public `/s/<token>` route). Worth a
  deliberate pre-merge check (not just log-watching) that both env vars are
  set in the `backend` Railway service, and that the new
  `shipments_seller_token_idx` uniqueness holds under concurrent draft
  creation, before this lands on `main`.
- Carried over from prior reports, still unconfirmed (one day older each,
  no new signal on any of these):
  - **`apps/backend/src/routes/tracker.ts`** (PR #125/#127) — now 4 days in
    production; confirm `TRACKER_API_URL`/`TRACKER_API_TOKEN` are set if the
    tracker feature is meant to be live, and check for `502`/`501` volume
    from `/api/tracker/*` if they aren't.
  - **`apps/backend/src/services/sellableInventory.ts`** (`availableQty`
    around line 92, PR #122) — ~9 days in production, still unconfirmed.
  - **`apps/backend/migrations/0090_backfill_other_item_type.sql`** /
    `routes/inventory.ts` / `itemTypes.ts` (PR #121) — still unconfirmed
    against prod's row count.
  - Migrations `0088`–`0090` — worth confirming `_migration_ledger` on prod
    Postgres shows all applied cleanly.

None of this is a claim that anything is broken — only that the queue of
unconfirmed production changes has held steady (no new prod release today)
while the un-merged backlog behind it on `dev` keeps growing.

## Recommended fixes

Not application code — this is infra/session configuration for the
inspector routine itself, now a repeat item for the **thirty-sixth
consecutive day**:

- Add a Railway MCP server to this session's environment (or the pool it
  runs in), **or** set `RAILWAY_API_TOKEN` as an environment variable
  available to this routine's session, scoped to the
  `recycle-erp-experiment` Railway project
  (`e811d744-769f-4418-9f22-f371fd889923`).
- The outbound proxy still returns `403` (policy denial) on `CONNECT` to
  `backboard.railway.com:443`, confirmed again today, identical to every
  prior day. Whoever owns the environment's egress policy needs to allow
  `backboard.railway.com` (or `*.railway.com` / `*.railway.app`), or this
  routine keeps failing even after a token is supplied.
- The routine's own task brief still states production deploys from the
  `prod` branch — `prod` was retired 2026-07-20 and production now tracks
  `main` directly (per CLAUDE.md). Still uncorrected as of today; low
  priority next to the access blackout, but continues to mislead whoever
  next edits this routine's instructions.
- **This is now the thirty-sixth consecutive scheduled run with zero
  visibility — over five weeks — spanning four production releases
  (v1.62.0-era, v1.62.6, v1.65.0, v1.67.0) that remain unverified against
  real logs or error rates, with a fifth feature set (the ShipSaving
  shipments integration, now five PRs deep on `dev`, v1.70.1) queued behind
  it.** The inspection routine has gone longer without signal than the
  interval between most of the releases it exists to watch. Fixing the
  environment access path remains the single highest-leverage action
  available — until then, each day's report can only restate and extend
  the same gap.

## Infra status

Unknown — not observed this run. No conclusions should be drawn about
`backend`, `Postgres`, `db-sync`, or `pg-r2-backup` deployment health, crash
loops, or error rates from this report. With thirty-six straight days of
zero visibility (2026-07-14 through 2026-08-18) and four production
releases unconfirmed, this remains an open risk for the inspection routine
itself to flag until infra access is fixed.
