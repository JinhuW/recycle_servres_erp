# Daily production-health inspection — 2026-08-10

## Summary verdict

⚠️ **INSPECTION INCOMPLETE — Railway access unavailable (28th consecutive day) — another release shipped blind**

This run could not reach Railway by any of the three sanctioned paths, so no
logs, deployment statuses, or HTTP error rates were collected for `backend`,
`Postgres`, `db-sync`, or `pg-r2-backup`. This is a report about the
inspection tooling, not a verdict on application health.

This is the same failure mode as every report from
[2026-07-14](./2026-07-14.md) through [2026-08-09](./2026-08-09.md) —
twenty-seven straight prior runs, now twenty-eight with today's. Nothing
about the environment changed overnight to fix it.

**Another release shipped inside this run's lookback window.** PR #123 —
"release: v1.62.6" — merged `dev`→`main` at 2026-08-10T00:55:34Z, about 15
hours before this run, inside the window (2026-08-09T16:08:44Z–
2026-08-10T16:14:13Z). Per `CLAUDE.md` a `dev`→`main` merge is a full
production release. This routine again has no way to confirm it deployed
cleanly or that it isn't erroring.

## What was tried

1. **Railway MCP server** — `ToolSearch` with `"railway logs deployments
   error rate metrics"` and `"railway"`. No matching tools found; only
   `github` and `Claude_Code_Remote` MCP servers are registered for this
   session.
2. **Railway GraphQL API fallback** (`https://backboard.railway.com/graphql/v2`)
   — checked for `RAILWAY_API_TOKEN` via `env | grep -i railway` (no
   output) and for a worktree `.env` (not present; `new-session.sh` warned
   `.env not found in the main checkout`). Probed the endpoint anyway:
   ```
   curl -sS -m 10 -o /dev/null -w "HTTP %{http_code}\n" \
     -X POST https://backboard.railway.com/graphql/v2 \
     -H "Content-Type: application/json" -d '{"query":"query{me{id}}"}'
   → curl: (56) CONNECT tunnel failed, response 403 (HTTP 000)
   ```
   Identical to every prior day — an explicit egress policy denial at the
   sandbox's proxy gateway, not a transient network blip and not a
   Railway-side failure.
3. **Railway CLI / cached credentials** — not present on `PATH`; not
   pursued further since the network-policy block above would stop a CLI
   call the same way regardless of credentials.
4. **Conclusion** — neither access path is configured in this environment,
   for the twenty-eighth day running.

## Secondary signal (GitHub, not Railway — informational only)

Does **not** substitute for backend log/error-rate visibility:

- `list_commits` on `main` since 2026-08-09T16:08:44Z shows commits through
  **PR #123** (2026-08-10T00:55:34Z, `dev`→`main`, tagged **v1.62.6**),
  folding in PR #122 (sell-orders inventory-by-quantity fix) and PR #121
  (legacy Other-line item-type backfill), both merged 2026-08-09 evening.
- `prod` branch: `list_commits(sha: "prod")` still returns nothing newer
  than 2026-07-21 — consistent with prior reports; `main` remains the
  actual deploy source, per `CLAUDE.md`'s note that `prod` is retired.
- Net effect: a real, recent, substantial change is running in production
  that this routine cannot verify.

## Errors found

Not collected this run (no data source available).

## Root-cause analysis

N/A — no logs were retrieved, so no error triage was possible this run.
For context once access is restored, the newest changes folded into
today's release (v1.62.6) are the highest-value places to check first:

- **PR #122** — `apps/backend/src/services/sellableInventory.ts` (see the
  comment at line 8 and `availableQty` at line 92). This *itself* fixed a
  production bug: a sell order that left Draft previously reserved an
  entire inventory lot regardless of the quantity named on the line, so
  partial sells hid the remainder from the sellable picker and a second
  order against the same lot was wrongly refused as "already on sell
  order." The fix now reserves the qty each committed line actually names.
  Worth confirming no regression in lot availability math (e.g.
  over-reservation or negative `availableQty`) post-release, since this
  path touches concurrent sell-order commits directly.
- **PR #121** — `apps/backend/migrations/0090_backfill_other_item_type.sql`
  and the classification logic it backfills from (`apps/backend/src/routes/inventory.ts`,
  `apps/backend/src/routes/itemTypes.ts`). Backfills `item_type` for ~84
  legacy `Other` inventory lines that were left NULL when `item_type`
  arrived in migration `0082`. The backfill is scoped to NULL rows only
  (won't overwrite hand-chosen values on re-run), but worth confirming the
  migration applied cleanly against prod's row count and that no
  unrecognised description got mis-keyed into `Server`/`HDD`/`CPU`/`GPU`.
- Migrations `0088`–`0090` are the newest in the tree (`0090` head as of
  this run) — worth a quick check that the `_migration_ledger` on prod
  Postgres shows all three applied without error, once DB access exists
  via Railway.

None of this is a claim that these are broken — only that they are the
newest, most invasive changes in the window this report covers, so they're
the first place to look once Railway access exists.

## Recommended fixes

Not application code — this is infra/session configuration for the
inspector routine itself, and it is now a repeat item for the
**twenty-eighth consecutive day**:

- Add a Railway MCP server to this session's environment (or the pool it
  runs in), **or** set `RAILWAY_API_TOKEN` as an environment variable
  available to this routine's session, scoped to the
  `recycle-erp-experiment` Railway project
  (`e811d744-769f-4418-9f22-f371fd889923`).
- The outbound proxy for this session/environment still returns `403`
  (policy denial) on `CONNECT` to `backboard.railway.com:443`, confirmed
  again today via direct `curl`. Whoever owns the environment's egress
  policy needs to add `backboard.railway.com` (or `*.railway.com` /
  `*.railway.app`) to the allowed-hosts list, or this routine will keep
  failing even after a token is supplied.
- Once both the network policy and a token are in place, verify with a
  minimal query against `https://backboard.railway.com/graphql/v2` before
  relying on it for the full 24h log pull.
- **This is now the twenty-eighth consecutive scheduled run with zero
  visibility**, and today is again materially worse than a quiet day
  because a real release (v1.62.6 / PR #123) shipped inside the blind
  window, on top of yesterday's v1.62.0 release which is also still
  unverified. Recommend a manual check of the `backend` service's Railway
  dashboard (deployment status + error rate) until access is restored —
  two unverified releases have now stacked up.

## Infra status

Unknown — not observed this run. No conclusions should be drawn about
`backend`, `Postgres`, `db-sync`, or `pg-r2-backup` deployment health,
crash loops, or error rates from this report. With twenty-eight straight
days of zero visibility (2026-07-14 through 2026-08-10), and two
production releases (v1.62.0, v1.62.6) having shipped in that blind
window without confirmation, this remains an open, worsening risk for the
inspection routine itself to flag until infra access is fixed.
