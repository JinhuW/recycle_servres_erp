node_modules/
dist/
build/
.wrangler/
.turbo/
.cache/
.vite/

# Local agent worktrees / TS build cache (not source)
.claude/
*.tsbuildinfo

# Env files
.env
.env.local
.env.*.local
.dev.vars

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp

# Terraform
infra/terraform/**/.terraform/*
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log
*.auto.tfvars
**/*.secret.tfvars
.terraform.lock.hcl

# Superpowers brainstorm/visual-companion artifacts
.superpowers/

# Local-only compose override (host-published Postgres for the test loop)
docker-compose.override.yml

# Bind-mounted Postgres data dir (created on `docker compose up`).
/data/

# Ad-hoc DB dumps (pg_dump output) — keep out of git, may contain real data.
*.sql.dump
recycle_erp_dump.sql
backups/
# Root-level ad-hoc SQL scratch + PO data backups (real data, manual fixes).
# Tracked SQL lives under apps/backend/, so scoping these to the root is safe.
/*.sql
/po*_backup_*/
