# Optional. Defaults are baked into docker-compose.yml so `docker compose up`
# Just Works on a fresh clone. Override anything below for prod.

# Postgres superuser password. Used by the `postgres` service and woven into
# the backend's DATABASE_URL. Defaults to `recycle` if unset.
POSTGRES_PASSWORD=recycle
