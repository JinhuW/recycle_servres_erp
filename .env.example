# Copy to .env and fill in. Required by docker-compose.yml.

# Postgres superuser password. Used by the `postgres` service and woven into
# the backend's DATABASE_URL. Compose fails fast if this is unset.
POSTGRES_PASSWORD=
